// 962
[clientscript,fairyrings_clockwise](int $int0)
%fairyrings_hasclicked = true;
def_int $int1 = 0;
def_int $int2 = 0;
if ($int0 = 51380233) {
    %fairyrings_left_offset = calc((%fairyrings_left_offset - 90 + 360) % 360);
    if (%fairyrings_left_direction = 1 & if_find(fairyrings:left_s) = true) {
        $int1, $int2 = ~script15524(%fairyrings_left_offset);
        if ($int2 <= $int1) {
            %fairyrings_left_direction = -1;
        }
    } else {
        %fairyrings_left_direction = -1;
    }
} else if ($int0 = 51380242) {
    %fairyrings_middle_offset = calc((%fairyrings_middle_offset - 90 + 360) % 360);
    if (%fairyrings_middle_direction = 1 & if_find(fairyrings:mid_s) = true) {
        $int1, $int2 = ~script15524(%fairyrings_middle_offset);
        if ($int2 <= $int1) {
            %fairyrings_middle_direction = -1;
        }
    } else {
        %fairyrings_middle_direction = -1;
    }
} else if ($int0 = 51380251) {
    %fairyrings_right_offset = calc((%fairyrings_right_offset - 90 + 360) % 360);
    if (%fairyrings_right_direction = 1 & if_find(fairyrings:right_s) = true) {
        $int1, $int2 = ~script15524(%fairyrings_right_offset);
        if ($int2 <= $int1) {
            %fairyrings_right_direction = -1;
        }
    } else {
        %fairyrings_right_direction = -1;
    }
}
