// 19618
[proc,script19618](int $int0, int $int1, int $int2, int $int3, int $int4, int $int5, int $int6, enum $enum7, enum $enum8, enum $enum9)(int, int)
def_int $int10 = $int4;
def_int $int11 = 0;
def_int $int12 = 0;
def_obj $obj13 = null;
def_int $int14 = 0;
if ($enum7 ! null) {
    $int12 = enum_getoutputcount($enum7);
    while ($int11 < $int12) {
        if (calc($int10 + $int5) >= $int3) {
            $int10 = $int4;
            $int2 = calc($int2 + ($int6 + 4));
        }
        ~cc_add_child_graphic($int0, $int1++, $int10, $int2, ^setpos_abs_left, ^setpos_abs_top, $int5, $int6, ^setsize_abs, ^setsize_abs, enum(int, graphic, $enum7, $int11));
        if ($enum8 ! null) {
            ~script14990(0, enum(int, string, $enum8, $int11));
        }
        $int10 = calc($int10 + ($int5 + 4));
        $int11 = calc($int11 + 1);
    }
}
if ($enum9 ! null) {
    $int11 = 0;
    $int12 = enum_getoutputcount($enum9);
    $int14 = scale($int6, 32, 36);
    while ($int11 < $int12) {
        if (calc($int10 + $int14) >= $int3) {
            $int10 = $int4;
            $int2 = calc($int2 + ($int6 + 4));
        }
        $obj13 = enum(int, obj, $enum9, $int11);
        ~script17950($int0, $int1++, $int10, $int2, ^setpos_abs_left, ^setpos_abs_top, $int14, $int6, ^setsize_abs, ^setsize_abs, $obj13, 1);
        ~script14738($obj13);
        $int10 = calc($int10 + ($int14 + 4));
        $int11 = calc($int11 + 1);
    }
}
if ($int10 ! $int4) {
    $int2 = calc($int2 + ($int6 + 4));
}
return($int1, $int2);
