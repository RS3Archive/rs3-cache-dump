// 20172
[proc,script20172](int $int0, int $int1, int $int2)
if ($int1 = -1) {
    return;
}
switch_int ($int0) {
    case 0 :
        if ($int2 = 1) {
            %league_task_grouping_tier_toggles = long_setbit(%league_task_grouping_tier_toggles, $int1);
        } else {
            %league_task_grouping_tier_toggles = long_clearbit(%league_task_grouping_tier_toggles, $int1);
        }
    case 1 :
        if ($int2 = 1) {
            %league_task_grouping_locality_toggles = long_setbit(%league_task_grouping_locality_toggles, $int1);
        } else {
            %league_task_grouping_locality_toggles = long_clearbit(%league_task_grouping_locality_toggles, $int1);
        }
    case default :
        ~error("Grouping <tostring($int0)> missing plugin.");
}
