// 13565
[proc,script13565](int $int0, int $int1)(int)
def_int $int2 = 1;
def_int $int3 = enum_getoutputcount(int_to_stat);
def_stat $stat4 = null;
while ($int2 <= $int3) {
    $stat4 = enum(int, stat, int_to_stat, $int2);
    if ($stat4 ! null) {
        if ($int1 = 1) {
            if (stat_base($stat4) >= $int0) {
                return(1);
            }
        } else if (stat_base($stat4) > $int0) {
            return(1);
        }
    }
    $int2 = calc($int2 + 1);
}
return(0);
