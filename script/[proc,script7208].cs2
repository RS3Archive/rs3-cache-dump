// 7208
[proc,script7208](string $string0, component $component0)
if (string_length($string0) > 0 & string_length(~script2332($string0, " ", "")) > 0) {
    if (%p_dialog_mode = 7) {
        resume_countdialog(~script7209($string0));
    } else if (%p_dialog_mode = 11) {
        resume_countdialog_long(~script17392($string0));
    } else if (%p_dialog_mode = 9) {
        resume_stringdialog($string0);
    }
} else {
    switch_component (%p_dialog_listener) {
    }
    abort_dialog;
    ~script10522($component0);
}
~script378;
~script8841(11, 0);
