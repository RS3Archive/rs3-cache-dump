// 2522
[proc,script2522](dbrow $dbrow0, int $int1)(boolean)
def_dbrow $dbrow2 = enum(int, dbrow, settings_all_categories, $int1);
if ($dbrow2 = null) {
    return(false);
}
def_int $int3 = 0;
def_int $int4 = db_getfieldcount($dbrow2, settings_category:col3);
while ($int3 < $int4) {
    if (db_getfield($dbrow2, settings_category:col3, $int3) = $dbrow0) {
        return(true);
    }
    $int3 = calc($int3 + 1);
}
return(false);
