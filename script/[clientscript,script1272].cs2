// 1272
[clientscript,script1272](component $component0)
def_string $string0 = if_gettext(toplevel_v2_minimap:alert_text);
if (compare("", $string0) ! 0) {
    ~script8800($string0, $component0, -1);
} else {
    ~script8800("No information to display at this time.", $component0, -1);
}
