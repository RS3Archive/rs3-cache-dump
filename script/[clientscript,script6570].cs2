// 6570
[clientscript,script6570](struct $struct0, int $int1, int $int2, int $int3, int $int4)
def_int $int5 = 0;
if (struct_param($struct0, combatv2_is_item_ability) = 1) {
    if ($int3 = 1) {
        %combatv2_cooldown_item_ability_1_start_client = clientclock;
    }
    if ($int4 = 1) {
        %combatv2_cooldown_item_ability_1_end_client = calc(($int2 - $int1) * 30 + clientclock);
    }
} else {
    switch_struct ($struct0) {
        case default :
            return;
        case combatv2_dummy_global_cooldown_ability :
            if ($int3 = 1) {
                %combatv2_global_cooldown_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_global_cooldown_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_dummy_interrupt_cooldown_ability :
            if ($int4 = 1) {
                %combatv2_interrupt_cooldown_end_client = calc(($int2 - $int1) * 30 + clientclock);
                return;
            }
        case combatv2_ability_attack_strike :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_strike_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_strike_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_charge, combatv2_ability_attack_charge_override :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_charge_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_charge_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_sever :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_sever_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_sever_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_adaptive_sweep :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_adaptive_sweep_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_adaptive_sweep_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_havoc :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_riposte_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_riposte_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_smash :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_smash_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_smash_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_backhand :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_backhand_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_backhand_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_haemorrhage :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_haemorrhage_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_haemorrhage_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_flurry, combatv2_ability_attack_flurry_override, combatv2_ability_attack_flurry_bloodlust, combatv2_ability_attack_flurry_override_bloodlust :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_flurry_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_flurry_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 7 * 30);
            }
        case combatv2_ability_attack_whirlwind, combatv2_ability_attack_whirlwind_bloodlust :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_whirlwind_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_whirlwind_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_overpower :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_overpower_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_overpower_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_eviscerate :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_eviscerate_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_eviscerate_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_meteor_strike :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_meteor_strike_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_meteor_strike_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_balanced_strike :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_balanced_strike_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_balanced_strike_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_blood_tendrils :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_blood_tendrils_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_blood_tendrils_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_attack_dive, combatv2_ability_attack_bladed_dive :
            if ($int3 = 1) {
                %combatv2_cooldown_attack_bladed_dive_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_attack_bladed_dive_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_dismember :
            if ($int3 = 1) {
                %combatv2_cooldown_melee_dismember_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_melee_dismember_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_melee_dismember_recast_1 :
            if ($int3 = 1) {
                %combatv2_cooldown_melee_dismember_recast_1_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_melee_dismember_recast_1_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_melee_dismember_recast_2 :
            if ($int3 = 1) {
                %combatv2_cooldown_melee_dismember_recast_2_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_melee_dismember_recast_2_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_punish :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_punish_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_punish_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_fury, combatv2_ability_strength_fury_override :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_fury_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_fury_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_smite :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_smite_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_smite_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_cleave :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_cleave_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_cleave_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_assault, combatv2_ability_strength_assault_bloodlust :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_assault_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_assault_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 3 * 30);
            }
        case combatv2_ability_strength_quake :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_quake_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_quake_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_destroy :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_destroy_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_destroy_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 3 * 30);
            }
        case combatv2_ability_strength_berserk :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_berserk_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_berserk_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_frenzy :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_frenzy_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_frenzy_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 7 * 30);
            }
        case combatv2_ability_strength_pulverise :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_pulverise_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_pulverise_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_strength_chaos_roar :
            if ($int3 = 1) {
                %combatv2_cooldown_strength_chaos_roar_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_strength_chaos_roar_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_strength_chaos_roar_end_effect_client = calc(clientclock + (struct_param($struct0, combatv2_ability_effect_duration) - 1) * 30);
            }
        case combatv2_ability_defence_anticipation :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_anticipation_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_anticipation_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_freedom :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_freedom_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_freedom_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_freedom_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_taunt :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_taunt_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_taunt_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_resonance :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_resonance_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_resonance_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_resonance_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_preparation :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_preparation_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_preparation_end_client = calc(($int2 - $int1) * 30 + clientclock);
                if (%invent_perk_preparation > 0) {
                    %combatv2_cooldown_defence_preparation_end_effect_client = calc(clientclock + scale(struct_param($struct0, combatv2_ability_effect_duration), 100, calc(100 + 15 * %invent_perk_preparation)) * 30);
                } else {
                    %combatv2_cooldown_defence_preparation_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
                }
            }
        case combatv2_ability_defence_bash :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_bash_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_bash_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_reflect :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_reflect_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_reflect_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_reflect_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_debilitate :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_debilitate_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_debilitate_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_revenge :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_revenge_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_revenge_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_revenge_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_barricade :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_barricade_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_barricade_end_client = calc(($int2 - $int1) * 30 + clientclock);
                $int5 = calc(8 + scale(~script17450, 90, 9));
                if (%invent_perk_turtling > 0) {
                    $int5 = calc($int5 + max(%invent_perk_turtling, scale($int5, 100, calc(%invent_perk_turtling * 10))));
                }
                switch_int (%pof_rewards_perk_animal_bgh_malletops) {
                    case 1 :
                        $int5 = calc($int5 + 3);
                    case 2 :
                        $int5 = calc($int5 + 6);
                }
                %combatv2_cooldown_defence_barricade_end_effect_client = calc(clientclock + $int5 * 30);
            }
        case combatv2_ability_defence_resurgence :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_resurgence_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_resurgence_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
            %combatv2_cooldown_defence_rejuvinate_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
        case combatv2_ability_defence_heroism :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_heroism_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_heroism_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_heroism_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_natural_instinct :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_natural_instinct_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_natural_instinct_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_natural_instinct_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_devotion :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_devotion_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_devotion_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_unsullied :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_unsullied_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_unsullied_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_aggression :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_aggression_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_aggression_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_defence_divert :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_divert_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_divert_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_divert_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_defence_stop_attacking :
            if ($int3 = 1) {
                %combatv2_cooldown_defence_stop_attacking_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_defence_stop_attacking_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_defence_stop_attacking_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_regenerate :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_regenerate_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_regenerate_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_guthix_blessing :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_guthix_blessing_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_guthix_blessing_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_constitution_guthix_blessing_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_sacrifice :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_sacrifice_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_sacrifice_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_constitution_sacrifice_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_transfigure :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_transfigure_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_transfigure_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_6awe2_blessing :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_6awe2_blessing_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_6awe2_blessing_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_constitution_6awe2_blessing_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_ice_asylum :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_ice_asylum_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_ice_asylum_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_constitution_ice_asylum_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_tuska_wrath :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_tuska_wrath_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_tuska_wrath_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_storm_shards :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_storm_shards_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_storm_shards_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_shatter :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_shatter_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_shatter_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_siphon :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_siphon_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_siphon_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_surging_onslaught :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_surging_onslaught_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_surging_onslaught_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_reprisal :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_reprisal_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_reprisal_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_constitution_reprisal_end_effect_client = calc(clientclock + struct_param($struct0, combatv2_ability_effect_duration) * 30);
            }
        case combatv2_ability_constitution_ingenuity_of_the_humans :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_ingenuity_of_the_humans_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_ingenuity_of_the_humans_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_demon_slayer :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_demon_slayer_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_demon_slayer_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_dragon_slayer :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_dragon_slayer_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_dragon_slayer_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_undead_slayer :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_undead_slayer_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_undead_slayer_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_limitless :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_limitless_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_limitless_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_slaying :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_slaying_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_slaying_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_constitution_ferocious :
            if ($int3 = 1) {
                %combatv2_cooldown_constitution_ferocious_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_constitution_ferocious_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_piercing_shot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_piercing_shot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_piercing_shot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_binding_shot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_binding_shot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_binding_shot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_escape :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_escape_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_escape_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_snipe :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_snipe_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_snipe_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 1 * 30);
            }
        case combatv2_ability_ranged_fragmentation_shot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_fragmentation_shot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_fragmentation_shot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_corruption :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_corruption_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_corruption_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_ricochet, combatv2_ability_ranged_greater_ricochet :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_ricochet_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_ricochet_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_snap_shot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_snap_shot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_snap_shot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_rapid_fire :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_rapid_fire_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_rapid_fire_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 7 * 30);
            }
        case combatv2_ability_ranged_bombardment :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_bombardment_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_bombardment_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_explosive_shot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_explosive_shot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_explosive_shot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_unload :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_unload_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_unload_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 3 * 30);
            }
        case combatv2_ability_ranged_headshot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_headshot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_headshot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_galeshot :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_galeshot_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_galeshot_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_deaths_swiftness, combatv2_ability_ranged_deaths_shroud :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_deaths_swiftness_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_deaths_swiftness_end_client = calc(($int2 - $int1) * 30 + clientclock);
                if (%invent_perk_plantedfeet > 0 & $struct0 = combatv2_ability_ranged_deaths_swiftness) {
                    %combatv2_cooldown_ranged_deaths_swiftness_end_effect_client = calc(clientclock + scale(calc(struct_param($struct0, combatv2_ability_effect_duration) - 1), 100, calc(100 + 26)) * 30);
                } else {
                    %combatv2_cooldown_ranged_deaths_swiftness_end_effect_client = calc(clientclock + (struct_param($struct0, combatv2_ability_effect_duration) - 1) * 30);
                }
            }
        case combatv2_ability_ranged_shadow_tendrils :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_shadow_tendrils_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_shadow_tendrils_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_puncture_arrow :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_puncture_arrow_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_puncture_arrow_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_demoralise :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_demoralise_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_demoralise_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_rout :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_rout_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_rout_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_ranged_imbue_shadows :
            if ($int3 = 1) {
                %combatv2_cooldown_ranged_imbue_shadows_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_ranged_imbue_shadows_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_chastise, combatv2_ability_magic_blood_tithe_wrack :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_chastise_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_chastise_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_surge :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_surge_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_surge_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_impact :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_impact_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_impact_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_impact2 :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_impact2_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_impact2_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_chain, combatv2_ability_magic_greater_chain :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_chain_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_chain_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_combust :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_combust_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_combust_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_corruption :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_corruption_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_corruption_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_dragonbreath :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_dragon_breath_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_dragon_breath_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_choke :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_choke_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_choke_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 3 * 30);
            }
        case combatv2_ability_magic_detonate :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_detonate_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_detonate_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_wild_magic :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_wild_magic_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_wild_magic_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_metamorphosis :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_metamorphosis_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_metamorphosis_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_magic_metamorphosis_end_effect_client = calc(clientclock + (struct_param($struct0, combatv2_ability_effect_duration) - 1) * 30);
            }
        case combatv2_ability_magic_tsunami :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_tsunami_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_tsunami_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_overload :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_overload_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_overload_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_sonic_boom, combatv2_ability_magic_greater_sonic_wave :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_sonic_boom_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_sonic_boom_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_concentrated_blast, combatv2_ability_magic_greater_concentrated_blast :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_concentrated_blast_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_concentrated_blast_end_client = calc(($int2 - $int1) * 30 + clientclock);
                if ($struct0 = combatv2_ability_magic_greater_concentrated_blast) {
                    %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 2 * 30);
                } else {
                    %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 2 * 30);
                }
            }
        case combatv2_ability_magic_sunshine, combatv2_ability_magic_solar_charge :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_sunshine_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_sunshine_end_client = calc(($int2 - $int1) * 30 + clientclock);
                if (%invent_perk_plantedfeet > 0 & $struct0 = combatv2_ability_magic_sunshine) {
                    %combatv2_cooldown_magic_sunshine_end_effect_client = calc(clientclock + scale(calc(struct_param($struct0, combatv2_ability_effect_duration) - 1), 100, calc(100 + 26)) * 30);
                } else {
                    %combatv2_cooldown_magic_sunshine_end_effect_client = calc(clientclock + (struct_param($struct0, combatv2_ability_effect_duration) - 1) * 30);
                }
            }
        case combatv2_ability_magic_smoke_tendrils :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_smoke_tendrils_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_smoke_tendrils_end_client = calc(($int2 - $int1) * 30 + clientclock);
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 3 * 30);
            }
        case combatv2_ability_magic_shock :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_shock_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_shock_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_horror :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_horror_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_horror_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case magic_vengeance, magic_vengeance_other, magic_vengeance_group :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_vengeance_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_vengeance_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case magic_disruption_shield :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_disruption_shield_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_disruption_shield_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_golden_touch :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_golden_touch_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_golden_touch_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_magic_magma_tempest, combatv2_ability_magic_magma_tempest_revolution :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_magma_tempest_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_magma_tempest_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case magic_temporal_anomaly :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_temporal_anomaly_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_temporal_anomaly_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case magic_confuse, magic_curse, magic_enfeeble, magic_smoke_cloud, magic_stagger, magic_teleblock, magic_vulnerability, magic_weaken, magic_bind, magic_entangle, magic_snare :
            if ($int3 = 1) {
                %combatv2_cooldown_magic_combat_spell_shared_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_magic_combat_spell_shared_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_death_touch :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_death_touch_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_death_touch_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_soul_sap :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_soul_sap_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_soul_sap_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_conjure_skeleton_warrior :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_conjure_skeleton_warrior_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_conjure_skeleton_warrior_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_command_skeleton_warrior :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_command_skeleton_warrior_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_command_skeleton_warrior_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_finger_of_death :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_finger_of_death_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_finger_of_death_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_soul_strike :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_soul_strike_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_soul_strike_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_volley_of_souls :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_volley_of_souls_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_volley_of_souls_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_bloat :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_bloat_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_bloat_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_blood_siphon :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_blood_siphon_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_blood_siphon_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
            %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 2 * 4 * 30);
        case combatv2_ability_necromancy_spectral_scythe :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_spectral_scythe_recast_1 :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_recast_1_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_recast_1_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_spectral_scythe_recast_2 :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_recast_2_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_spectral_scythe_recast_2_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_death_skulls :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_death_skulls_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_death_skulls_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_living_death :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_living_death_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_living_death_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_conjure_putrid_zombie :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_conjure_putrid_zombie_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_conjure_putrid_zombie_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_command_putrid_zombie :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_command_putrid_zombie_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_command_putrid_zombie_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_conjure_vengeful_ghost :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_conjure_vengeful_ghost_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_conjure_vengeful_ghost_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_command_vengeful_ghost :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_command_vengeful_ghost_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_command_vengeful_ghost_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_life_transfer :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_life_transfer_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_life_transfer_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_lesser_bone_shield :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_lesser_bone_shield_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_lesser_bone_shield_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_greater_bone_shield :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_greater_bone_shield_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_greater_bone_shield_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_threads_of_fate :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_threads_of_fate_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_threads_of_fate_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_invoke_death :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_invoke_death_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_invoke_death_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_darkness :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_darkness_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_darkness_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_split_soul :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_split_soul_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_split_soul_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_invoke_lord_of_bones :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_invoke_lord_of_bones_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_invoke_lord_of_bones_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_conjure_phantom_guardian :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_conjure_phantom_guardian_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_conjure_phantom_guardian_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_necromancy_command_phantom_guardian :
            if ($int3 = 1) {
                %combatv2_cooldown_necromancy_command_phantom_guardian_start_client = clientclock;
            }
            if ($int4 = 1) {
                %combatv2_cooldown_necromancy_command_phantom_guardian_end_client = calc(($int2 - $int1) * 30 + clientclock);
            }
        case combatv2_ability_mah5_armadyl_battlestaff_dummy :
            if ($int4 = 1) {
                %combatv2_cooldown_channelling_end_effect_client = calc(clientclock + 1 * 4 * 30);
            }
    }
}
~combatv2_cooldown_start($struct0);
