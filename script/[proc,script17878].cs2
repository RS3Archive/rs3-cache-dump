// 17878
[proc,script17878](dbrow $dbrow0, int $int1)(int, dbrow, dbrow, dbrow, dbrow, dbrow)
if ($dbrow0 = null) {
    ~error("Invalid pass.");
    return(0, null, null, null, null, null);
}
if ($int1 = ~script17892($dbrow0)) {
    return(~script17879($dbrow0));
}
$int1 = max(0, $int1);
~script17927;
def_int $int2 = 0;
def_dbrow $dbrow3 = null;
def_dbrow $dbrow4 = null;
def_dbrow $dbrow5 = null;
def_dbrow $dbrow6 = null;
def_dbrow $dbrow7 = null;
def_int $int8 = calc($int1 - 1);
def_int $int9 = db_getfieldcount($dbrow0, bp3_table:col8);
def_int $int10 = 0;
def_dbrow $dbrow11 = null;
while ($int8 < $int9) {
    $int10, $dbrow11 = db_getfield($dbrow0, bp3_table:col8, $int8);
    if ($int10 = $int1) {
        switch_int ($int2) {
            case 0 :
                $dbrow3 = $dbrow11;
            case 1 :
                $dbrow4 = $dbrow11;
            case 2 :
                $dbrow5 = $dbrow11;
            case 3 :
                $dbrow6 = $dbrow11;
            case 4 :
                $dbrow7 = $dbrow11;
            case default :
                ~error("We need to add more possible index's, we're only expecting 5 rewards per level.");
        }
        $int2 = calc($int2 + 1);
    } else if ($int10 > $int1) {
        return($int2, $dbrow3, $dbrow4, $dbrow5, $dbrow6, $dbrow7);
    }
    $int8 = calc($int8 + 1);
}
return($int2, $dbrow3, $dbrow4, $dbrow5, $dbrow6, $dbrow7);
