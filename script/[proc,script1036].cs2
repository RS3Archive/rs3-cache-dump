// 1036
[proc,script1036]()(chatcat)
def_chatcat $chatcat0 = null;
if (enum(int, int, gamearea_type, %gamearea_id) > 0) {
    $chatcat0 = enum(int, chatcat, gamearea_contextmenu, %gamearea_id);
    if ($chatcat0 ! null) {
        return($chatcat0);
    }
}
return(enum(int, chatcat, quickchat_skillcontext, %quickchat_lastskill));
