// 15628
[proc,script15628]
def_int $int0 = 0;
def_int $int1 = 550;
def_int $int2 = 56;
def_int $int3 = 50;
def_int $int4 = 0;
def_boolean $boolean5 = ~is_mobile_client;
if ($boolean5 = false) {
    $int1 = 546;
    $int2 = 106;
    if (%lobby_current_tab = 2 | %lobby_current_tab = 4 | %lobby_current_tab = 3) {
        $int0 = 1;
        $int1 = 0;
    }
}
if (%lobby_current_tab = 1 & map_lang = 0) {
    $int0 = 1;
    $int1 = 0;
}
if (if_gethide(lobbyscreen:autologout_warning) = false) {
    $int4 = 30;
    if (if_gethide(lobbyscreen:update_timer) = true) {
        if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_centre, lobbyscreen:autologout_warning);
    } else {
        if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_top, lobbyscreen:autologout_warning);
        if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_bottom, lobbyscreen:update_timer);
    }
} else if (if_gethide(lobbyscreen:update_timer) = false) {
    $int4 = 30;
    if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_centre, lobbyscreen:update_timer);
}
def_int $int6 = 0;
def_int $int7 = 0;
def_int $int8 = 0;
def_int $int9 = 0;
if (if_gethide(lobbyscreen:preload_cache) = false) {
    $int6, $int7, $int8, $int9 = ~script2956;
    if_setsize(calc($int6 + $int8), if_getheight(preload_cache:base), ^setsize_minus, ^setsize_abs, preload_cache:base);
    if_setposition($int6, if_gety(preload_cache:base), ^setpos_abs_left, ^setpos_abs_top, preload_cache:base);
    $int4 = calc($int4 + if_getheight(preload_cache:base));
}
if (%lobby_current_tab = 5) {
    $int2 = 56;
    $int3 = 0;
}
if ($boolean5 = false) {
    if ($int0 = 0) {
        if_setsize(800, calc($int1 + $int4), ^setsize_abs, ^setsize_abs, lobbyscreen:mainmodal_window);
    } else {
        if_setsize(800, $int1, ^setsize_abs, ^setsize_minus, lobbyscreen:mainmodal_window);
    }
    if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_top, lobbyscreen:mainmodal_window);
}
if_setsize(0, calc($int2 + $int4), ^setsize_minus, ^setsize_minus, lobbyscreen:tabbed_content_panel);
if_setsize(0, $int4, ^setsize_minus, ^setsize_abs, lobbyscreen:warning_area);
if_setposition(0, $int3, ^setpos_abs_centre, ^setpos_abs_bottom, lobbyscreen:warning_area);
switch_int (%lobby_current_tab) {
    case 0 :
        ~script15633;
        ~script10941;
    case 2 :
        if ($boolean5 = false) {
            if_setontimer("script10055", lobbyscreen_pane_friendslist:main);
        } else {
            if_setontimer("script10055", lobbyscreen_pane_mobile_social:main);
        }
    case 3 :
        if ($boolean5 = false) {
            ~script11557;
        }
    case 4 :
        if ($boolean5 = false) {
            ~script11460;
        }
    case 5 :
        ~script15590(2);
}
def_int $setposv10 = ^setpos_abs_centre;
if ($boolean5 = true) {
    $setposv10 = ^setpos_abs_top;
}
if_setposition(0, 0, ^setpos_abs_centre, $setposv10, lobbyscreen_report_abuse_stage1:mainmodal_window);
if_setposition(0, 0, ^setpos_abs_centre, $setposv10, lobbyscreen_report_abuse_stage2:mainmodal_window);
if_setposition(0, 0, ^setpos_abs_centre, $setposv10, lobbyscreen_report_abuse_ignore:mainmodal_window);
