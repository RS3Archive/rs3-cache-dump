// 8575
[proc,script8575](int $int0)
if (%prayer_quick_number_of_presets = 0) {
    if_sethide(true, prayer_quick_preset_interface:scrollbar_layer);
} else {
    if_sethide(false, prayer_quick_preset_interface:scrollbar_layer);
}
cc_deleteall(prayer_quick_preset_interface:preset_button_layer);
cc_deleteall(prayer_quick_preset_interface:preset_name_layer);
cc_deleteall(prayer_quick_preset_interface:preset_rubbish_btn_layer);
cc_deleteall(prayer_quick_preset_interface:preset_ability_layer);
cc_deleteall(prayer_quick_preset_interface:preset_edit_btn_layer);
def_int $int1 = 0;
def_int $int2 = 0;
while ($int1 = 0) {
    if (~script2222($int2) = 0) {
        ~script8573($int2);
        $int1 = 1;
    } else {
        ~script8572($int2);
        ~script8574($int2);
    }
    if (++$int2 > 9) {
        $int1 = 1;
        return;
    }
}
