// 13877
[clientscript,script13877]
if (has_nxt = false) {
    return;
}
if (%loadingscreen_on ! 1) {
    if_setontimer(null, toplevel_v2:map_loaded_tracker);
    map_loadingscreen_settriggerpercent(0, 0);
} else {
    if_setontimer("script8298", toplevel_v2:map_loaded_tracker);
    map_loadingscreen_settriggerpercent(20, 90);
}
