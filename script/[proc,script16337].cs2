// 16337
[proc,script16337](struct $struct0)(string)
def_string $string0 = struct_param($struct0, bslay_name);
def_string $string1 = struct_param($struct0, bslay_name);
def_int $int1 = -1;
def_int $int2 = 23;
while ($int2 <= 26) {
    if (~script10509($int2) = true | %weekends_boss_spotlight_week_tracker = $int2) {
        $int1 = $int2;
        while ($int1 <= 26) {
            if (~script9851($int1, $struct0) = true & $int1 = $int2 & %weekends_boss_spotlight_week_tracker <= $int2) {
                if (%weekends_boss_spotlight_week_tracker ! 0) {
                    $string0 = "<col=FF00><$string1></col>";
                }
            } else if (~script9851($int1, $struct0) = true & $int1 = calc($int2 + 1)) {
                $string0 = "<col=F99222><$string1></col>";
            }
            $int1 = calc($int1 + 1);
        }
    }
    $int2 = calc($int2 + 1);
}
return($string0);
