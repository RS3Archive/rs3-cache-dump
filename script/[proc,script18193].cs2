// 18193
[proc,script18193](dbrow $dbrow0, int $int1, int $int2, int $int3, string $string0, int $int4, int $int5, int $int6)(dbrow, int)
def_dbrow $dbrow7 = null;
def_int $int8 = 0;
if (~script18206($dbrow0) = false) {
    $dbrow7 = db_find_get($int1);
    if ($dbrow7 ! null) {
        return($dbrow7, $int2);
    }
}
if ($dbrow7 = null & $int2 < $int1 & $int3 = 1) {
    while ($int2 < $int1) {
        $dbrow7 = ~script18189($int8, $string0, $int4, $int5, $int6);
        $int2 = calc($int2 + 1);
        if (db_getfieldcount($dbrow7, mtxmgt_cosmetic:col17) > 0) {
            $int8 = calc(db_getfield($dbrow7, mtxmgt_cosmetic:col17, 0) + 1);
        }
    }
}
if (db_getfieldcount($dbrow0, mtxmgt_cosmetic:col17) > 0) {
    $int8 = calc(db_getfield($dbrow0, mtxmgt_cosmetic:col17, 0) + 1);
}
if ($int3 = 1) {
    $dbrow7 = ~script18189($int8, $string0, $int4, $int5, $int6);
    if ($dbrow7 ! null) {
        return($dbrow7, calc($int2 + 1));
    }
    if (~script18195($string0) = true) {
        $dbrow7 = ~script18190;
        if ($dbrow0 ! $dbrow7) {
            return($dbrow7, calc($int2 + 1));
        }
    }
}
return(null, $int2);
