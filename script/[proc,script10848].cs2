// 10848
[proc,script10848](int $int0)(string)
if ($int0 < 0) {
    return("00:00");
}
def_int $int1 = scale(3, 5, $int0);
def_int $int2 = calc($int1 / 60);
def_int $int3 = 0;
if ($int2 >= 60) {
    $int3 = calc($int2 / 60);
    $int2 = calc($int2 % 60);
}
$int1 = calc($int1 % 60);
if ($int3 > 0) {
    if ($int2 >= 10) {
        if ($int1 >= 10) {
            return("<tostring($int3)>:<tostring($int2)>:<tostring($int1)>");
        } else {
            return("<tostring($int3)>:<tostring($int2)>:0<tostring($int1)>");
        }
    } else if ($int1 >= 10) {
        return("<tostring($int3)>:0<tostring($int2)>:<tostring($int1)>");
    } else {
        return("<tostring($int3)>:0<tostring($int2)>:0<tostring($int1)>");
    }
} else if ($int2 >= 10) {
    if ($int1 >= 10) {
        return("<tostring($int2)>:<tostring($int1)>");
    } else {
        return("<tostring($int2)>:0<tostring($int1)>");
    }
} else if ($int1 >= 10) {
    return("0<tostring($int2)>:<tostring($int1)>");
} else {
    return("0<tostring($int2)>:0<tostring($int1)>");
}
if ($int1 >= 10) {
    return("<tostring($int2)>:<tostring($int1)></col>");
}
return("<tostring($int2)>:0<tostring($int1)></col>");
