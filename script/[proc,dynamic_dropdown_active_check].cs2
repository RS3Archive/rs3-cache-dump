// 10454
[proc,dynamic_dropdown_active_check](component $component0, enum $enum1, int $int2)(int)
switch_component ($component0) {
    case group_parent:settings_click_area :
        if (~script10394($enum1, $int2) = true) {
            return(0);
        }
    case telemetry:add_row_button_active_layer, toplevel_v2_parent_suboverlay_telemetry:add_row_button_active_layer :
        if (~telemetry_member = 0) {
            switch_int ($int2) {
                case 31, 32 :
                    return(0);
            }
        }
    case toplevel_v2_parent_suboverlay_telemetry_settings:dropdown_click_layer :
        if ($enum1 = telemetry_interval_options & ~telemetry_member = 0) {
            switch_int ($int2) {
                case 1, 2 :
                    return(0);
            }
        }
    case escape_menu:dropdown_click_load :
        if (~script12542($int2) = true) {
            return(0);
        }
    case toplevel_v2_edit_mode:dropdown_click_load :
        if (~script12542($int2) = true) {
            return(0);
        }
    case mobile_chat_overlay:channel_dropdown_click :
        if (~script15847($int2) = 0) {
            return(0);
        }
}
switch_enum ($enum1) {
    case actionbar_visible_settings :
        if (~script11805($int2) = true) {
            return(0);
        }
}
return(1);
