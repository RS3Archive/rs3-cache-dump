// 656
[proc,script656]
if (if_find(toplevel_v2_combat_bar:spell_icon) = true) {
    cc_clearops;
    cc_clearscripthooks;
}
if (if_find(topstat_lore:spell_icon) = true) {
    cc_clearops;
    cc_clearscripthooks;
}
if (if_find(toplevel_v2_combat_bar_mobile:spell_icon) = true) {
    cc_clearops;
    cc_clearscripthooks;
}
