// 16323
[proc,trh164_get_dbrow](int $int0)(dbrow)
db_find(trh164_day:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null & $int0 ! 8677) {
    ~error("No data was found for advent calendar on the specified date: <tostring($int0)>");
}
return($dbrow1);
