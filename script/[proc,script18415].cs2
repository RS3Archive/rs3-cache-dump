// 18415
[proc,script18415](int $int0, dbrow $dbrow1)(boolean)
if ($int0 ! -1 & $dbrow1 ! null & db_getfieldcount($dbrow1, buff_select_event_day:col0) >= $int0) {
    return(true);
}
~error("The current day of the running buff_select_event doesn't have enough buffs for all buff selection buttons.");
return(false);
