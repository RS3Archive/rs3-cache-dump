// 16579
[proc,script16579](struct $struct0, int $int1)(string)
switch_int (%mtx_front_end_confirm_shop_type) {
    case 0 :
        if (struct_param($struct0, marketplace_spotlight_membership_discount) = 0) {
            return("");
        }
        if (~script3921($int1) <= 0) {
            return("");
        }
    case 2 :
        if (%rewardshop_struct = null) {
            return("");
        }
        if (struct_param(%rewardshop_struct, big_window_rewardshop_runecoins) = false) {
            return("");
        }
        if (struct_param($struct0, rewardshop_cost_f2p) < 0) {
            return("");
        }
}
if (playermember = true) {
    return("As you're a member, this item is 10% cheaper for you! We've already added that discount to the price.");
}
return("If you were a member, this item would be 10% cheaper for you!");
