// 2301
[proc,quickchat_onkey_search](int $key0, char $char1, int $int2)
def_component $component3 = ~script8599($int2);
def_component $component4 = ~script8596($int2);
def_component $component5 = ~script8595($int2);
def_component $component6 = ~script8597($int2);
def_int $int7 = calc(if_getnextsubid($component5) - 1);
def_int $int8 = string_length(~script8611($int2));
def_int $int9 = chatphrase_find(~script8611($int2), false);
def_int $int10 = 0;
def_chatphrase $chatphrase11 = null;
switch_int ($key0) {
    case ^key_page_up :
        if ($int7 = 0) {
            return;
        }
        if (~script8617($int2) <= 0) {
            ~script8618($int2, calc($int7 - 1));
        } else {
            ~script8618($int2, calc(~script8617($int2) - 1));
        }
        if (cc_find($component5, calc(~script8617($int2) + 1)) = true) {
            if (.cc_find($component5, 0) = true) {
                .cc_setposition(cc_getx, cc_gety, ^setpos_abs_left, ^setpos_abs_top);
                .cc_setsize(cc_getwidth, cc_getheight, ^setsize_abs, ^setsize_abs);
                .cc_setcolour(0x577e45);
                .cc_setfill(true);
            }
            if (cc_gety < if_getscrolly($component5)) {
                ~scrollbar_resize($component6, $component5, calc(if_getscrolly($component5) - cc_getheight));
            } else if (calc(cc_gety + cc_getheight) > calc(if_getheight($component5) + if_getscrolly($component5))) {
                ~scrollbar_resize($component6, $component5, if_getscrollheight($component5));
            }
        }
        return;
    case ^key_page_down :
        if ($int7 = 0) {
            return;
        }
        if (~script8617($int2) = calc($int7 - 1)) {
            ~script8618($int2, 0);
        } else {
            ~script8618($int2, calc(~script8617($int2) + 1));
        }
        if (cc_find($component5, calc(~script8617($int2) + 1)) = true) {
            if (.cc_find($component5, 0) = true) {
                .cc_setposition(cc_getx, cc_gety, ^setpos_abs_left, ^setpos_abs_top);
                .cc_setsize(cc_getwidth, cc_getheight, ^setsize_abs, ^setsize_abs);
                .cc_setcolour(0x577e45);
                .cc_setfill(true);
            }
            if (calc(cc_gety + cc_getheight) > calc(if_getheight($component5) + if_getscrolly($component5))) {
                ~scrollbar_resize($component6, $component5, calc(if_getscrolly($component5) + cc_getheight));
            } else if (cc_gety < if_getscrolly($component5)) {
                ~scrollbar_resize($component6, $component5, 0);
            }
        }
        return;
    case ^key_home :
        ~quickchat_open(~script8613($int2), ~script8605($int2), $int2);
        return;
    case ^key_escape :
        ~quickchat_close($int2);
        return;
    case ^key_return :
        if (~script8617($int2) < 0) {
            if ($int8 > 0) {
                ~script8622($int2, 0);
                if_setontimer(null, $component4);
                ~script1904($component3, $component5, $component6, $int2);
            } else {
                ~quickchat_close($int2);
            }
        } else if ($int9 > 0 & cc_find($component5, calc(~script8617($int2) + 1)) = true) {
            while ($int10 < $int9) {
                $chatphrase11 = chatphrase_findnext;
                if (compare(cc_gettext, chatphrase_gettext($chatphrase11)) = 0) {
                    ~quickchat_phrase($component3, $chatphrase11, 0, $int2);
                    return;
                }
                $int10 = calc($int10 + 1);
            }
        }
        return;
    case ^key_backspace :
        if ($int8 > 0) {
            ~script8612($int2, substring(~script8611($int2), 0, calc($int8 - 1)));
        } else if (~script8615($int2) = false) {
            ~quickchat_close($int2);
        } else {
            ~script8618($int2, -1);
            if_sethide(true, ~script8588($int2));
            if_sethide(true, ~script8590($int2));
            if_sethide(true, ~script8594($int2));
            if_sethide(false, ~script8598($int2));
            if_sethide(false, ~script8599($int2));
            if_sethide(true, ~script8600($int2));
            ~quickchat_return($component3, 0, $int2);
            return;
        }
    case default :
        if (char_isprintable($char1) = true & $int8 < 80) {
            ~script8612($int2, removetags(append_char(~script8611($int2), $char1)));
        } else {
            return;
        }
}
if_settext("Search for: <~script8611($int2)>*", $component4);
if_settextshadow(true, $component4);
~script8622($int2, 50);
if_setontimer("script1903($component3, $component4, $component5, $component6, $int2)", $component4);
