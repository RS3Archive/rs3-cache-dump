// 20782
[proc,script20782]
if (locselect_count > 0) {
    if (%construction_house_mousespace_room_added_from_door = 0) {
        if (~script6431 = true) {
            if (~script20783(%construction_house_mousespace_locked_coord) = false) {
                return;
            }
            ~script20779(%construction_house_mousespace_locked_coord);
        } else {
            ~script20779(%construction_house_mousespace_current_coord);
        }
    }
    locselect_submit;
    ~script20777;
}
