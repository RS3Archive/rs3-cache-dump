// 2290
[proc,runepass_get_reward_string](struct $struct0, int $int1)(string)
if ($int1 = 1 & string_length(struct_param($struct0, runepass_reward_desc)) > 0) {
    return(struct_param($struct0, runepass_reward_desc));
}
if (string_length(struct_param($struct0, runepass_reward_name)) > 0) {
    return(struct_param($struct0, runepass_reward_name));
}
def_string $string0 = ~script17935($struct0);
if (string_length($string0) > 0) {
    return($string0);
}
return("???");
