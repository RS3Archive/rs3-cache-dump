// 1885
[proc,script1885]
def_struct $struct0 = null;
~script13992(pop_voyage_list:list_1_background, null, rs3tli_3x3_icon_button, ~equal_int(%pop_voyage_list_selected, 1));
~script13992(pop_voyage_list:list_2_background, null, rs3tli_3x3_icon_button, ~equal_int(%pop_voyage_list_selected, 2));
~script13992(pop_voyage_list:list_3_background, null, rs3tli_3x3_icon_button, ~equal_int(%pop_voyage_list_selected, 3));
switch_int (%pop_voyage_list_selected) {
    case 1 :
        if (%pop_voyage_list_mode = 0) {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_random_1_id);
        } else {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_story_1_id);
        }
    case 2 :
        if (%pop_voyage_list_mode = 0) {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_random_2_id);
        } else {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_story_2_id);
        }
    case 3 :
        if (%pop_voyage_list_mode = 0) {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_random_3_id);
        } else {
            $struct0 = enum(int, struct, pop_voyage_list, %pop_voyage_story_3_id);
        }
}
~script1951($struct0);
