// 3376
[proc,script3376](component $component0, component $component1, int $int2, int $int3, int $int4, int $int5, enum $enum6, enum $enum7, int $int8, string $string0, int $int9, int $int10, int $int11, string $string1, boolean $boolean12)(int, int)
if ($enum6 = null | $component0 = null | $component1 = null) {
    return($int3, $int5);
}
if ($int8 = -1) {
    $int8 = calc(enum_getoutputcount($enum6) - 1);
    if ($enum6 = graphics_options_fullscreen_resolution) {
        $int8 = calc(fullscreen_modecount - 1);
    }
}
if ($int9 = -1 | $int9 > $int8) {
    $int9 = 0;
}
def_string $string2 = "";
if (string_length($string1) = 0) {
    switch_enum ($enum6) {
        case graphics_options_fullscreen_resolution :
            $string2 = ~script12292(~graphics_options_resolutions($int9));
        case actionbar_visible_settings, combatv2_actionbar_bar_list, actionbar_names_mobile :
            $string2 = ~script16005($enum6, $int9);
        case distinction_cape_recols_from_string :
            $string2 = "Custom presets";
        case construction_house_exterior_style_names :
            $string2 = "Select exterior style";
        case default :
            if (enum_hasoutput(enum, cheevo_paths, $enum6) = true) {
                if (enum(int, struct, $enum6, $int9) = null) {
                    $string2 = "Select a Path";
                } else {
                    $string2 = ~script12292(struct_param(enum(int, struct, $enum6, $int9), cheevo_path_name));
                }
            } else {
                $string2 = ~script12292(enum(int, string, $enum6, $int9));
            }
    }
} else {
    $string2 = $string1;
}
def_int $int13 = 0;
if (string_length($string2) = 0) {
    $string2 = "Select an element";
}
def_int $int14 = calc(if_getnextsubid($component0) + 3);
if ($int10 = 0) {
    if_clearops($component1);
    if_clearscripthooks($component1);
    if ($boolean12 = false) {
        if_setop(1, "View options", $component1);
        if_setonop("dynamic_dropdown_open($component1, -1, $component0, $int14, $enum6, $enum7, $int8, $int9, $int10, $int11)", $component1);
    }
    ~script13971($component0, null, rs3_dropdown, $string2, $boolean12);
} else if ($int10 = 1) {
    cc_create($component1, ^iftype_text, $int5);
    cc_setsize($int4, 24, ^setsize_abs, ^setsize_abs);
    cc_setposition($int2, $int3, ^setpos_abs_left, ^setpos_abs_top);
    cc_clearops;
    cc_clearscripthooks;
    if ($boolean12 = false) {
        cc_setop(1, "View options");
        cc_setonop("dynamic_dropdown_open(event_com, event_comsubid, $component0, $int14, $enum6, $enum7, $int8, $int9, $int10, $int11)");
    }
    ~script13980($component0, null, rs3_dropdown, $int2, $int3, $int4, -1, $boolean12, $string2);
}
return(calc($int3 + 24 + 4), calc($int5 + 1));
