// 19119
[proc,script19119]()(dbrow)
db_find(trh201_modifier:col17, ~date_runeday);
def_dbrow $dbrow0 = db_findnext;
if ($dbrow0 = null) {
    ~error("No active modifier found for today's date, defaulting.");
    $dbrow0 = trh201_modifier_oddments;
}
return($dbrow0);
