// 13573
[proc,script13573]()(boolean)
def_int $int0 = 1;
def_stat $stat1 = enum(int, stat, int_to_stat, $int0);
while ($stat1 ! null) {
    if ($stat1 ! invention & (stat_base($stat1) > 5 & $int0 ! 6 | stat_base($stat1) > 15)) {
        return(false);
    }
    $int0 = calc($int0 + 1);
    $stat1 = enum(int, stat, int_to_stat, $int0);
}
return(true);
