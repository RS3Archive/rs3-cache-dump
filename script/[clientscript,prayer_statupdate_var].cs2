// 7820
[clientscript,prayer_statupdate_var](component $component0, enum $enum1)
def_int $int2 = 0;
def_struct $struct3 = null;
while (cc_find($component0, $int2) = true) {
    $struct3 = enum(int, struct, $enum1, $int2);
    $struct3 = ~script9101($struct3);
    ~prayer_set_graphics($struct3, $component0, $int2);
    $int2 = calc($int2 + 1);
}
