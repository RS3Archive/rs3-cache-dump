// 5371
[clientscript,script5371](int $int0, int $int1, int $int2)
if ($int0 < 1) {
    if_settext("-", agidad_overlay:num_2);
}
if ($int0 < 2) {
    if_settext("-", agidad_overlay:num_3);
}
if ($int0 < 3) {
    if_settext("-", agidad_overlay:num_4);
}
if ($int0 < 4) {
    if_settext("-", agidad_overlay:num_5);
}
if ($int0 < 5) {
    if_settext("-", agidad_overlay:num_6);
}
if ($int0 < 6) {
    if_settext("-", agidad_overlay:num_7);
}
switch_int ($int0) {
    case 0 :
        if_settext(tostring($int1), agidad_overlay:num_1);
    case 1 :
        if_settext(tostring($int1), agidad_overlay:num_2);
    case 2 :
        if_settext(tostring($int1), agidad_overlay:num_3);
    case 3 :
        if_settext(tostring($int1), agidad_overlay:num_4);
    case 4 :
        if_settext(tostring($int1), agidad_overlay:num_5);
    case 5 :
        if_settext(tostring($int1), agidad_overlay:num_6);
    case 6 :
        if_settext(tostring($int1), agidad_overlay:num_7);
}
switch_int ($int0) {
    case 0 :
        ~script5373(agidad_overlay:num_2, $int2);
    case 1 :
        ~script5373(agidad_overlay:num_3, $int2);
    case 2 :
        ~script5373(agidad_overlay:num_4, $int2);
    case 3 :
        ~script5373(agidad_overlay:num_5, $int2);
    case 4 :
        ~script5373(agidad_overlay:num_6, $int2);
    case 5 :
        ~script5373(agidad_overlay:num_7, $int2);
}
def_int $int3 = calc(63 * $int0 - 189);
def_int $int4 = calc($int3 + 63);
if_setontimer("script5372(agidad_overlay:marker_current, $int3)", agidad_overlay:marker_current);
if_setontimer("script5372(agidad_overlay:marker_new, $int4)", agidad_overlay:marker_new);
def_int $int5 = calc(if_getx(agidad_overlay:marker_current) + if_getwidth(agidad_overlay:marker_current) / 2 - if_getwidth(agidad_overlay:numbers) / 2);
if ($int5 < $int3) {
    sound_vorbis_volume(agidad_hi_lo_slide, 1, 0, 180);
}
