// 6472
[proc,script6472](boolean $boolean0)
def_enum $enum1 = mtxmgt_subcat_headers;
def_int $int2 = calc(enum_getoutputcount($enum1) - 1);
def_enum $enum3 = mtxmgt_subcat_lists;
while ($int2 >= 0) {
    if_sethide($boolean0, enum(int, component, $enum1, $int2));
    if_sethide($boolean0, enum(int, component, $enum3, $int2));
    $int2 = calc($int2 - 1);
}
