// 17872
[proc,script17872](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    ~error("Invalid Pass.");
    return(-1);
}
def_int $int1 = 0;
if ($dbrow0 = dbrow_7906) {
    $int1 = %bp3_viewed_pass_repeatable_level_claim_count;
} else {
    if (db_getrowtable($dbrow0) ! bp3_table) {
        ~error("Invalid Pass.");
        return(-1);
    }
    $int1 = var_reference_get(db_getfield($dbrow0, bp3_table:col13, 0));
}
return($int1);
