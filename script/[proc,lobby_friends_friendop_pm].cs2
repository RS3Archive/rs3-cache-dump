// 11473
[proc,lobby_friends_friendop_pm](string $string0)
if (userdetail_quickchat = false) {
    ~lobbyscreen_input("Send message to <$string0>", "", 0, $string0, "");
} else {
    mes("Users restricted to quick-chat cannot send messages from the Lobby.");
}
