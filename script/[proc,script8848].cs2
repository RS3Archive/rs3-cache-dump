// 8848
[proc,script8848]
if (%keyboard_manager_primary_enum = null) {
    return;
}
def_int $int0 = 0;
def_int $int1 = enum_getoutputcount(%keyboard_manager_primary_enum);
def_int $int2 = 0;
while ($int0 < $int1) {
    $int2 = enum(int, int, %keyboard_manager_primary_enum, $int0);
    if (enum_getreversecount(int, keyboard_manager_auto_disable_systems, $int2) ! 0) {
        ~script8843($int2, 0);
    }
    $int0 = calc($int0 + 1);
}
