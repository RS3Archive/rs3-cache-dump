// 10076
[proc,script10076]()(component)
switch_toplevelinterface (if_gettop) {
    case default :
        return(toplevel_v2:tooltips_param_layer);
    case lobbyscreen :
        return(lobbyscreen:tooltips_param_layer);
    case loginscreen :
        return(loginscreen:tooltips_param_layer);
}
