// 17866
[proc,script17866](dbrow $dbrow0)(boolean)
if ($dbrow0 = null) {
    ~error("Invalid pass.");
    return(false);
}
if ($dbrow0 = dbrow_7906) {
    return(%bp3_viewed_pass_purchased_prem);
}
if (db_getrowtable($dbrow0) ! bp3_table) {
    ~error("Invalid pass");
    return(false);
}
return(~int_to_bool(var_reference_get(db_getfield($dbrow0, bp3_table:col16, 0))));
