// 5200
[proc,script5200](int $int0)(int)
switch_int ($int0) {
    case 1 :
        if (%lumbcat_quest >= 60) {
            return(1);
        }
    case 2 :
        if (%motwl_main >= 60) {
            return(1);
        }
    case 3 :
        if (%vampire_quest >= 3) {
            return(1);
        }
    case 4 :
        if (~script6432($int0) >= 24) {
            return(1);
        }
    case 5 :
        if (%romcom_quest >= 100) {
            return(1);
        }
    case 6 :
        if (%phoenixgang >= 7) {
            return(1);
        }
    case 7 :
        if (%demonslayer_main >= 3) {
            return(1);
        }
    case 8 :
        if (~script6432($int0) >= 17) {
            return(1);
        }
    case 9 :
        if (%gobdip_main >= 6) {
            return(1);
        }
    case 10 :
        if (%owen1_main >= 170) {
            return(1);
        }
    case 11 :
        if (%squire >= 7) {
            return(1);
        }
    case 12 :
        if (~script6432($int0) >= 11) {
            return(1);
        }
    case 14 :
        if (%dragonquest >= 10) {
            return(1);
        }
    case 15 :
        if (%tg11_progress >= 25 | %tg14_crest = 1) {
            return(1);
        }
    case 0 :
    case default :
        ~error("Herald cape: Failed to check goal completion, invalid goal id (<tostring($int0)>)");
}
return(0);
