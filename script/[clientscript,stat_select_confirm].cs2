// 11277
[clientscript,stat_select_confirm]
if (%stat_select > 0 & %stat_select <= enum_getoutputcount(stat_autoint_to_int)) {
    resume_countdialog(tostring(%stat_select));
} else {
    mes("Select a valid skill before confirming.");
    return;
}
