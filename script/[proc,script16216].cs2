// 16216
[proc,script16216](int $int0)(boolean)
if ($int0 = 0) {
    return(false);
}
if ($int0 = -1) {
    return(true);
}
switch_client_type (clienttype) {
    case default :
        return(false);
    case enhanced_windows :
        if (testbit($int0, 0) = ^true) {
            return(true);
        }
    case enhanced_mac :
        if (testbit($int0, 1) = ^true) {
            return(true);
        }
    case enhanced_android :
        if (testbit($int0, 2) = ^true) {
            return(true);
        }
    case enhanced_ios :
        if (testbit($int0, 3) = ^true) {
            return(true);
        }
    case clienttype_9 :
        if (testbit($int0, 4) = ^true) {
            return(true);
        }
    case enhanced_linux :
        if (testbit($int0, 5) = ^true) {
            return(true);
        }
}
return(false);
