// 13520
[clientscript,slayer_switch_mode](component $component0, int $int1)
if ($int1 = 3) {
    return;
}
if ($int1 = 2) {
    ~script14062(calc(1 - %slayer_counter_display_mode));
    ~script14061(calc(1 - %slayer_counter_display_mode));
    return;
}
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 0;
$int2, $int3 = ~script8138(1031);
def_int $int5 = if_getwidth(slayer_count:slayer_window_wrapper);
def_int $int6 = struct_param(toplevel_v2_layout_default_slayer_counter, toplevel_v2_default_window_width);
def_int $int7 = struct_param(toplevel_v2_layout_default_slayer_counter, toplevel_v2_default_window_height);
def_int $int8 = 5;
def_int $int9 = calc(($int6 - 60) / max($int8, 1));
def_int $int10 = calc(($int7 - 30) / max($int8, 1));
if ($int3 = -1) {
    $int3 = 0;
}
if ($int2 = -1) {
    $int2 = $int4;
}
if ($int5 <= 60) {
    $int4 = 1;
    if_setop(1, "Shrink", $component0);
    if_setontimer("slayer_switch($int6, $int9, $int10, 1, $int2, $int4, $int3)", slayer_count:slayer_window_wrapper);
} else if ($int5 >= $int6) {
    if_setop(1, "Expand", $component0);
    if_setontimer("slayer_switch(60, $int9, $int10, -1, $int2, $int4, $int3)", slayer_count:slayer_window_wrapper);
}
