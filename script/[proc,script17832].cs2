// 17832
[proc,script17832](obj $obj0, int $int1)(int)
if (oc_param($obj0, isunique) = 1) {
    return($int1);
}
def_int $int2 = calc(~script17831 / 10);
if (~script18411(5) = true) {
    $int2 = calc($int2 + 50);
}
if (~script19657 = true & ~ironman_mode = false) {
    $int2 = calc($int2 + 20);
}
switch_obj ($obj0) {
    case lesser_necroplasm, greater_necroplasm, powerful_necroplasm :
        switch_int (~script17826(1)) {
            case 2 :
                $int2 = calc($int2 + 5);
            case 3 :
                $int2 = calc($int2 + 10);
            case 4 :
                $int2 = calc($int2 + 15);
        }
    case slayer_necromancy_mask_ensouled :
        return($int1);
}
if (~script18411(5) = true) {
    $int2 = calc($int2 + 50);
}
return(max(1, scale($int1, 100, calc(100 + $int2))));
