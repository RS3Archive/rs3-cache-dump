// 10487
[proc,script10487](int $int0, string $string0, int $int1)
cc_resetlinkplayer;
def_int $int2 = -1;
switch_int ($int0) {
    case 3, 7 :
        $int2 = friend_getslotfromname($string0);
        if ($int2 ! -1) {
            cc_setlinkfriend($int2);
        }
    case 41 :
        if (activeclanchannel_find_affined($int1) = true) {
            $int2 = activeclanchannel_getuserslot($string0);
            if ($int2 ! -1) {
                cc_setlinkactiveclanchannel($int2);
            }
        }
    case 44 :
        if (activeclanchannel_find_listened = true) {
            $int2 = activeclanchannel_getuserslot($string0);
            if ($int2 ! -1) {
                cc_setlinkactiveclanchannel($int2);
            }
        }
    case 24, 22 :
        if (player_group_find = true) {
            $int2 = ~script10276($string0);
            if ($int2 ! -1) {
                cc_setlinkplayergroup($int2, 1);
            }
        }
}
