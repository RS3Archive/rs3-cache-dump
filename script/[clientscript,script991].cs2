// 991
[clientscript,script991](int $int0)
if_sethide(false, rsraffle:claim_fireworks);
if (clientclock > $int0) {
    if_sethide(true, rsraffle:claim_fireworks);
    if_setontimer(null, rsraffle:claim_window);
}
