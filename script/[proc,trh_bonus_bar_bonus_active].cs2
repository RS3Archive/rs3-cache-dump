// 6764
[proc,trh_bonus_bar_bonus_active](struct $struct0, dbrow $dbrow1)(boolean)
switch_struct ($struct0) {
    case trh177_promo :
        return(~script16421($dbrow1));
    case trh196_promo :
        return(~script6734($dbrow1));
    case trh205_promo :
        return(~script19752($dbrow1));
    case default :
        ~error("Unexpected promo in trh_bonus_bar_bonus_active - returning false");
}
return(false);
