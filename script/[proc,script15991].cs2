// 15991
[proc,script15991](int $int0, int $int1)(int)
def_int $int2 = 1;
def_int $int3 = ~script11803;
if ((~script15992($int0, $int1) = true | ~script15993($int0) = true) & $int0 <= $int3 & $int0 >= 0) {
    return($int0);
}
while ($int2 <= $int3) {
    if (~script15993($int2) = true) {
        return($int2);
    }
    $int2 = calc($int2 + 1);
}
~error("Couldn't find a free action bar preset for initialising a new additional action bar since all action bar presets are displayed at the same time. This should be impossible!");
return(-1);
