// 17102
[proc,script17102](int $int0, int $int1, int $int2, int $int3)(string, int)
def_string $string0 = "";
if (~death_get_type = 2) {
    if (testbit(%death_item_protection_bitmask, $int0) = ^true) {
        if ($int1 < 5) {
            $int1 = calc($int1 + 1);
            return("<col=<tostring($int2, 16)>> [ACTIVE]</col>", $int1);
        } else {
            return("<col=<tostring($int3, 16)>> [BACKUP]</col>", $int1);
        }
    } else if ($int0 = 1 & %deathkeep_info_dangermode = 1) {
        return("<col=<tostring($int2, 16)>> [PREVIEW ACTIVE]</col>", $int1);
    }
    return("", $int1);
}
def_int $int4 = 0;
switch_int ($int0) {
    case 1, 2, 3 :
        $int4 = ~equal_int(%death_last_base_type, $int0);
    case 4 :
        if (~hardcore_mode = true | %death_last_base_type = $int0) {
            $int4 = 1;
        }
    case 10 :
        if (%death_last_protect_prayer = calc(0 + 1)) {
            $int4 = 1;
        }
    case 11 :
        if (%death_last_protect_prayer = calc(1 + 1)) {
            $int4 = 1;
        }
    case 12 :
        $int4 = %death_last_protect_scarab_powder;
    case 13 :
        $int4 = %death_last_protect_invent_hoarding;
    case 17 :
        $int4 = %death_last_protect_fury_shark;
    case 14 :
        if (%death_last_protect_div > 0 & inv_getobj(worn, 17) = div_sign_item_protect) {
            $int4 = 1;
        }
    case 15 :
        if (%death_last_protect_div > 0 & (%death_last_protect_div > 1 | inv_getobj(worn, 17) ! div_sign_item_protect)) {
            $int4 = 1;
        }
    case 16 :
        if (%death_last_protect_div > 0 & %death_last_protect_div = 2 & inv_getobj(worn, 17) ! div_sign_item_protect) {
            $int4 = 1;
        }
    case default :
        return("", $int1);
}
if ($int4 = 1) {
    $int1 = calc($int1 + 1);
    return("<col=<tostring($int2, 16)>> [ACTIVE]</col>", $int1);
}
return("", $int1);
