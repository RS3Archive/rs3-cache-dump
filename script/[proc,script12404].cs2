// 12404
[proc,script12404](int $int0)(int)
def_int $int1 = 400;
def_int $int2 = 100;
switch_int ($int0) {
    case 1 :
        if (%gwd2_rep_unlocked_seren_kc = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 2 :
        if (%gwd2_rep_unlocked_sliske_kc = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 3 :
        if (%gwd2_rep_unlocked_zamorak_kc = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 4 :
        if (%gwd2_rep_unlocked_zaros_kc = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
}
if (%combat_mastery_passive_tier_unlocked >= 2) {
    $int2 = max(0, calc($int2 - 50));
}
$int1 = scale($int1, 100, $int2);
return($int1);
