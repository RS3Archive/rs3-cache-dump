// 20624
[proc,script20624](int $int0)
if ($int0 > ~script20471) {
    ~error("Unknown tab ID when trying to trigger fakeop.");
}
if_triggerop(marketplace_currency_banner:marketplace_store_navigation_fakeop_layer, $int0, 1);
