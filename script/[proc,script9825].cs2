// 9825
[proc,script9825](struct $struct0, dbrow $dbrow1)(dbrow)
def_int $int2 = 0;
switch_struct ($struct0) {
    case trh155_promo :
        if (%trh155_promo_bonus_remaining > 0) {
            return(trh_promo_categories_trh155_bonus);
        }
    case trh175_promo :
        if (~date_runeday < 8644) {
            if (map_members = true) {
                return(trh_promo_categories_trh175);
            } else {
                return(trh_promo_categories_trh175_f2p);
            }
        } else if (map_members = true) {
            return(trh_promo_categories_trh175_1);
        } else {
            return(trh_promo_categories_trh175_f2p_1);
        }
    case trh176_promo :
        $int2 = ~script18405;
        if ($int2 < 8692) {
            return(trh_promo_categories_trh176);
        } else if ($int2 < 8694) {
            return(trh_promo_categories_trh176_v2);
        } else if ($int2 < 8697) {
            return(trh_promo_categories_trh176_v3);
        } else if ($int2 < 8702) {
            return(trh_promo_categories_trh176_v4);
        } else {
            return(trh_promo_categories_trh176_v5);
        }
}
return($dbrow1);
