// 6062
[proc,script6062](int $int0)(int)
db_find(dino_base_camp_building:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 ! null) {
    return(db_getfieldcount($dbrow1, dino_base_camp_building:col5));
}
return(0);
