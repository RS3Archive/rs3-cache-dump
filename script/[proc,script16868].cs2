// 16868
[proc,script16868](int $int0)(string)
def_int $int1 = calc($int0 - ~date_runeday);
if (~script16825 ! 1 | $int1 < 0) {
    return("00:00:00");
}
def_string $string0 = "";
def_int $int2 = 0;
if ($int1 > 7 | $int1 = 7 & ~script16847(~date_runeday) = true) {
    if (calc($int1 / 7) > 0) {
        $string0 = append($string0, ~text_plural(calc($int1 / 7), "1 week, ", "<tostring(calc($int1 / 7))> weeks, "));
    }
    $string0 = append($string0, ~script985(calc($int1 % 7), "0 days", "1 day", "<tostring(calc($int1 % 7))> days"));
    return($string0);
} else {
    $int2 = ~script16848($int0);
    return(~script15908(calc($int2 / 1440), -1, -1, calc(($int2 - $int2 / 1440 * 1440) / 60), calc($int2 % 60), -1, -1, 5, -1, 0, 1, 1, 0, 0, 1));
}
