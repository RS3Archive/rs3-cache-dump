// 12398
[proc,script12398](dbrow $dbrow0)(string)
def_int $int1 = ~script12399($dbrow0);
def_int $int2 = ~script12401($dbrow0);
def_int $int3 = 0;
def_string $string0 = "";
def_string $string1 = "";
while (++$int3 <= $int2) {
    switch_int ($int3) {
        case 1 :
            $string1 = db_getfield(db_getfield($dbrow0, fairyrings:col5, 0), fairyrings:col2, 0);
        case 2 :
            $string1 = db_getfield(db_getfield($dbrow0, fairyrings:col6, 0), fairyrings:col2, 0);
        case 3 :
            $string1 = db_getfield(db_getfield($dbrow0, fairyrings:col7, 0), fairyrings:col2, 0);
        case 4 :
            $string1 = db_getfield(db_getfield($dbrow0, fairyrings:col8, 0), fairyrings:col2, 0);
    }
    if ($int3 < $int1) {
        if (string_length($string0) = 0) {
            $string0 = append($string0, $string1);
        } else {
            $string0 = append($string0, "    <$string1>");
        }
    } else if ($int3 = $int1) {
        if (string_length($string0) = 0) {
            $string0 = append($string0, "<col=ffffff><$string1></col>");
        } else {
            $string0 = append($string0, "    <col=ffffff><$string1></col>");
        }
    } else if (string_length($string0) = 0) {
        $string0 = append($string0, "? ? ?");
    } else {
        $string0 = append($string0, "    ? ? ?");
    }
}
return($string0);
