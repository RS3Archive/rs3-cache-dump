// 2853
[proc,script2853](int $int0)
def_string $string0 = enum(int, string, pop_map_zone_names, $int0);
def_string $string1 = if_gettext(pop_map:map_zone_name);
if (compare($string0, $string1) = 0) {
    if_settext("Region Information", pop_map:map_zone_name);
    if_settext("", pop_map:map_zone_details);
}
