// 10325
[clientscript,script10325](component $component0, string $string0, int $int1)
def_string $string1 = removetags($string0);
switch_int ($int1) {
    case 1 :
        ~script8512(%chatbox_focus, $string0);
    case 4 :
    case 5 :
    case 6 :
    case 7 :
    case 8 :
    case 9 :
        if (friend_test($string0) = true) {
            ~group_mes("Attempting to remove <$string0> from your Friends List");
            %friendtodelete = $string1;
            ~script2018(8);
        } else if (ignore_test($string0) = true) {
            ~group_mes("Attempting to remove <$string0> from your Ignore List.");
            ignore_del($string1);
        } else {
            ~group_mes("Attempting to add <$string0> to your Friends List.");
            friend_add($string1);
        }
    case 10 :
        ~group_mes("Attempting to add <$string0> to your Ignore List.");
        ignore_add($string1);
}
