// 11412
[proc,script11412]
if (%loot_inventory_open = 1) {
    if (%loot_previous_active_window = 2) {
        return;
    } else if (~script6431 = true) {
        ~script8311(%loot_previous_active_window);
    } else {
        ~script8313(%loot_previous_active_window);
    }
} else {
    ~script8323(2, 0);
}
