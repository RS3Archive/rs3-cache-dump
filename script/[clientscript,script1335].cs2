// 1335
[clientscript,script1335](int $int0, component $component1, component $component2)
if (activeclanchannel_find_affined(0) = false) {
    mes_typed(^chattype_clanmessage, 0, "You need to be in a clan to do that.");
    return;
}
if (~is_mobile_client = true) {
    %lobby_caret_clan = string_length(%lobbyscreen_input_clan);
} else {
    %lobby_caret_clan = ~script1401($int0, %lobbyscreen_input_clan, museo_sans_11pt_regular, if_getx($component1));
}
~script1566(0, %lobbyscreen_input_clan, 80);
~script1390($component1, $component2, %lobbyscreen_input_clan);
