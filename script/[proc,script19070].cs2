// 19070
[proc,script19070](enum $enum0)(dbrow)
def_int $int1 = enum_getoutputcount($enum0);
def_dbrow $dbrow2 = null;
def_int $int3 = 0;
while (++$int3 < calc($int1 + 1)) {
    $dbrow2 = enum(int, dbrow, $enum0, $int3);
    if (~script19068($dbrow2) = false) {
        return($dbrow2);
    }
}
~error("No blueprints available");
return(null);
