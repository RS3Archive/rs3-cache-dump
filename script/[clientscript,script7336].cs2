// 7336
[clientscript,script7336](int $int0)
def_string $string0 = "";
def_enum $enum1 = null;
switch_int ($int0) {
    case 0 :
        $enum1 = pop_ports_upgrade_bar;
        if_settext("Upgrade Bar", pop_port_upgrade:title);
    case 1 :
        $enum1 = pop_ports_upgrade_office;
        if_settext("Upgrade Office", pop_port_upgrade:title);
    case 2 :
        $enum1 = pop_ports_upgrade_workshop;
        if_settext("Upgrade Workshop", pop_port_upgrade:title);
    case 5 :
        $enum1 = pop_ports_upgrade_warehouse;
        if_settext("Upgrade Warehouse", pop_port_upgrade:title);
    case 4 :
        $enum1 = pop_ports_upgrade_shipwright;
        if_settext("Upgrade Shipwright", pop_port_upgrade:title);
    case 3 :
        $enum1 = pop_ports_upgrade_lodgings;
        if_settext("Upgrade Lodgings", pop_port_upgrade:title);
    case 10 :
        $enum1 = pop_ports_upgrade_icons;
        if_settext("Upgrade Icon Hotspot 1", pop_port_upgrade:title);
    case 11 :
        $enum1 = pop_ports_upgrade_icons;
        if_settext("Upgrade Icon Hotspot 2", pop_port_upgrade:title);
    case 12 :
        $enum1 = pop_ports_upgrade_icons;
        if_settext("Upgrade Icon Hotspot 3", pop_port_upgrade:title);
    case 6 :
        $enum1 = pop_ports_upgrade_totems;
        if_settext("Upgrade Totem Hotspot 1", pop_port_upgrade:title);
    case 7 :
        $enum1 = pop_ports_upgrade_totems;
        if_settext("Upgrade Totem Hotspot 2", pop_port_upgrade:title);
    case 8 :
        $enum1 = pop_ports_upgrade_totems;
        if_settext("Upgrade Totem Hotspot 3", pop_port_upgrade:title);
    case 9 :
        $enum1 = pop_ports_upgrade_totems;
        if_settext("Upgrade Totem Hotspot 4", pop_port_upgrade:title);
    case 13 :
        $enum1 = pop_ports_upgrade_portal;
        if_settext("Upgrade Portal", pop_port_upgrade:title);
}
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 0;
$int4 = calc($int4 + ~pop_update_button(0, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(1, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(2, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(3, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(4, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(5, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(6, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(7, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(8, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(9, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(10, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(11, $int0, $enum1, $int4));
$int4 = calc($int4 + ~pop_update_button(12, $int0, $enum1, $int4));
if_setscrollsize(0, calc($int4 * 90), pop_port_upgrade:content_layer);
if (calc($int4 * 90) > 180) {
    if_sethide(false, pop_port_upgrade:scrollbar_layer);
} else {
    if_sethide(true, pop_port_upgrade:scrollbar_layer);
}
~rs3_scrollbar_vertical(pop_port_upgrade:scrollbar_layer, pop_port_upgrade:content_layer);
