// 480
[proc,script480](stat $stat0)(int)
if (%trh30_special_credit_stat ! 0) {
    if ($stat0 = enum(int, stat, int_to_stat, %trh30_special_credit_stat)) {
        return(300);
    }
    return(200);
}
return(200);
