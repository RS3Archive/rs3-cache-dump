// 11358
[clientscript,script11358](struct $struct0, int $int1)
def_int $int2 = struct_param($struct0, mtx_pet_ability_cooldown);
if ($int1 > 0) {
    $int2 = $int1;
}
switch_struct ($struct0) {
    case mtx_pet_ability_banking :
        %mtx_pet_bank_cooldown_end = calc($int2 * 30 + clientclock);
    case mtx_pet_ability_repair :
        %mtx_pet_repair_cooldown_end = calc($int2 * 30 + clientclock);
    case mtx_pet_ability_alchemy :
        %mtx_pet_alch_cooldown_end = calc($int2 * 30 + clientclock);
    case mtx_pet_ability_fertiliser :
        %mtx_pet_fertiliser_cooldown_end = calc($int2 * 30 + clientclock);
    case mtx_pet_ability_lifesaver :
        %mtx_pet_lifesaver_cooldown_end = calc($int2 * 30 + clientclock);
    case mtx_pet_ability_bob :
        %mtx_pet_bob_cooldown_end = calc($int2 * 30 + clientclock);
}
