// 14907
[clientscript,arch_material_storage_onload]
def_int $int0 = if_getwidth(arch_material_storage:mainmodal_window);
def_int $int1 = if_getwidth(arch_material_storage:storage_window);
def_int $int2 = if_getwidth(arch_material_storage:inventory_window);
if (~script6431 = true) {
    $int0, $int2 = 492, 232;
}
if (if_find(arch_material_storage:mainmodal_window) = true) {
    cc_setsize($int0, cc_getheight, ^setsize_abs, ^setsize_abs);
}
if (if_find(arch_material_storage:storage_window) = true) {
    cc_setsize($int1, cc_getheight, ^setsize_abs, ^setsize_abs);
}
if (if_find(arch_material_storage:inventory_window) = true) {
    cc_setsize($int2, cc_getheight, ^setsize_abs, ^setsize_abs);
}
~arch_material_storage_build_storage;
~arch_material_storage_build_inventory;
