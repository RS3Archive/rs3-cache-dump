// 15118
[proc,script15118](int $int0)(dbrow)
if ($int0 <= 0) {
    return(null);
}
db_find(event_crafting_core_event:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Unable to find an event at ID <tostring($int0)>!");
    return(null);
}
return($dbrow1);
