// 13751
[clientscript,script13751](component $component0, int $int1, int $int2)
def_int $int3 = if_gettrans($component0);
switch_int ($int2) {
    case 0 :
        if (clientclock >= calc($int1 + 70)) {
            if ($int3 > 0) {
                if_settrans(~clamp(calc($int3 - 30), 0, 255), $component0);
            } else {
                $int2 = 1;
            }
        }
    case 1 :
        if (clientclock >= calc($int1 + 100)) {
            if ($int3 < 255) {
                if_settrans(~clamp(calc($int3 + 30), 0, 255), $component0);
            } else {
                $int2 = 2;
            }
        }
    case 2 :
        if (clientclock >= calc($int1 + 150)) {
            if ($int3 > 0) {
                if_settrans(~clamp(calc($int3 - 30), 0, 255), $component0);
            } else {
                $int2 = 3;
            }
        }
    case 3 :
        if (clientclock >= calc($int1 + 165)) {
            if ($int3 < 255) {
                if_settrans(~clamp(calc($int3 + 30), 0, 255), $component0);
            } else {
                $int2 = 4;
            }
        }
    case 4 :
        if (clientclock >= calc($int1 + 180)) {
            if ($int3 > 0) {
                if_settrans(~clamp(calc($int3 - 30), 0, 255), $component0);
            } else {
                $int2 = 5;
            }
        }
    case 5 :
        if (clientclock >= calc($int1 + 195)) {
            if ($int3 < 255) {
                if_settrans(~clamp(calc($int3 + 30), 0, 255), $component0);
            } else {
                $int2 = 6;
            }
        }
    case 6 :
        if (clientclock >= calc($int1 + 245)) {
            if ($int3 > 0) {
                if_settrans(~clamp(calc($int3 - 30), 0, 255), $component0);
            } else {
                $int2 = 7;
                if_setontimer(null, $component0);
                return;
            }
        }
}
if_setontimer("script13751($component0, $int1, $int2)", $component0);
