// 3610
[proc,script3610](dbrow $dbrow0, int $int1)(int)
switch_dbrow ($dbrow0) {
    case default :
        switch_int ($int1) {
            case 0 :
                if (db_getfieldcount($dbrow0, runepass_task:col2) > 0) {
                    return(stat_base_actual(db_getfield($dbrow0, runepass_task:col2, 0)));
                }
            case 1 :
                if (db_getfieldcount($dbrow0, runepass_task:col3) > 0) {
                    return(stat_base_actual(db_getfield($dbrow0, runepass_task:col3, 0)));
                }
        }
    case null :
}
return(1);
