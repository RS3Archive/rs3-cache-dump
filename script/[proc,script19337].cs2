// 19337
[proc,script19337](dbrow $dbrow0)(boolean)
def_int $int1 = 0;
def_int $int2 = db_getfieldcount($dbrow0, group_ironman_journey_rewards:col4);
def_struct $struct3 = null;
while ($int1 < $int2) {
    $struct3 = ~script19338($dbrow0, $int1, %group_ironman_cache_ruleset);
    if ($struct3 ! null & ~script10881($struct3) = 0) {
        return(false);
    }
    $int1 = calc($int1 + 1);
}
$int1 = 0;
$int2 = db_getfieldcount($dbrow0, group_ironman_journey_rewards:col3);
def_dbrow $dbrow4 = null;
def_int $int5 = -1;
while ($int1 < $int2) {
    $dbrow4 = ~script19339($dbrow0, $int1, %group_ironman_cache_ruleset);
    if ($dbrow4 ! null & ~script19384($dbrow4) = 0) {
        return(false);
    }
    $int1 = calc($int1 + 1);
}
return(true);
