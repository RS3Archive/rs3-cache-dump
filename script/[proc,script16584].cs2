// 16584
[proc,script16584](int $int0, dbrow $dbrow1)(boolean)
def_int $int2 = -1;
if ($dbrow1 = null) {
    return(true);
} else {
    while (++$int2 < db_getfieldcount($dbrow1, trh_whitelisted_categories:col0)) {
        if (db_getfield($dbrow1, trh_whitelisted_categories:col0, $int2) = $int0) {
            return(true);
        }
    }
    return(false);
}
