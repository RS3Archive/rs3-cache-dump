// 5008
[proc,script5008]()(int)
def_int $int0 = -1;
def_int $int1 = -1;
def_int $int2 = 0;
if (activeclansettings_find_affined(0) = true) {
    $int0 = activeclansettings_getaffinedslot(chat_playername_unfiltered);
    if ($int0 < 0) {
        return(0);
    }
    $int1 = activeclansettings_getaffinedrank($int0);
    $int2 = calc(%clan_core_next_update_minute - date_minutes);
    if ($int2 < 360) {
        if ($int1 < 126) {
            return(0);
        } else {
            return(1);
        }
    } else {
        return(1);
    }
}
return(0);
