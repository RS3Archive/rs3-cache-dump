// 15332
[proc,cheevo_reset_dropdown]
if (if_find(achievements_sub:build_layer) = true) {
    cc_setparam(cheevo_current_drop_down_id, -1);
}
cc_deleteall(achievements_sub:cheevo_details);
if_sethide(true, achievements_sub:cheevo_details);
