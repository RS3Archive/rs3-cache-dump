// 16031
[proc,script16031](int $int0)(int)
switch_int ($int0) {
    case 0 :
        if (%rsraffle_entry_claimed_losing_cat_1 < %rsraffle_entry_total_cat_1_losing) {
            return(0);
        } else {
            return(1);
        }
    case 1 :
        if (%rsraffle_entry_claimed_losing_cat_2 < %rsraffle_entry_total_cat_2_losing) {
            return(0);
        } else {
            return(1);
        }
    case 2 :
        if (%rsraffle_entry_claimed_losing_cat_3 < %rsraffle_entry_total_cat_3_losing) {
            return(0);
        } else {
            return(1);
        }
    case 3 :
        if (%rsraffle_entry_claimed_losing_cat_4 < %rsraffle_entry_total_cat_4_losing) {
            return(0);
        } else {
            return(1);
        }
    case 4 :
        if (%rsraffle_entry_claimed_losing_cat_5 < %rsraffle_entry_total_cat_5_losing) {
            return(0);
        } else {
            return(1);
        }
    case 5 :
        if (%rsraffle_entry_claimed_losing_cat_6 < %rsraffle_entry_total_cat_6_losing) {
            return(0);
        } else {
            return(1);
        }
    case 6 :
        if (%rsraffle_entry_claimed_losing_cat_7 < %rsraffle_entry_total_cat_7_losing) {
            return(0);
        } else {
            return(1);
        }
    case 7 :
        if (%rsraffle_entry_claimed_losing_cat_8 < %rsraffle_entry_total_cat_8_losing) {
            return(0);
        } else {
            return(1);
        }
    case 8 :
        if (%rsraffle_entry_claimed_losing_cat_9 < %rsraffle_entry_total_cat_9_losing) {
            return(0);
        } else {
            return(1);
        }
    case 9 :
        if (%rsraffle_entry_claimed_losing_cat_10 < %rsraffle_entry_total_cat_10_losing) {
            return(0);
        } else {
            return(1);
        }
}
