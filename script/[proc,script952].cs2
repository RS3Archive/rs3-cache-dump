// 952
[proc,script952](struct $struct0, stat $stat1)(int)
if ($struct0 = null) {
    return(0);
}
switch_stat ($stat1) {
    case agility :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case summoning :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case attack :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case hitpoints :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case construction :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case cooking :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case crafting :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case defence :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case divination :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case dungeoneering :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case farming :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case firemaking :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case fishing :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case fletching :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case herblore :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case hunter :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case invention :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case magic :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case mining :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case necromancy :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case prayer :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case ranged :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case runecraft :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case slayer :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case smithing :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case strength :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case thieving :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
    case woodcutting :
        return(~clamp(calc(stat_visible_xp_actual($stat1) * 10 / 1500000), 3, 17));
}
return(0);
