// 20244
[clientscript,script20244](component $component0)
def_dbrow $dbrow1 = ~script20117(%league_active_player);
if (~script20132($dbrow1) = false) {
    return;
}
if (%league_relic_cache_next_tier_points = 0) {
    if_setontimer("script20244($component0)", $component0);
    return;
} else {
    if_setontimer(null, $component0);
}
def_int $int2 = 12202555;
def_int $int3 = 16464712;
def_enum $enum4 = db_getfield($dbrow1, league:col18, 0);
def_int $int5 = 0;
def_int $int6 = %league_task_completed_total;
cc_deleteall($component0);
def_var_int $var_int7 = db_getfield($dbrow1, league:col22, 0);
def_int $int8 = calc(%league_relic_cache_next_tier_points - %league_relic_cache_current_tier_points);
def_int $int9 = calc(var_reference_get($var_int7) - %league_relic_cache_current_tier_points);
def_int $int10 = ~script20245;
def_string $string0 = "<tostring_localised($int9, true)> / <tostring_localised($int8, true)> gained before the next relic unlock.";
if ($int10 ! -1) {
    $string0 = "A relic is ready to unlock!";
    ~script20246($component0, 0, 0, $int9, $int9, rs3_icon_leagues_points_unlock, $string0, $int2, $int3);
} else {
    if ($int9 < 0) {
        $string0 = "";
    }
    ~script20246($component0, 0, 0, $int8, $int9, rs3_icon_leagues_points_progress, $string0, $int2, $int3);
}
if (~script20129 = true) {
    if (%league_locality_unlock_count = 0) {
        $int5 = enum(int, int, $enum4, %league_locality_unlock_count);
    } else if (%league_locality_unlock_count < enum_getoutputcount($enum4)) {
        $int6 = enum(int, int, $enum4, calc(%league_locality_unlock_count - 1));
        $int5 = calc(enum(int, int, $enum4, %league_locality_unlock_count) - $int6);
        $int6 = calc(%league_task_completed_total - $int6);
    } else {
        $int5 = db_getfield($dbrow1, league:col7, 0);
    }
    ~script20246($component0, 1, 28, $int5, $int6, "aif_toplevel_worldmap_btn,0", "", $int2, $int3);
}
