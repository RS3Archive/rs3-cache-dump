// 8341
[proc,script8341](component $component0, int $int1)(int)
def_int $int2 = 0;
if (if_find($component0) = true) {
    if ($int1 = 1) {
        $int2 = cc_param(toplevel_v2_has_scrollbar_horizontal);
    } else if ($int1 = 2) {
        $int2 = cc_param(toplevel_v2_has_scrollbar_vertical);
    }
}
return($int2);
