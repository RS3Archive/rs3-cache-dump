// 15301
[proc,script15301](int $int0)(npc)
def_int $int1 = ~script15302($int0);
def_int $int2 = ~script15303($int0);
def_npc $npc3 = enum(int, npc, slayer_pod_npc_list, $int1);
def_enum $enum4 = null;
if ($int2 > 0 & enum(npc, boolean, slayer_pod_mob_uses_sub_id, $npc3) = true) {
    $enum4 = enum(npc, enum, slayer_pod_npc_to_mob_enum, $npc3);
    if ($enum4 ! null) {
        $npc3 = enum(int, npc, $enum4, calc($int2 - 1));
    }
}
return($npc3);
