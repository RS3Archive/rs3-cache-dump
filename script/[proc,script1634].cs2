// 1634
[proc,script1634](enum $enum0, int $int1)(int)
if ($enum0 = null) {
    return(-1);
}
def_int $int2 = 0;
def_int $int3 = enum_getoutputcount($enum0);
while ($int2 < $int3) {
    if (enum(int, int, $enum0, $int2) = $int1) {
        return(~group_get_check_var(calc($int2 + 1)));
    }
    $int2 = calc($int2 + 1);
}
return(-1);
