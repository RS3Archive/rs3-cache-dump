// 18500
[clientscript,script18500](component $component0)
def_struct $struct1 = ~script18493;
def_dbrow $dbrow2 = struct_param($struct1, trh_banner_reward_track);
def_string $string0 = "";
switch_dbtable (db_getrowtable($dbrow2)) {
    case trh_banner_reward_track :
        if (db_getfieldcount($dbrow2, trh_banner_reward_track:col0) > 0) {
            $string0 = db_getfield($dbrow2, trh_banner_reward_track:col0, 0);
        }
    case trh_banner_reward_track_list :
        if (db_getfieldcount($dbrow2, trh_banner_reward_track_list:col0) > 0) {
            $string0 = db_getfield($dbrow2, trh_banner_reward_track_list:col0, 0);
        }
}
if_settext(db_getfield($dbrow2, trh_banner_reward_track_list:col0, 0), $component0);
