// 12474
[proc,script12474](int $int0)(string)
db_find(mm16_task:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    return("false");
}
def_int $int2 = db_getfieldcount($dbrow1, mm16_task:col11);
if ($int2 = 0) {
    return("true");
}
def_int $int3 = 0;
def_string $string0 = "You need to have completed the following cases:";
def_dbrow $dbrow4 = null;
while ($int3 < $int2) {
    $int0 = db_getfield($dbrow1, mm16_task:col11, $int3);
    db_find(mm16_task:col0, $int0);
    $dbrow4 = db_findnext;
    if ($dbrow4 ! null) {
        $string0 = "<$string0><br>- <db_getfield($dbrow4, mm16_task:col2, 0)>";
    }
    $int3 = calc($int3 + 1);
}
return($string0);
