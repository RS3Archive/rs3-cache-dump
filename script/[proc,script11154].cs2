// 11154
[proc,script11154](int $int0, int $int1)(int)
def_int $int2 = 0;
if (stat_base(fletching) < 10) {
    $int2 = 180;
} else if (stat_base(fletching) < 20) {
    $int2 = 370;
} else if (stat_base(fletching) < 25) {
    $int2 = 610;
} else if (stat_base(fletching) < 35) {
    $int2 = 920;
} else if (stat_base(fletching) < 40) {
    $int2 = 1110;
} else if (stat_base(fletching) < 50) {
    $int2 = 1380;
} else if (stat_base(fletching) < 55) {
    $int2 = 1670;
} else if (stat_base(fletching) < 65) {
    $int2 = 1940;
} else if (stat_base(fletching) < 70) {
    $int2 = 2250;
} else if (stat_base(fletching) < 80) {
    $int2 = 2500;
} else if (stat_base(fletching) < 85) {
    $int2 = 2780;
} else if (stat_base(fletching) < 90) {
    $int2 = 3050;
} else if (stat_base(fletching) < 95) {
    $int2 = 3370;
} else {
    $int2 = 3880;
}
if ($int0 = 1) {
    $int2 = calc($int2 / 2);
}
if ($int1 = 1) {
    $int2 = calc($int2 + scale($int2, 100, 10));
}
return($int2);
