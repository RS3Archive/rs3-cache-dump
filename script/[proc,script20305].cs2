// 20305
[proc,script20305](dbrow $dbrow0)(string)
def_dbrow $dbrow1 = ~script20119(%league_active_player);
if ($dbrow1 = null) {
    return("");
}
def_enum $enum2 = db_getfield($dbrow1, league_task_settings:col0:2, 0);
return(tostring(enum(int, int, $enum2, db_getfield($dbrow0, league_task:col6, 0))));
