// 13927
[proc,script13927](int $int0)
if (%slide_puzzle_struct = null) {
    return;
}
def_enum $enum1 = struct_param(%slide_puzzle_struct, slidepuzzle_sprite_single_enum);
if ($enum1 = null) {
    $enum1 = enum(int, enum, struct_param(%slide_puzzle_struct, slidepuzzle_sprite_anim_enum), $int0);
}
def_int $int2 = 0;
def_graphic $graphic3 = null;
def_int $int4 = struct_param(%slide_puzzle_struct, slidepuzzle_empty_slot);
while ($int2 < 25) {
    $graphic3 = enum(int, graphic, $enum1, ~script13932($int2));
    if (cc_find(slide_puzzle:puzzle_layer, $int2) = true) {
        cc_setgraphic($graphic3);
        if ($graphic3 = null) {
            cc_clearops;
        } else {
            cc_setop(1, "Move");
        }
    }
    $graphic3 = enum(int, graphic, $enum1, $int2);
    if (cc_find(slide_puzzle:puzzle_hint, $int2) = true) {
        cc_setgraphic($graphic3);
    }
    $int2 = calc($int2 + 1);
}
