// 5953
[proc,script5953](int $int0, struct $struct1)(int)
if ($int0 = -1 | $struct1 = null) {
    return(-1);
}
def_enum $enum2 = enum(int, enum, slayer_master_enum, %slayer_master);
if ($enum2 = null) {
    ~error("Null $slayer_master_table for %slayer_master <tostring(%slayer_master)> in enum slayer_master_enum");
    return(-1);
}
if (enum_hasoutput(struct, $enum2, $struct1) = false) {
    ~error("Slayer struct for <struct_param($struct1, slayer_name)> is not on enum for %slayer_master <tostring(%slayer_master)>");
    return(-1);
}
return(enum_getreverseindex(struct, int, $enum2, $struct1, 0));
