// 5747
[proc,script5747](dbrow $dbrow0)(int)
def_int $int1 = 0;
def_dbrow $dbrowarray0(30);
def_int $int2 = 0;
$dbrowarray0($int2) = $dbrow0;
def_int $int3 = 0;
while ($int2 > -1) {
    $dbrow0 = $dbrowarray0($int2);
    if (~script5759($dbrow0) = true) {
        $int1 = calc($int1 + ~script17032($dbrow0));
        $int3 = calc(db_getfieldcount($dbrow0, intern_notes_dbrow:col0) - 1);
        while ($int3 > -1) {
            $dbrowarray0($int2++) = db_getfield($dbrow0, intern_notes_dbrow:col0, $int3--);
        }
    }
    $int2 = calc($int2 - 1);
}
return($int1);
