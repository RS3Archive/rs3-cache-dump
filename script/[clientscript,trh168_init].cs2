// 14460
[clientscript,trh168_init]
if (playermember = true) {
    ~script14466(trh168_yellow_prizes, trh168_yellow_amounts, trh168:yellow_prize_content);
} else {
    ~script14466(trh168_yellow_prizes_f2p, trh168_yellow_amounts, trh168:yellow_prize_content);
}
~trh168_show_tab(2);
cc_deleteall(wheel_of_fortune:fake_op);
def_int $int0 = -1;
def_int $int1 = ~script19062;
while (++$int0 < $int1) {
    cc_create(wheel_of_fortune:fake_op, ^iftype_graphic, $int0);
}
$int0 = 0;
while (++$int0 <= 12) {
    ~script14465($int0);
}
if_setopcursor(1, wof_trh_key_cursor, trh168:claim_chest);
if_settext("Fill the gems on the right by selecting prizes from the categories on the left. You are guaranteed to win one of these prizes!", trh168:tutorial_text);
