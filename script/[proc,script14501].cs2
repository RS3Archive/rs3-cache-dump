// 14501
[proc,script14501](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    return(0);
}
def_int $int1 = db_getfieldcount($dbrow0, collection:col13);
def_obj $obj2 = null;
def_int $int3 = 1;
def_int $int4 = -1;
switch_dbrow ($dbrow0) {
    case collection_boss_nex2 :
        $int3 = 0;
        while (++$int4 < $int1) {
            $obj2 = db_getfield($dbrow0, collection:col13, $int4);
            switch_obj ($obj2) {
                case nex2_robes_unlock_chest_umbra, nex2_robes_unlock_chest_cruor, nex2_robes_unlock_chest_glacies, nex2_robes_unlock_chest_fumus :
                    if (~script14500($obj2) > 0) {
                        $int3 = 1;
                    }
                case default :
                    if (~script14500($obj2) = 0) {
                        return(0);
                    }
            }
        }
    case collection_cluescroll_hard :
        $int3 = 0;
        while (++$int4 < $int1) {
            $obj2 = db_getfield($dbrow0, collection:col13, $int4);
            switch_obj ($obj2) {
                case tt14_barrel_cape, tt14_backstab_cape, tt14_effigy_cape :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 0);
                    }
                case trail_ranger_coif, trail_ranger_torso, trail_ranger_legs, trail_ranger_vambraces, trail_mage_torso, trail_mage_legs, trail_mage_hat, trail_mage_amulet, trail_silver_plate_legs, trail_silver_plate_chest, trail_fighter_helm, trail_fighter_shield :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 1);
                    }
                case trail14_dye_shadow, trail14_dye_barrows :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 2);
                    }
                case default :
                    if (~script14500($obj2) = 0) {
                        return(0);
                    }
            }
        }
        if (bitcount($int3) = 3) {
            $int3 = 1;
        } else {
            $int3 = 0;
        }
    case collection_cluescroll_elite :
        $int3 = 0;
        while (++$int4 < $int1) {
            $obj2 = db_getfield($dbrow0, collection:col13, $int4);
            switch_obj ($obj2) {
                case tt14_backstab_cape, tt14_effigy_cape :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 0);
                    }
                case trail_prayer_staff, trail_prayer_cloak, trail_prayer_head, trail_prayer_robe_t, trail_prayer_robe_l :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 1);
                    }
                case trail14_dye_shadow, trail14_dye_barrows, trail17_dye_ice, trail14_dye_thirdage, blood_dye :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 2);
                    }
                case default :
                    if (~script14500($obj2) = 0) {
                        return(0);
                    }
            }
        }
        if (bitcount($int3) = 3) {
            $int3 = 1;
        } else {
            $int3 = 0;
        }
    case collection_cluescroll_master :
        $int3 = 0;
        while (++$int4 < $int1) {
            $obj2 = db_getfield($dbrow0, collection:col13, $int4);
            switch_obj ($obj2) {
                case tt14_backstab_cape, tt14_effigy_cape :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 0);
                    }
                case trail17_secondage_melee_helm, trail17_secondage_melee_body, trail17_secondage_melee_legs, trail17_secondage_melee_weapon, trail17_secondage_magic_helm, trail17_secondage_magic_body, trail17_secondage_magic_legs, trail17_secondage_magic_weapon, trail17_secondage_range_helm, trail17_secondage_range_body, trail17_secondage_range_legs, trail17_secondage_range_weapon :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 1);
                    }
                case trail17_explorer_hat :
                case default :
                    if (~script14500($obj2) = 0) {
                        return(0);
                    }
            }
        }
        if (bitcount($int3) = 2) {
            $int3 = 1;
        } else {
            $int3 = 0;
        }
    case collection_cluescroll_master_complete :
        $int3 = 0;
        while (++$int4 < $int1) {
            $obj2 = db_getfield($dbrow0, collection:col13, $int4);
            switch_obj ($obj2) {
                case trail17_secondage_melee_helm, trail17_secondage_melee_body, trail17_secondage_melee_legs, trail17_secondage_melee_weapon :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 0);
                    }
                case trail17_secondage_magic_helm, trail17_secondage_magic_body, trail17_secondage_magic_legs, trail17_secondage_magic_weapon :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 1);
                    }
                case trail17_secondage_range_helm, trail17_secondage_range_body, trail17_secondage_range_legs, trail17_secondage_range_weapon :
                    if (~script14500($obj2) > 0) {
                        $int3 = setbit($int3, 2);
                    }
                case default :
                    if (~script14500($obj2) = 0) {
                        return(0);
                    }
            }
        }
        if (bitcount($int3) = 3) {
            $int3 = 1;
        } else {
            $int3 = 0;
        }
    case default :
        while (++$int4 < $int1) {
            if (~script14496($dbrow0, $int4) = 0) {
                return(0);
            }
        }
}
return($int3);
