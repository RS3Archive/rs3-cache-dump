// 2511
[proc,script2511]()(int, int)
def_enum $enum0 = ~script20382;
def_dbrow $dbrow1 = enum(int, dbrow, $enum0, 0);
if (db_getfieldcount($dbrow1, settings_category:col3) = 0) {
    return(1023, 0);
}
$dbrow1 = db_getfield($dbrow1, settings_category:col3, 0);
return(0, enum_getreverseindex(dbrow, int, settings_all_categories, $dbrow1, 0));
