// 8769
[proc,script8769](int $int0)(int)
def_int $int1 = %zoom;
def_int $int2 = 0;
if (%toplevel_v2_camera_mode ! 1 & %toplevel_v2_camera_mode ! 3) {
    return($int1);
}
if (%toplevel_v2_camera_mode = 1) {
    $int2 = scale(calc($int0 - 500), calc(3000 - 500), 1000000);
    $int1 = calc(%zoom + scale(%zoom, 1870000, $int2));
    %zoom = max(min(%zoom, 5725), 1860);
} else {
    $int2 = scale(calc($int0 - 10), calc(4090 - 10), 1000000);
    $int1 = calc(%zoom + scale(%zoom, 1870000, $int2));
    if (~script6431 = true) {
        %zoom = max(min(%zoom, 9000), 1350);
    } else {
        %zoom = max(min(%zoom, 12000), 1350);
    }
}
return($int1);
