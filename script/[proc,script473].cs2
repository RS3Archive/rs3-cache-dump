// 473
[proc,script473](stat $stat0, int $int1, int $int2)(boolean)
def_int $int3 = 1;
def_int $int4 = -1;
def_stat $stat5 = null;
def_int $intarray0(28);
while ($int3 < 28) {
    if (~script7292(enum(int, stat, int_to_stat, $int3)) >= 2000000000) {
        $intarray0($int3) = 1;
    } else {
        $intarray0($int3) = 0;
    }
    $int3 = calc($int3 + 1);
}
if (~skill_unlocked(invention) = false) {
    $intarray0(enum(stat, int, stat_to_int, invention)) = 1;
}
$int3 = 0;
while ($int3 < $int1) {
    $int4 = ~script7230(intarray0, $int2);
    if ($int4 < 1) {
        return(false);
    }
    $stat5 = enum(int, stat, int_to_stat, $int4);
    if ($stat0 = $stat5) {
        return(true);
    } else {
        $intarray0($int4) = 1;
    }
    $int3 = calc($int3 + 1);
}
return(false);
