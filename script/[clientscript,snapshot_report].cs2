// 135
[clientscript,snapshot_report](int $int0)
def_int $int1 = -1;
if (string_length(%snapshot_name) > 0) {
    if ($int0 = 8 & compare(%snapshot_name, %last_macro_name) = 0) {
        mes("Thank-you, your abuse report has been received.");
    } else {
        chat_sendabusereport(%snapshot_name, $int0, %snapshot_mute, "");
        if ($int0 = 8) {
            %last_macro_name = %snapshot_name;
        }
        %snapshot_mute = false;
        ~static_checkbox_create(snapshot_main_v2:name_list_mute_checkbox_build, snapshot_main_v2:name_list_mute_checkbox_click, "", "", "", ~bool_to_int(%snapshot_mute));
        ~static_checkbox_create(snapshot_main_v2:offence_mute_checkbox_build, snapshot_main_v2:offence_mute_checkbox_click, "", "", "", ~bool_to_int(%snapshot_mute));
    }
}
if (compare(%snapshot_name, chat_playername) = 0) {
    ~script15197;
    return;
}
~script1997;
