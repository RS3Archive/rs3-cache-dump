// 13127
[clientscript,script13127](component $component0, int $int1, enum $enum2)
if (cc_find($component0, $int1) = true) {
    ~dynamic_dropdown_hide;
    ~dynamic_dropdown_custom_expand($enum2, $int1);
}
