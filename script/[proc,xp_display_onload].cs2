// 5659
[proc,xp_display_onload]
def_int $int0 = 0;
def_int $int1 = 0;
def_component $component2 = null;
if (~script6431 = true) {
    $int0 = enum_getoutputcount(stat_autoint_to_int);
    while (++$int1 < $int0) {
        $component2 = enum(int, component, stat_to_xp_popup_button_component, $int1);
        if_setop(1, "", $component2);
    }
}
~xp_display_updatevars;
if (%xpdisplay_hide_interface = 1) {
    if_sethide(true, toplevel_v2:xp_tracker_window_content);
} else {
    if_sethide(false, toplevel_v2:xp_tracker_window_content);
}
