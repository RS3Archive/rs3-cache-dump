// 12711
[proc,script12711]()(int, int)
def_int $int0 = 0;
def_int $int1 = max(0, calc(%trh54_super_charge / (500 / 100)));
def_int $int2 = calc(500 / 10);
def_int $int3 = calc(64 / 4);
def_int $int4 = calc(500 / 10 * 9);
def_int $int5 = calc(64 / 4 * 3);
def_int $int6 = 1;
if (%trh54_super_charge < $int2) {
    $int0 = calc(500 / 10 / (64 / 4));
    $int6 = min(calc(64 / 4), max(1, calc(%trh54_super_charge / $int0)));
} else if (%trh54_super_charge < $int4) {
    $int0 = calc(500 / 5 * 2 / (64 / 4));
    $int6 = calc((%trh54_super_charge - ($int2 - 1)) / $int0 + $int3);
} else {
    $int0 = calc(500 / 10 / (64 / 4));
    $int6 = calc((%trh54_super_charge - ($int4 - 1)) / $int0 + $int5);
}
return($int1, $int6);
