// 20424
[proc,script20424](int $int0)(dbrow)
if ($int0 = 0) {
    return(null);
}
def_dbrow $dbrow1 = enum(int, dbrow, anniversary_skillcapes_skill_id_to_dbrow, $int0);
if ($dbrow1 = null) {
    ~error("Can't match skill_id <tostring($int0)> to a dbrow! Something has gone wrong!");
}
return($dbrow1);
