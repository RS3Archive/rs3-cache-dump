// 9784
[proc,script9784](int $int0)(string, string, boolean)
$int0 = ~script15861($int0);
if ($int0 = -1) {
    return("", "", true);
}
def_string $string0 = ~script15897($int0);
def_string $string1 = tostring($int0);
def_string $string2 = "<$string0><br><br>Load this preset and close the bank interface.";
def_boolean $boolean1 = false;
def_int $int2 = 0;
switch_int ($int0) {
    case 8, 9, 10, 11, 12, 13, 14, 15 :
        if (playermember = false) {
            $string2 = "This slot is only available to members.";
            $boolean1 = true;
        }
    case 19 :
        $string1 = "";
        if (map_members = false) {
            $string2 = "A beast of burden preset can only be loaded on members' worlds.";
            $boolean1 = true;
        }
    case 100 :
        $string1 = "";
        $int2 = calc(~script15926 + 1);
        $string2 = "Go to preset page <tostring($int2)> of <tostring(2)>.";
    case 0 :
        $string1 = "";
        $string2 = "Open the preset settings.";
}
return($string1, $string2, $boolean1);
