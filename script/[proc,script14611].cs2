// 14611
[proc,script14611](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    return(0);
}
def_int $int1 = db_getfield($dbrow0, arch_relic:col0, 0);
def_int $int2 = 0;
if (db_getfieldcount($dbrow0, arch_relic:col4) > 0) {
    $int2 = db_getfield($dbrow0, arch_relic:col4, 0);
} else {
    $int2 = calc(~script14612($dbrow0) * 20);
}
if (~script3092 = 1) {
    $int2 = calc($int2 / 2);
}
return($int2);
