// 8128
[proc,script8128](component $component0, int $int1, int $int2)
def_boolean $boolean3 = ~script11695($component0);
if ($boolean3 = false) {
    if ($int2 = 1) {
        cc_setposition(4, calc((0 - if_getheight($component0)) / 2 + $int1 / 2 + 2), ^setpos_abs_centre, ^setpos_abs_centre);
    }
    cc_setsize(max(0, $int1), cc_getheight, ^setsize_abs, ^setsize_abs);
} else {
    cc_setsize(31, max(0, $int1), ^setsize_abs, ^setsize_abs);
}
