// 3555
[proc,script3555]
if (if_gethide(toplevel_v2:escape_menu) = false) {
    if (~script8292(-1, -1) = true) {
        if (struct_param(toplevel_v2_window_data_parent, toplevel_v2_window_close_button_layer) ! null) {
            if_triggerop(struct_param(toplevel_v2_window_data_parent, toplevel_v2_window_close_button_layer), 1, 1);
        }
    } else if (if_gethide(escape_menu_mobile:logout_popup) = false) {
        if_sethide(true, escape_menu_mobile:logout_popup);
    } else if (if_gethide(escape_menu_mobile:side_panel) = false) {
        ~script9540;
    } else if (~script15532(0) = true) {
        if_triggerop(mobile_ribbon_left:home_screen_button, -1, 1);
    } else {
        ~script8182;
    }
}
