// 2296
[clientscript,script2296](int $int0)
def_model $model1 = null;
if ($int0 = 0) {
    if (%pop_captain_temp_hat = enum_getoutputcount(pop_uniform_hat_male)) {
        if (~script2270 = 1) {
            $model1 = enum(int, model, pop_uniform_hat_female, 0);
        } else {
            $model1 = enum(int, model, pop_uniform_hat_male, 0);
        }
    } else if (~script2270 = 1) {
        $model1 = enum(int, model, pop_uniform_hat_female, %pop_captain_temp_hat);
    } else {
        $model1 = enum(int, model, pop_uniform_hat_male, %pop_captain_temp_hat);
    }
} else if (%pop_captain_temp_hat = 0) {
    if (~script2270 = 1) {
        $model1 = enum(int, model, pop_uniform_hat_female, enum_getoutputcount(pop_uniform_hat_male));
    } else {
        $model1 = enum(int, model, pop_uniform_hat_male, enum_getoutputcount(pop_uniform_hat_male));
    }
} else if (~script2270 = 1) {
    $model1 = enum(int, model, pop_uniform_hat_female, %pop_captain_temp_hat);
} else {
    $model1 = enum(int, model, pop_uniform_hat_male, %pop_captain_temp_hat);
}
if_setmodel($model1, pop_captain_customise:hat);
