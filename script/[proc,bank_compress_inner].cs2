// 14358
[proc,bank_compress_inner]
def_int $int0 = 0;
def_int $int1 = %bank_first_freeslot;
def_obj $obj2 = null;
def_int $int3 = 0;
while ($int1 < 1820) {
    if (cc_find(bank:bank_inv, $int1) = true & cc_getinvobject = bank_empty_slot) {
        $int0, $obj2, $int3 = ~script14360(max($int0, calc($int1 + 1)));
        if (cc_find(bank:bank_inv, $int1) = true) {
            cc_setobject($obj2, $int3);
        }
        if (cc_find(bank:bank_inv, $int0) = true) {
            cc_setobject_nonum(bank_empty_slot, 0);
        }
    }
    $int1 = calc($int1 + 1);
}
if_setontimer("script14357", bank:bank_items_container);
