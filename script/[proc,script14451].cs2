// 14451
[proc,script14451](enum $enum0)(int)
if ($enum0 = null) {
    return(0);
}
def_int $int1 = enum_getoutputcount($enum0);
def_int $int2 = -1;
while (++$int2 < $int1) {
    if (~script14449(enum(int, int, $enum0, $int2)) = 0) {
        return(1);
    }
}
return(0);
