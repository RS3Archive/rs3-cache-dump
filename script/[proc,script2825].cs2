// 2825
[proc,script2825](int $int0)
if (%evq_spline_counter = calc(spline_length(0) - 1)) {
    if ($int0 = 1) {
        cam_smoothreset;
    }
    if_setoncamfinished(null, fade2:eventlayer);
    return;
}
cam_movealong(0, %evq_spline_counter, enum(int, int, %evq_spline_enum_speed, %evq_spline_counter), enum(int, int, %evq_spline_enum_speed, calc(%evq_spline_counter + 1)), 1, %evq_spline_counter);
%evq_spline_counter = calc(%evq_spline_counter + 1);
