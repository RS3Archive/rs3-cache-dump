// 6873
[proc,script6873]
def_enum $enum0 = player_kit_hair_index_to_basecolour;
def_enum $enum1 = player_kit_hair_colour;
def_int $int2 = 2344;
def_int $int3 = 0;
def_int $int4 = 0;
def_int $int5 = 0;
def_string $string0 = "";
if ($enum0 ! null & $enum1 ! null) {
    $int4 = enum_getoutputcount($enum0);
    while ($int3 < $int4) {
        cc_create(mtxmgt:colours, ^iftype_rectangle, $int5);
        cc_setsize(18, 18, ^setsize_abs, ^setsize_abs);
        if (calc($int3 % 2) = 0) {
            cc_setposition(5, calc(35 + $int3 * 12), ^setpos_abs_left, ^setpos_abs_top);
        } else {
            cc_setposition(5, calc(35 + ($int3 - 1) * 12), ^setpos_abs_right, ^setpos_abs_top);
        }
        cc_setfill(true);
        cc_setcolour(enum(int, int, $enum1, $int3));
        cc_setop(1, "Select-primary-colour");
        if (%mtxmgt_enable_secondary_colour = 1) {
            cc_setop(2, "Select-secondary-colour");
        }
        $int5 = calc($int5 + 1);
        cc_create(mtxmgt:colours, ^iftype_graphic, $int5);
        if (calc($int3 % 2) = 0) {
            cc_setposition(4, calc(34 + $int3 * 12), ^setpos_abs_left, ^setpos_abs_top);
        } else {
            cc_setposition(4, calc(34 + ($int3 - 1) * 12), ^setpos_abs_right, ^setpos_abs_top);
        }
        cc_setsize(20, 20, ^setsize_abs, ^setsize_abs);
        ~script14990(0, $string0);
        $int5 = calc($int5 + 1);
        $int3 = calc($int3 + 1);
    }
    ~script6875;
}
