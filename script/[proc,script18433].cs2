// 18433
[proc,script18433](int $int0)(graphic)
db_find(buff_select_event_buff:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 ! null) {
    return(db_getfield($dbrow1, buff_select_event_buff:col6, 0));
}
return(null);
