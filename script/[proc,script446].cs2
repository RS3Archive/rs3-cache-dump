// 446
[proc,script446](component $component0, dbrow $dbrow1, int $int2)
def_int $int3 = -1;
if ($dbrow1 ! null) {
    if ($int2 = 0 & db_getfield($dbrow1, trh_chest:col5, 0) ! null) {
        if_setmodel(db_getfield($dbrow1, trh_chest:col5, 0), $component0);
    } else {
        if_setmodel(db_getfield($dbrow1, trh_chest:col4, 0), $component0);
    }
    if_setmodelanim(db_getfield($dbrow1, trh_chest:col8, 0), $component0);
    while (++$int3 < db_getfieldcount($dbrow1, trh_chest:col6)) {
        if_setretex($int3, db_getfield($dbrow1, trh_chest:col6, $int3), $component0);
    }
}
