// 13399
[proc,script13399](struct $struct0, int $int1)(string)
def_string $string0 = "";
def_int $int2 = ~script17278($struct0);
def_int $int3 = ~script17280($struct0);
def_string $string1 = "";
def_string $string2 = "";
if ($int1 = 1) {
    $string1 = tostring(abs($int2));
    $string2 = tostring(abs($int3));
} else {
    $string1 = ~script17279($struct0);
    $string2 = ~script17281($struct0);
}
if ($int2 > 0) {
    $string0 = "Restores <col=00ff00><$string1></col> special move points";
} else if ($int2 < 0) {
    $string0 = "Removes <col=ff0000><$string1></col> special move points";
}
if ($int3 > 0) {
    if (string_length($string0) > 0) {
        $string0 = append($string0, ", and renews <col=00ff00><$string2></col> special move points over the duration");
    } else {
        $string0 = append($string0, "Renews <col=00ff00><$string2></col> special move points over the duration");
    }
} else if ($int3 < 0) {
    if (string_length($string0) > 0) {
        $string0 = append($string0, ", and drains <col=ff0000><$string2></col> special move points over the duration");
    } else {
        $string0 = append($string0, "Drains <col=ff0000><$string2></col> special move points over the duration");
    }
}
if (string_length($string0) > 0) {
    $string0 = append($string0, ".");
}
return($string0);
