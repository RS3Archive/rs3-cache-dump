// 2297
[clientscript,script2297](int $int0)
def_model $model1 = null;
if ($int0 = 0) {
    if (%pop_captain_temp_torso = enum_getoutputcount(pop_captain_body)) {
        if (~script2270 = 1) {
            $model1 = enum(int, model, pop_captain_body_female, 0);
        } else {
            $model1 = enum(int, model, pop_captain_body, 0);
        }
    } else if (~script2270 = 1) {
        $model1 = enum(int, model, pop_captain_body_female, %pop_captain_temp_torso);
    } else {
        $model1 = enum(int, model, pop_captain_body, %pop_captain_temp_torso);
    }
} else if (%pop_captain_temp_torso = 0) {
    if (~script2270 = 1) {
        $model1 = enum(int, model, pop_captain_body_female, enum_getoutputcount(pop_captain_body_female));
    } else {
        $model1 = enum(int, model, pop_captain_body, enum_getoutputcount(pop_captain_body));
    }
} else if (~script2270 = 1) {
    $model1 = enum(int, model, pop_captain_body_female, %pop_captain_temp_torso);
} else {
    $model1 = enum(int, model, pop_captain_body, %pop_captain_temp_torso);
}
if_setmodel($model1, pop_captain_customise:torso);
