// 2298
[proc,quickchat_onkey_countdialog](int $key0, char $char1, int $int2)
def_component $component3 = ~script8599($int2);
def_component $component4 = ~script8589($int2);
def_chatphrase $chatphrase5 = null;
if (if_find($component4) = true) {
    $chatphrase5 = cc_param(toplevel_v2_chat_quickchat_rootphrase);
}
def_int $int6 = string_length(tostring(~script8621($int2)));
def_int $int7 = string_indexof_char("0123456789", $char1, 0);
switch_int ($key0) {
    case ^key_escape :
        ~quickchat_close($int2);
        return;
    case ^key_home :
        ~quickchat_open(~script8613($int2), ~script8605($int2), $int2);
        return;
    case ^key_return :
        if ($int6 > 0) {
            ~quickchat_phrase_int($component3, $chatphrase5, ~script8621($int2), $int2);
        } else {
            ~quickchat_close($int2);
        }
        return;
    case ^key_backspace :
        if ($int6 > 0) {
            ~script8622($int2, calc(~script8621($int2) / 10));
        } else if (~script8615($int2) = false) {
            ~quickchat_close($int2);
        } else {
            if_sethide(true, ~script8588($int2));
            if_sethide(true, ~script8590($int2));
            if_sethide(true, ~script8594($int2));
            if_sethide(false, ~script8598($int2));
            if_sethide(false, ~script8599($int2));
            if_sethide(true, ~script8600($int2));
            return;
        }
    case default :
        if ($int7 >= 0 & $int6 < 10 & calc(~script8621($int2) * 10 + $int7) > 0) {
            ~script8622($int2, calc(~script8621($int2) * 10 + $int7));
        }
}
if (~script8621($int2) > 0) {
    if_settext("Please enter a value: <tostring(~script8621($int2))>*", $component4);
} else {
    if_settext("Please enter a value: *", $component4);
}
if_settextshadow(true, $component4);
