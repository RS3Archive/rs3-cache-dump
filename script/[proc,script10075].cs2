// 10075
[proc,script10075]()(component)
switch_toplevelinterface (if_gettop) {
    case default :
        return(toplevel_v2:tooltips_layer);
    case lobbyscreen :
        return(lobbyscreen:tooltips_layer);
    case loginscreen :
        return(loginscreen:tooltips_layer);
}
