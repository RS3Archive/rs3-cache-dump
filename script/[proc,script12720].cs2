// 12720
[proc,script12720](boolean $boolean0)
def_int $int1 = cc_param(rs3tli_component_sound_id);
if ($int1 ! -1) {
    switch_int (cc_param(rs3tli_component_sound_event)) {
        case 0 :
            if ($boolean0 = true) {
                ~script12721($int1);
            }
        case default :
            ~script7005($int1);
            cc_setparam(rs3tli_component_sound_id, -1);
    }
}
