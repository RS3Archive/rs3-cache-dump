// 17300
[proc,script17300](obj $obj0, obj $obj1)(int)
def_int $int2 = 0;
if (oc_param($obj0, recipe_ignore_runestaff) = true) {
    if (oc_members($obj1) = 1 & map_members = false) {
        return(0);
    }
    $int2 = ~script11882($int2, inv_total(inv, $obj1));
    if ($obj0 ! eyeglo_ground_mud_runes) {
        $int2 = ~script11882($int2, ~script12787($obj1, 0));
    }
} else {
    $int2 = ~get_rune_total($obj1);
}
return($int2);
