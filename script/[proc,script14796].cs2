// 14796
[proc,script14796](int $int0)
def_int $int1 = enum_getoutputcount(arch_site_map_list);
def_int $int2 = 0;
def_int $int3 = 0;
def_string $string0 = "";
def_int $int4 = -1;
if ($int0 = 1) {
    ~script8180($int0, 18);
    while (++$int4 < $int1) {
        $int2, $int3, $string0 = ~script14801(enum(int, dbrow, arch_site_map_list, $int4));
        ~script8844(2, $int2, $int3, arch_site_map:icon_control_layer, $int4);
    }
} else {
    while (++$int4 < $int1) {
        ~script8844(2, 0, 0, arch_site_map:icon_control_layer, $int4);
    }
}
