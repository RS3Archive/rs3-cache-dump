// 13580
[proc,script13580](component $component0, int $int1, int $int2, int $int3)
if (if_find($component0) = true) {
    if ($int1 ! -1) {
        cc_setparam(rs3tli_progress_current, ~clamp($int1, 1, 65536));
    }
    if ($int2 ! -1) {
        cc_setparam(rs3tli_progress_target, ~clamp($int2, 1, 65536));
    }
    cc_setparam(rs3tli_progress_vertical, ~clamp($int3, 0, 1));
}
