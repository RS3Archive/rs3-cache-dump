// 19507
[proc,script19507]
if (%toplevel_v2_legacy_interface = 1) {
    ~script19508(struct_param(toplevel_v2_window_data_bank_window, toplevel_v2_window_layer), 714);
    if_sethide(true, group_ironman_storage:filter_bg);
} else {
    if_sethide(false, group_ironman_storage:filter_bg);
}
def_int $int0 = 0;
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 0;
$int0, $int2, $int1, $int3 = ~script2956;
if_setposition($int0, $int2, ^setpos_abs_left, ^setpos_abs_top, group_ironman_storage:all);
if_setsize(calc($int0 + $int1), calc($int2 + $int3), ^setsize_minus, ^setsize_minus, group_ironman_storage:all);
def_int $int4 = 213;
def_boolean $boolean5 = ~script6431;
if ($boolean5 = true) {
    if_setposition(70, 12, ^setpos_abs_right, ^setpos_abs_top, group_ironman_storage:help_button_layer);
    $int4 = 253;
} else {
    if_setposition(50, 15, ^setpos_abs_right, ^setpos_abs_top, group_ironman_storage:help_button_layer);
}
def_int $int6 = if_getheight(group_ironman_storage:content);
if_setsize($int4, 0, ^setsize_abs, ^setsize_minus, group_ironman_storage:right_panel);
if_setsize(calc($int4 + 4), 0, ^setsize_minus, ^setsize_minus, group_ironman_storage:main_panel);
if (%bank_filter ! 0 | %share_preset_menu_open = 1) {
    if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_bottom, group_ironman_storage:clan_items_layer);
} else {
    if_setposition(0, 0, ^setpos_abs_right, ^setpos_abs_bottom, group_ironman_storage:clan_items_layer);
}
