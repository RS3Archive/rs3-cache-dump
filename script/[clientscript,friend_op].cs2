// 126
[clientscript,friend_op](int $int0, string $string0, int $int1)
if (%has_displayname_client = false) {
    return;
}
def_int $int2 = -1;
def_string $string1 = removetags($string0);
switch_int ($int0) {
    case 1 :
        if (%share_in_progress = 0) {
            ~script8512(%chatbox_focus, $string1);
        }
    case 2 :
        ~quickchat_open_private($int1, $string1, 19);
    case 4, 5 :
        if (%share_in_progress = 1 & $int0 = 4) {
            ~script8512(%chatbox_focus, $string1);
        }
        mes("That player is currently offline.");
    case 8 :
        %friendtodelete = $string1;
        ~script2018(8);
}
%share_in_progress = 0;
