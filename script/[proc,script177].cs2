// 177
[proc,script177](int $int0)
if ($int0 = 1) {
    ~script1558(%chatbox_focus, 0);
    ~script7172(%chatbox_focus);
    if (~script8586(%chatbox_focus) ! null) {
        if_setonkey("quickchat_onkey(event_keycode, event_keychar)", ~script8586(%chatbox_focus));
    }
} else {
    %combatv2_bar_delay_on = 1;
    ~script7172(-1);
    ~script1558(%chatbox_focus, 0);
    if (~script8586(%chatbox_focus) ! null) {
        if_setonkey(null, ~script8586(%chatbox_focus));
    }
}
