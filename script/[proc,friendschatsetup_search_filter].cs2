// 11986
[proc,friendschatsetup_search_filter](string $string0)
def_int $int0 = 0;
def_int $int1 = friend_count;
if (compare($string0, "") = 0) {
    while ($int0 < $int1) {
        if (cc_find(friendschatsetup:rank, $int0) = true) {
            cc_sethide(false);
        }
        if (cc_find(friendschatsetup:icon, $int0) = true) {
            cc_sethide(false);
        }
        if (cc_find(friendschatsetup:name, $int0) = true) {
            cc_sethide(false);
        }
        $int0 = calc($int0 + 1);
    }
    return;
}
def_boolean $boolean2 = false;
while ($int0 < $int1) {
    if (cc_find(friendschatsetup:name, $int0) = true) {
        if (string_indexof_string(lowercase(cc_gettext), lowercase($string0), 0) = -1) {
            $boolean2 = true;
        } else {
            $boolean2 = false;
        }
        cc_sethide($boolean2);
    }
    if (cc_find(friendschatsetup:rank, $int0) = true) {
        cc_sethide($boolean2);
    }
    if (cc_find(friendschatsetup:icon, $int0) = true) {
        cc_sethide($boolean2);
    }
    $int0 = calc($int0 + 1);
}
