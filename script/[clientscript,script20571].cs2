// 20571
[clientscript,script20571](stat $stat0, int $int1, component $component2, component $component3)
%marketplace_bonus_xp_current_choice = $int1;
def_int $int4 = calc(enum_getoutputcount(marketplace_bonus_xp_reward_buttonlist) - 1);
while ($int4 >= 0) {
    $component2 = enum(int, component, marketplace_bonus_xp_reward_buttonlist, $int4);
    if ($component2 ! null) {
        if_callonresize($component2);
    }
    $int4 = calc($int4 - 1);
}
if_callonresize(marketplace_bonus_xp_redeem:redeem);
