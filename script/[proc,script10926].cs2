// 10926
[proc,script10926](stat $stat0, int $int1, int $int2)(int)
if ($int1 = 0 & $int2 = 1) {
    mes("You cannot use this to increase your <enum(stat, string, stat_to_string, $stat0)> skill.");
    sound_vorbis_volume(omge_error, 1, 0, 150);
}
return($int1);
