// 13044
[proc,book_get_title](dbrow $dbrow0)(string)
if ($dbrow0 = null) {
    return("");
}
def_string $string0 = db_getfield($dbrow0, book:col1, 0);
if (string_length($string0) = 0) {
    if (~script18962($dbrow0) ! null) {
        $string0 = oc_name(db_getfield($dbrow0, book:col3, 0));
    } else {
        $string0 = "Book";
    }
}
return($string0);
