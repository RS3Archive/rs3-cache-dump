// 6698
[proc,script6698](component $component0, component $component1, int $int2, component $component3, int $int4)
def_string $string0 = "This shows how many moths you have collected in your jar.";
if (%bigchinchompa_jarcontents = -1) {
    if_settext("-<br>-", $component1);
    if_setonmouserepeat("script8799($string0, event_com, event_comsubid)", $component0);
    return;
}
def_int $int5 = 0;
def_int $int6 = 0;
def_int $int7 = 0;
def_int $int8 = 0;
def_int $int9 = 0;
def_int $int10 = 0;
def_int $int11 = 0;
def_int $int12 = 0;
def_int $int13 = 0;
while ($int13 < 32) {
    switch_int (calc($int13 / 4)) {
        case 0 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int5 = setbit($int5, calc($int13 % 4));
            }
        case 1 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int6 = setbit($int6, calc($int13 % 4));
            }
        case 2 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int7 = setbit($int7, calc($int13 % 4));
            }
        case 3 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int8 = setbit($int8, calc($int13 % 4));
            }
        case 4 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int9 = setbit($int9, calc($int13 % 4));
            }
        case 5 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int10 = setbit($int10, calc($int13 % 4));
            }
        case 6 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int11 = setbit($int11, calc($int13 % 4));
            }
        case 7 :
            if (testbit(%bigchinchompa_jarcontents, $int13) = ^true) {
                $int12 = setbit($int12, calc($int13 % 4));
            }
    }
    $int13 = calc($int13 + 1);
}
def_int $int14 = calc($int5 + $int6 + $int7 + $int8 + $int9 + $int10 + $int11 + $int12);
if_settext("<col=ffffff><tostring(min($int14, 15))></col><br>15", $component1);
if_settext(tostring($int5), bigchinchompa_hud:moth1);
if_settext(tostring($int6), bigchinchompa_hud:moth2);
if_settext(tostring($int7), bigchinchompa_hud:moth3);
if_settext(tostring($int8), bigchinchompa_hud:moth4);
if_settext(tostring($int9), bigchinchompa_hud:moth5);
if_settext(tostring($int10), bigchinchompa_hud:moth6);
if_settext(tostring($int11), bigchinchompa_hud:moth7);
if_settext(tostring($int12), bigchinchompa_hud:moth8);
~deltooltip_action($component3);
