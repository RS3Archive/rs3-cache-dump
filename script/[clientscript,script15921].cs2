// 15921
[clientscript,script15921](component $component0, int $int1)
if (cc_find($component0, $int1) = true) {
    if ($int1 > 0 & ~script15456($int1) = true) {
        return;
    }
    if_setsize(0, cc_getheight, ^setsize_minus, ^setsize_abs, sworld_start:sworld_preset_highlight_active);
    if_setposition(cc_getx, cc_gety, ^setpos_abs_left, ^setpos_abs_top, sworld_start:sworld_preset_highlight_active);
    %share_preset_last_preset = $int1;
}
if_sethide(false, sworld_start:sworld_preset_highlight_active);
