// 16383
[proc,script16383](dbrow $dbrow0, int $int1)(dbrow)
def_int $int2 = 0;
def_dbrow $dbrow3 = null;
switch_int (~script16379) {
    case 1 :
        $int2 = db_getfieldcount($dbrow0, uitutorial:col3);
        if ($int1 < $int2) {
            $dbrow3 = db_getfield($dbrow0, uitutorial:col3, $int1);
        }
    case 2 :
        $int2 = db_getfieldcount($dbrow0, uitutorial:col4);
        if ($int1 < $int2) {
            $dbrow3 = db_getfield($dbrow0, uitutorial:col4, $int1);
        }
    case 0 :
        $int2 = db_getfieldcount($dbrow0, uitutorial:col2);
        if ($int1 < $int2) {
            $dbrow3 = db_getfield($dbrow0, uitutorial:col2, $int1);
        }
}
return($dbrow3);
