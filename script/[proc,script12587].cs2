// 12587
[proc,script12587](int $int0, int $int1, int $int2)(string)
def_int $int3 = calc($int0 / 7);
def_int $int4 = calc($int3 / 52);
if ($int4 > 0) {
    return("<gt>1y");
} else if ($int3 > 0) {
    return("<tostring($int3)>w");
} else if ($int0 > 0) {
    if ($int1 > 0) {
        return("<tostring($int0)>d <tostring($int1)>h");
    } else {
        return("<tostring($int0)>d");
    }
} else if ($int1 > 9) {
    return("<tostring($int1)>h");
} else if ($int1 > 0) {
    if ($int2 > 0) {
        return("<tostring($int1)>h <tostring($int2)>m");
    } else {
        return("<tostring($int1)>h");
    }
} else if ($int2 > 0) {
    return("<tostring($int2)>m");
} else {
    return("");
}
