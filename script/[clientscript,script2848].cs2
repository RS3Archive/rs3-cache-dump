// 2848
[clientscript,script2848](component $component0, int $int1, component $component2, int $int3, int $int4, int $int5)
def_int $int6 = 0;
def_int $int7 = 0;
if ($component2 ! null) {
    if ($int3 = -1) {
        if (if_find($component2) = true & $component2 = rm_wires:wires_corral) {
            $int6 = $int4;
            $int7 = $int5;
            if (.cc_find($component0, $int1) = true) {
                .cc_setposition(enum(int, int, rm_sq_corral_x, $int1), enum(int, int, rm_sq_corral_y, $int1), ^setpos_abs_left, ^setpos_abs_top);
            }
        }
    } else {
        if ($int3 < 9) {
            return;
        }
        if (~script2849(calc($int3 - 9)) = 0) {
            if (cc_find($component2, $int3) = true) {
                $int6 = cc_getx;
                $int7 = cc_gety;
            }
            if (.cc_find($component0, $int1) = true) {
                switch_int (randominc(2)) {
                    case 0 :
                        sound_synth(rm_puzzle_click_drop_01, 1, 0);
                    case 1 :
                        sound_synth(rm_puzzle_click_drop_02, 1, 0);
                    case 2 :
                        sound_synth(rm_puzzle_click_drop_03, 1, 0);
                }
                .cc_setposition($int6, $int7, ^setpos_abs_left, ^setpos_abs_top);
            }
        }
    }
}
