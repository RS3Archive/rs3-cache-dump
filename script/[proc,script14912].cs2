// 14912
[proc,script14912](dbrow $dbrow0)(string)
if ($dbrow0 = null) {
    return("");
}
def_obj $obj1 = db_getfield($dbrow0, arch_restoration:col6, 0);
def_int $int2 = 20;
def_dbrow $dbrowarray0($int2);
def_int $int3 = db_listall(collection_log);
def_dbrow $dbrowarray1($int3);
def_int $int4 = db_find_with_count(collection:col13, $obj1);
if ($int4 = 0) {
    return("None");
}
$int4 = db_find_refine(collection:col2, 2);
def_dbrow $dbrowarray2($int4);
def_int $int5 = 0;
def_int $int6 = -1;
def_int $int7 = -1;
def_int $int8 = -1;
while (++$int6 < $int4) {
    $dbrowarray2($int6) = db_findnext;
}
$int6 = -1;
while (++$int6 < $int4) {
    $int3 = db_find_with_count(collection_log:col7, $dbrowarray2($int6));
    $int7 = -1;
    while (++$int7 < $int3) {
        $dbrowarray1($int7) = db_findnext;
    }
    $int7 = -1;
    while (++$int7 < $int3) {
        $int2 = db_find_with_count(collector:col7, $dbrowarray1($int7));
        $int8 = -1;
        while (++$int8 < $int2) {
            $dbrowarray0($int5++) = db_findnext;
        }
    }
}
def_string $string0 = "";
def_dbrow $dbrow9 = null;
$int6 = 0;
while ($int6 < $int5) {
    $dbrow9 = $dbrowarray0($int6);
    if ($dbrow9 ! null) {
        $int7 = 0;
        while (++$int6 < $int5) {
            if ($dbrowarray0($int6) ! $dbrow9) {
                $dbrowarray0($int7++) = $dbrowarray0($int6);
            }
        }
        $int6 = 0;
        $int5 = $int7;
        $string0 = "<$string0><db_getfield($dbrow9, collector:col1, 0)>, ";
    }
}
if (string_length($string0) > 0) {
    return(substring($string0, 0, calc(string_length($string0) - 2)));
}
return("None");
