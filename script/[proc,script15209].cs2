// 15209
[proc,script15209](dbrow $dbrow0)
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 0;
def_int $int5 = 0;
$int1, $int2 = db_getfield($dbrow0, runepass_table:col2, 0);
$int3, $int4, $int5 = ~time_countdown($int2, 1);
if ($int3 <= 0 & $int4 <= 0 & $int5 <= 0) {
    if_settext("Event has finished", runepass:time_remaining_text);
    if_setontimer(null, runepass:time_remaining_layer);
    return;
}
if_settext("<tostring($int3)> Days, <tostring($int4)> Hrs, <tostring($int5)> Mins", runepass:time_remaining_text);
