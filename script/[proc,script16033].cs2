// 16033
[proc,script16033](struct $struct0)(int)
def_enum $enum1 = struct_param($struct0, rsraffle_prize_list);
def_int $int2 = -1;
def_int $int3 = enum_getoutputcount($enum1);
while (++$int2 < $int3) {
    if (~script16034($int2) = false) {
        return($int2);
    }
}
return(-1);
