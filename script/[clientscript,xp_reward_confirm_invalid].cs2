// 6044
[clientscript,xp_reward_confirm_invalid]
if (if_gethide(xp_reward:xp_type_choice) = false & %trh154_bonus_xp = 2) {
    mes("You must choose an xp type first.");
} else {
    mes("You must choose a skill first.");
}
sound_vorbis_volume(omge_error, 1, 0, 150);
