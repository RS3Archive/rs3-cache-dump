// 16637
[clientscript,trh184_modifier_onop](component $component0, int $int1)
def_int $int2 = ~trh184_get_modifier_slot_id($int1);
def_string $string0 = "";
if ($int2 = 0) {
    $string0 = "Triggers the Rainbow Bonus!";
} else if ($int2 = 1) {
    $string0 = "Awards a single prize.";
} else {
    $string0 = "Awards x<tostring($int2)> prizes!";
}
if ($int1 > 1) {
    $string0 = append($string0, "<br>Active in <tostring($int1)> keys.");
} else {
    $string0 = append($string0, "<br>Active on your next key.");
}
~script426($string0, $component0, -1, 0);
