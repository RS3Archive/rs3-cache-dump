// 20171
[proc,script20171](int $int0, int $int1)(int)
if ($int1 = -1) {
    return(0);
}
switch_int ($int0) {
    case 0 :
        return(long_getbit(%league_task_grouping_tier_toggles, $int1));
    case 1 :
        return(long_getbit(%league_task_grouping_locality_toggles, $int1));
    case default :
        ~error("Grouping <tostring($int0)> missing plugin.");
}
