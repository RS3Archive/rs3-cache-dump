// 3152
[proc,script3152](int $int0)(int)
switch_int ($int0) {
    case 1 :
        return(%incense_guam_active);
    case 2 :
        return(%incense_tarromin_active);
    case 3 :
        if (map_members = true) {
            return(%incense_marrentill_active);
        }
    case 4 :
        if (map_members = true) {
            return(%incense_harralander_active);
        }
    case 5 :
        if (map_members = true) {
            return(%incense_ranarr_active);
        }
    case 6 :
        if (map_members = true) {
            return(%incense_toadflax_active);
        }
    case 7 :
        if (map_members = true) {
            return(%incense_spirit_active);
        }
    case 8 :
        if (map_members = true) {
            return(%incense_irit_active);
        }
    case 9 :
        if (map_members = true) {
            return(%incense_wergali_active);
        }
    case 10 :
        if (map_members = true) {
            return(%incense_avantoe_active);
        }
    case 11 :
        if (map_members = true) {
            return(%incense_kwuarm_active);
        }
    case 12 :
        if (map_members = true) {
            return(%incense_bloodweed_active);
        }
    case 13 :
        if (map_members = true) {
            return(%incense_snapdragon_active);
        }
    case 14 :
        if (map_members = true) {
            return(%incense_cadantine_active);
        }
    case 15 :
        if (map_members = true) {
            return(%incense_lantadyme_active);
        }
    case 16 :
        if (map_members = true) {
            return(%incense_dwarf_active);
        }
    case 17 :
        if (map_members = true) {
            return(%incense_torstol_active);
        }
    case 18 :
        if (map_members = true) {
            return(%incense_fellstalk_active);
        }
    case default :
        ~error("Attempted to get var for unexpected burner type: <tostring($int0)>");
        return(-1);
}
return(-1);
