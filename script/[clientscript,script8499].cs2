// 8499
[clientscript,script8499](component $component0, int $int1)
def_struct $struct2 = null;
def_int $int3 = 0;
if (cc_find(prayer_quick_preset_interface:glow, $int1) = true) {
    if (~script6799(%prayer_quick_selected_preset) = 1) {
        $struct2 = enum(int, struct, combatv2_curse_order_by_level, $int1);
    } else {
        $struct2 = enum(int, struct, combatv2_prayer_order_by_level, $int1);
        $struct2 = ~script9101($struct2);
    }
    $int3 = struct_param($struct2, combatv2_ability_level_req);
    if (stat_base(prayer) < $int3) {
        cc_setgraphic(null);
        return;
    } else {
        cc_setgraphic(combatv2_prayerglow);
    }
    ~script8562($struct2);
}
