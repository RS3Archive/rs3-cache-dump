// 12243
[proc,script12243](enum $enum0)
def_int $int1 = -1;
while (++$int1 < enum_getoutputcount($enum0)) {
    if (~script7083(~script9101(enum(int, struct, $enum0, $int1))) = 1) {
        ~script12247(%prayer_oldest_temporary, $int1);
        if (++%prayer_oldest_temporary >= 20) {
            %prayer_oldest_temporary = calc(%prayer_oldest_temporary % 20);
        }
    }
}
