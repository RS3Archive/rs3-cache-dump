// 11870
[proc,mobile_lore_bar](component $component0, component $component1, component $component2)
def_int $int3 = 0;
if (%toplevel_v2_mobile_show_summoning_points = 1) {
    $int3 = max(0, scale(%lore_spell_points, 60, if_getheight($component2)));
    if_settext(tostring(%lore_spell_points), $component1);
} else {
    $int3 = max(0, scale(~lorepoints_visible, calc(~script859 / 10), if_getheight($component2)));
    if_settext(tostring(~lorepoints_visible), $component1);
}
if_setsize(if_getwidth($component0), $int3, ^setsize_abs, ^setsize_abs, $component0);
