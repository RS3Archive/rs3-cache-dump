// 11853
[proc,script11853](int $int0, int $int1)(int)
switch_int ($int1) {
    case 1 :
        $int0 = calc($int0 / 10);
    case 2, 4 :
        if (%telemetry_interval_type = telemetryinterval_1) {
            $int0 = calc(~script11836($int0, calc(%telemetry_stopwatch_time / 50)) / 10);
        }
    case 11, 15 :
        if (%toplevel_v2_legacy_combat = 1) {
            $int0 = calc($int0 / 10);
        }
    case 17, 18 :
        if (%toplevel_v2_legacy_combat = 1) {
            $int0 = calc($int0 / 10);
        }
        if (%telemetry_interval_type = telemetryinterval_1) {
            $int0 = ~script11837($int0, calc(%telemetry_stopwatch_time / 50));
        }
    case 35 :
        if (%telemetry_interval_type = telemetryinterval_1) {
            $int0 = ~script11836($int0, calc(%telemetry_stopwatch_time / 50));
        }
}
return($int0);
