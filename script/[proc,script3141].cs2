// 3141
[proc,script3141](int $int0, string $string0)
if (%lobbyscreen_world_switch > 0 & %lobbyscreen_world_switch ! $int0) {
    ~script3686("Sorry, you cannot switch to world <tostring($int0)> at this time.<br>Please continue on world <tostring(%lobbyscreen_world_switch)>.");
    $int0 = %lobbyscreen_world_switch;
}
if (worldlist_switch($int0, $string0) = true) {
    %loginscreen_pvp_warned = 0;
} else {
    ~script3686("Sorry, we couldn't contact world <tostring($int0)>.<br>Please choose a different world.");
}
~script3116;
