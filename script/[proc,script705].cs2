// 705
[proc,script705](int $int0, int $int1)(int)
def_int $int2 = ~script14353(0, calc(1820 - 1));
~error("Tab <tostring($int1)> returned slot <tostring($int0)> which is out of range (<tostring(1820)>). First freeslot is <tostring(%bank_first_freeslot)>. Empty proc found at: <tostring($int2)>. If the free slot isn't null that will be used instead.");
if ($int2 ! -1) {
    return($int2);
}
return($int0);
