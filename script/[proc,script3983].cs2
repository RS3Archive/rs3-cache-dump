// 3983
[proc,script3983](struct $struct0)(int)
if ($struct0 = null) {
    return(0);
}
switch_struct ($struct0) {
    case cheevo_atlum_beginner_reward :
        if (%atlum_easy_done = 1) {
            return(1);
        }
    case cheevo_atlum_easy_reward :
        if (%atlum_med_done = 1) {
            return(1);
        }
    case cheevo_atlum_medium_reward :
        if (%atlum_hard_done = 1) {
            return(1);
        }
    case cheevo_atlum_hard_reward :
        if (%atlum_elite_done = 1) {
            return(1);
        }
    case cheevo_atar_easy_reward :
        if (%atar_easy_done = 1) {
            return(1);
        }
    case cheevo_atar_medium_reward :
        if (%atar_med_done = 1) {
            return(1);
        }
    case cheevo_atar_hard_reward :
        if (%atar_hard_done = 1) {
            return(1);
        }
    case cheevo_atar_elite_reward :
        if (%atar_elite_done = 1) {
            return(1);
        }
    case cheevo_atdes_easy_reward :
        if (%atdes_easy_done = 1) {
            return(1);
        }
    case cheevo_atdes_medium_reward :
        if (%atdes_med_done = 1) {
            return(1);
        }
    case cheevo_atdes_hard_reward :
        if (%atdes_hard_done = 1) {
            return(1);
        }
    case cheevo_atdes_elite_reward :
        if (%atdes_elite_done = 1) {
            return(1);
        }
    case cheevo_atdun_easy_reward :
        if (%atdun_easy_done = 1) {
            return(1);
        }
    case cheevo_atdun_medium_reward :
        if (%atdun_med_done = 1) {
            return(1);
        }
    case cheevo_atdun_hard_reward :
        if (%atdun_hard_done = 1) {
            return(1);
        }
    case cheevo_atdun_elite_reward :
        if (%atdun_elite_done = 1) {
            return(1);
        }
    case cheevo_atelf_easy_reward :
        if (%atelf_easy_done = 1) {
            return(1);
        }
    case cheevo_atelf_medium_reward :
        if (%atelf_med_done = 1) {
            return(1);
        }
    case cheevo_atelf_hard_reward :
        if (%atelf_hard_done = 1) {
            return(1);
        }
    case cheevo_atelf_elite_reward :
        if (%atelf_elite_done = 1) {
            return(1);
        }
    case cheevo_atfal_easy_reward :
        if (%atfal_easy_done = 1) {
            return(1);
        }
    case cheevo_atfal_medium_reward :
        if (%atfal_med_done = 1) {
            return(1);
        }
    case cheevo_atfal_hard_reward :
        if (%atfal_hard_done = 1) {
            return(1);
        }
    case cheevo_atfal_elite_reward :
        if (%atfal_elite_done = 1) {
            return(1);
        }
    case cheevo_atjun_easy_reward :
        if (%atjun_easy_done = 2) {
            return(1);
        }
    case cheevo_atjun_medium_reward :
        if (%atjun_med_done = 2) {
            return(1);
        }
    case cheevo_atjun_hard_reward :
        if (%atjun_hard_done = 2) {
            return(1);
        }
    case cheevo_atjun_elite_reward :
        if (%atjun_elite_done = 1) {
            return(1);
        }
    case cheevo_atmo_easy_reward :
        if (%atmo_easy_done = 1) {
            return(1);
        }
    case cheevo_atmo_medium_reward :
        if (%atmo_med_done = 1) {
            return(1);
        }
    case cheevo_atmo_hard_reward :
        if (%atmo_hard_done = 1) {
            return(1);
        }
    case cheevo_atmo_elite_reward :
        if (%atmo_elite_done = 1) {
            return(1);
        }
    case cheevo_atseer_easy_reward :
        if (%atseer_easy_done = 1) {
            return(1);
        }
    case cheevo_atseer_medium_reward :
        if (%atseer_med_done = 1) {
            return(1);
        }
    case cheevo_atseer_hard_reward :
        if (%atseer_hard_done = 1) {
            return(1);
        }
    case cheevo_atseer_elite_reward :
        if (%atseer_elite_done = 1) {
            return(1);
        }
    case cheevo_atvar_easy_reward :
        if (%atvar_easy_done = 1) {
            return(1);
        }
    case cheevo_atvar_medium_reward :
        if (%atvar_med_done = 1) {
            return(1);
        }
    case cheevo_atvar_hard_reward :
        if (%atvar_hard_done = 1) {
            return(1);
        }
    case cheevo_atvar_elite_reward :
        if (%atvar_elite_done = 1) {
            return(1);
        }
    case cheevo_atwild_easy_reward :
        if (%atwild_reward_easy >= 1) {
            return(1);
        }
    case cheevo_atwild_medium_reward :
        if (%atwild_reward_med >= 1) {
            return(1);
        }
    case cheevo_atwild_hard_reward :
        if (%atwild_reward_hard >= 1) {
            return(1);
        }
    case cheevo_atwild_elite_reward :
        if (%atwild_reward_elite >= 1) {
            return(1);
        }
    case cheevo_atq200_easy_reward :
        if (%q200_varrock_armour_easy = 1) {
            return(1);
        }
    case cheevo_atq200_medium_reward :
        if (%q200_varrock_armour_medium = 1) {
            return(1);
        }
    case cheevo_atq200_hard_reward :
        if (%q200_varrock_armour_hard = 1) {
            return(1);
        }
    case cheevo_atq200_elite_reward :
        if (%q200_varrock_armour_elite = 1) {
            return(1);
        }
}
return(0);
