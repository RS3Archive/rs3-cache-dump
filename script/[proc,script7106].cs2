// 7106
[proc,script7106](obj $obj0, int $int1, int $int2, int $int3, boolean $boolean4, int $int5)(boolean)
if ($int1 > 0 & $int1 < 61) {
    if ($int1 = 22 & oc_category($obj0) = construction_house_furniture) {
        $int2 = ~script12588($int2);
    }
    if ($boolean4 = true) {
        if (stat(enum(int, stat, int_to_stat, $int1)) >= $int2 | ~assist_stat_int($int1) = true & $int3 = 0 & ~makex2012_get_assist_level($int5) >= $int2) {
            return(true);
        }
    } else if ($boolean4 = false & (stat_base(enum(int, stat, int_to_stat, $int1)) >= $int2 | ~assist_stat_int($int1) = true & $int3 = 0 & ~makex2012_get_assist_level($int5) >= $int2)) {
        return(true);
    }
    return(false);
}
switch_int ($int1) {
    case 61 :
        if (~script7073($int2) = 2) {
            return(true);
        } else {
            return(false);
        }
    case 62 :
        return(~script7163($int2));
    case 63 :
        return(~int_to_bool(~invent_blueprint_unlocked($int2)));
    case 64 :
        if (%makex2012_interaction_source = $int2) {
            return(true);
        }
        return(false);
    case 65 :
        return(~int_to_bool(~arch_research_get_complete_state(~arch_research_get_data($int2))));
    case 66 :
        return(~int_to_bool(~invent_blueprint_unlocked($int2)));
    case 67 :
        return(~script12592(~script11652($obj0)));
    case default :
        return(true);
}
