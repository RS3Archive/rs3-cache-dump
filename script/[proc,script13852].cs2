// 13852
[proc,script13852](int $int0, int $int1)
def_boolean $boolean2 = ~script6431;
if (%toplevel_v2_legacy_interface = 1) {
    ~script13853;
    return;
} else if ($boolean2 = true) {
    ~script13853;
} else if (~script20475 = false) {
    ~script13853;
    return;
}
if (~script15532(0) = true) {
    ~script13853;
    ~script13854(true, 0, 0, ^setpos_abs_left, ^setpos_abs_top);
    return;
}
def_string $string0 = "Switch layout: Edit Mode";
def_int $int3 = 17;
def_int $int4 = -1;
if (%varclientbit_42113 = 1) {
    if ($boolean2 = false & ~script2384(0, 1) < 0) {
        if_settext("Click on the cog icon to add buttons to this panel.", toplevel_v2_ribbon:error_msg);
    } else {
        if_settext("", toplevel_v2_ribbon:error_msg);
    }
    if ($boolean2 = false) {
        if_sethide(false, toplevel_v2_ribbon:extra_button);
        if_sethide(false, toplevel_v2_ribbon:mode_button);
        if (%league_active_player > 0) {
            if_setgraphic(~script20124, toplevel_v2_ribbon:league_button);
            if_sethide(false, toplevel_v2_ribbon:league_button);
        } else {
            if_sethide(true, toplevel_v2_ribbon:league_button);
        }
    } else {
        if_sethide(true, toplevel_v2_ribbon:extra_button);
        if_sethide(true, toplevel_v2_ribbon:mode_button);
        if_sethide(true, toplevel_v2_ribbon:league_button);
        $int3 = 0;
        $int4 = 0;
    }
    if ($int0 >= $int1) {
        ~script13854(false, 4, 3, ^setpos_abs_right, ^setpos_abs_top);
        if_setposition(0, 0, ^setpos_abs_right, ^setpos_abs_bottom, toplevel_v2_ribbon:mode_button);
        if_setposition(0, 0, ^setpos_abs_right, ^setpos_abs_centre, toplevel_v2_ribbon:extra_button);
        if (%league_active_player > 0 & $int4 = -1) {
            $int4 = max(32, min(48, $int1));
            if_setsize($int4, $int4, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:league_button);
            if_setposition(0, 0, ^setpos_abs_left, ^setpos_abs_centre, toplevel_v2_ribbon:league_button);
        }
        if_setsize(calc(17 + $int4), 0, ^setsize_minus, ^setsize_minus, toplevel_v2_ribbon:window_holder);
        if_setposition($int4, 0, ^setpos_abs_left, ^setpos_abs_centre, toplevel_v2_ribbon:window_holder);
    } else {
        ~script13854(false, 2, 3, ^setpos_abs_right, ^setpos_abs_bottom);
        if_setposition(0, 0, ^setpos_abs_left, ^setpos_abs_bottom, toplevel_v2_ribbon:mode_button);
        if_setposition(2, 0, ^setpos_abs_centre, ^setpos_abs_bottom, toplevel_v2_ribbon:extra_button);
        if (%league_active_player > 0 & $int4 = -1) {
            $int4 = max(32, min(48, $int0));
            if_setsize($int4, $int4, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:league_button);
            if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_top, toplevel_v2_ribbon:league_button);
        }
        if_setsize(0, calc($int3 + $int4), ^setsize_minus, ^setsize_minus, toplevel_v2_ribbon:window_holder);
        if_setposition(0, $int4, ^setpos_abs_centre, ^setpos_abs_top, toplevel_v2_ribbon:window_holder);
    }
    if_setonmouserepeat("script8799($string0, event_com, event_comsubid)", toplevel_v2_ribbon:mode_button);
    return;
} else if ($boolean2 = false) {
    if_sethide(true, toplevel_v2_ribbon:extra_button);
    if_sethide(false, toplevel_v2_ribbon:mode_button);
    if (%league_active_player > 0) {
        if_setgraphic(~script20124, toplevel_v2_ribbon:league_button);
        if_sethide(false, toplevel_v2_ribbon:league_button);
    } else {
        if_sethide(true, toplevel_v2_ribbon:league_button);
    }
} else {
    if_sethide(true, toplevel_v2_ribbon:extra_button);
    if_sethide(true, toplevel_v2_ribbon:mode_button);
    if_sethide(true, toplevel_v2_ribbon:league_button);
}
$string0 = "Switch layout: Default Mode";
if_setonmouserepeat("script8799($string0, event_com, event_comsubid)", toplevel_v2_ribbon:mode_button);
if_sethide(true, toplevel_v2_ribbon:extra_button);
if_settext("", toplevel_v2_ribbon:error_msg);
if (%toplevel_v2_mobile_interface = 1) {
    %varclientbit_42113 = 1;
    ~script13852($int0, $int1);
} else {
    if_setsize(15, 15, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:mode_button);
    if_setsize(15, 15, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:extra_button);
    if ($int0 >= $int1) {
        if_setposition(0, 0, ^setpos_abs_right, ^setpos_abs_bottom, toplevel_v2_ribbon:mode_button);
        if (%league_active_player > 0 & $int4 = -1) {
            $int4 = max(32, min(48, $int1));
            if_setsize($int4, $int4, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:league_button);
            if_setposition(0, 0, ^setpos_abs_left, ^setpos_abs_centre, toplevel_v2_ribbon:league_button);
        }
        if_setsize(calc(17 + $int4), 0, ^setsize_minus, ^setsize_minus, toplevel_v2_ribbon:window_holder);
        if_setposition($int4, 0, ^setpos_abs_left, ^setpos_abs_centre, toplevel_v2_ribbon:window_holder);
        ~script13854(false, 4, 3, ^setpos_abs_right, ^setpos_abs_top);
    } else {
        if_setposition(0, 0, ^setpos_abs_left, ^setpos_abs_bottom, toplevel_v2_ribbon:mode_button);
        if (%league_active_player > 0 & $int4 = -1) {
            $int4 = max(32, min(48, $int0));
            if_setsize($int4, $int4, ^setsize_abs, ^setsize_abs, toplevel_v2_ribbon:league_button);
            if_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_top, toplevel_v2_ribbon:league_button);
        }
        if_setsize(0, calc(17 + $int4), ^setsize_minus, ^setsize_minus, toplevel_v2_ribbon:window_holder);
        if_setposition(0, $int4, ^setpos_abs_centre, ^setpos_abs_top, toplevel_v2_ribbon:window_holder);
        ~script13854(false, 2, 3, ^setpos_abs_right, ^setpos_abs_bottom);
    }
}
