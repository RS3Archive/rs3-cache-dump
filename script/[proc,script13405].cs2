// 13405
[proc,script13405](int $int0, int $int1, int $int2)(string)
def_int $int3 = calc($int0 / 7);
def_int $int4 = calc($int0 % 7);
def_int $int5 = calc($int3 / 52);
if ($int5 > 0) {
    $int3 = calc($int3 % 52);
    return("<tostring($int5)>y <tostring($int3)>w");
} else if ($int3 > 0) {
    return("<tostring($int3)>w <tostring($int4)>d");
} else if ($int0 > 0) {
    if ($int1 > 0) {
        return("<tostring($int0)>d <tostring($int1)>h");
    } else {
        return("<tostring($int0)>d");
    }
} else if ($int1 > 9) {
    return("<tostring($int1)>h");
} else if ($int1 > 0) {
    if ($int2 > 0) {
        return("<tostring($int1)>h <tostring($int2)>m");
    } else {
        return("<tostring($int1)>h");
    }
} else {
    return("<tostring($int2)>m");
}
