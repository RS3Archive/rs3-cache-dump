// 1219
[clientscript,script1219]
def_int $int0 = shop_requestdatastatus;
if ($int0 <= 0) {
    return;
}
if_setontimer(null, free_trial_prompt:base);
if ($int0 = 2) {
    if_sethide(false, free_trial_prompt:error_layer);
    return;
}
if_sethide(false, free_trial_prompt:button);
def_string $string0 = ~script3148(~script17963(2), "1 Month + Trial");
if_settext("Then <$string0>/month recurring billing. Cancel at any time.", free_trial_prompt:start_trial_second_title);
if_sethide(false, free_trial_prompt:start_trial_second_title);
if_settext("Subscribe for <$string0>/month", free_trial_prompt:button_text);
def_string $string1 = "By selecting 'Try 7 days free', you authorise us to charge <$string0> per month, plus taxes, after the introductory offer has ended. Your subscription will renew automatically after the end of the introductory offer (any unused portion of the free trial will be forfeited if you purchase a subscription before the end of the introductory offer). Cancel renewal any time by pressing on the 'How to cancel' button.";
if_settext($string1, free_trial_prompt:tncs_terms_text);
