// 17484
[proc,script17484](dbrow $dbrow0, int $int1)(int)
def_int $int2 = 0;
def_int $int3 = db_getfieldcount($dbrow0, skill_tree_level:col1);
def_int $int4 = -1;
while (++$int4 < $int3) {
    if (~script17485($dbrow0, db_getfield($dbrow0, skill_tree_level:col1, $int4)) = $int1) {
        $int2 = calc($int2 + 1);
    }
}
return($int2);
