// 417
[clientscript,script417](component $component0)
def_int $int1 = if_gettrans($component0);
if (%conq_loading = 1) {
    $int1 = calc($int1 + 2);
    if ($int1 < 255) {
        if_settrans($int1, $component0);
    } else {
        if_sethide(true, $component0);
    }
}
