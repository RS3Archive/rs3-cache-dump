// 19931
[proc,script19931](dbrow $dbrow0)(int)
def_dbrow $dbrow1 = db_getfield($dbrow0, easter_egg_hunt_clue_table:col1, 0);
def_int $int2 = db_filter_find(db_filter_value(easter_egg_hunt_clue_table:col1, $dbrow1, ^filterop_eq, 0), easter_egg_hunt_clue_table:col0, true, -1, 0);
def_int $int3 = -1;
def_dbrow $dbrow4 = null;
while (++$int3 < $int2) {
    $dbrow4 = db_findnext;
    if ($dbrow4 = $dbrow0) {
        return($int3);
    }
}
~error("Unable to find clue number.");
return(0);
