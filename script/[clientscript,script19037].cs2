// 19037
[clientscript,script19037](component $component0, int $int1)
def_int $int2 = 0;
if (cc_find($component0, $int1) = true) {
    $int2 = max(0, calc(cc_gettrans + 3));
    cc_settrans($int2);
    if ($int2 >= 255) {
        cc_delete;
    }
}
