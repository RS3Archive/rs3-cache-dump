// 13748
[proc,script13748](int $int0)
if (if_gethide(bank:default_options_container) = false & if_gethide(bank:share_preset_quick_buttons) = false) {
    ~script13972(bank:side_tab_options, null, rs3tli_modal_tab, "Transfer", 1);
    ~script13972(bank:side_tab_presets, null, rs3tli_modal_tab, "Presets", 1);
} else {
    ~script13972(bank:side_tab_options, null, rs3tli_modal_tab, "Transfer", ~equal_int($int0, 0));
    ~script13972(bank:side_tab_presets, null, rs3tli_modal_tab, "Presets", ~equal_int($int0, 1));
}
