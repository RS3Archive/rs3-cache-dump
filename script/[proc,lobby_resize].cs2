// 4041
[proc,lobby_resize]
~script16923(lobbyscreen:base, lobbyscreen:main_bg);
~lobby_main_background_resize;
~script15628;
if (%lobby_current_tab = 5) {
    ~graphics_options_rebuild(getwindowmode, ~graphics_options_reviewoptions, 2);
}
