// 14495
[proc,script14495](struct $struct0)(int)
def_enum $enum1 = struct_param($struct0, cheevo_meta_list);
if ($enum1 = null) {
    return(0);
}
def_int $int2 = enum_getoutputcount($enum1);
def_dbrow $dbrow3 = null;
def_int $int4 = 0;
def_int $int5 = -1;
while (++$int5 < $int2) {
    $dbrow3 = enum(int, dbrow, $enum1, $int5);
    if (~arch_research_get_complete_state($dbrow3) = 1) {
        $int4 = setbit($int4, $int5);
    }
}
return($int4);
