// 4577
[proc,script4577](dbrow $dbrow0, int $int1, int $int2)(int)
switch_dbrow ($dbrow0) {
    case trh_promo_categories_trh163 :
        if (%trh163_promo_bonus_active = 1) {
            if ($int1 = 8) {
                return(0);
            }
            if ($int1 = 10) {
                return(5960);
            }
        }
    case trh_promo_categories_trh185 :
        $int2 = ~script16649($int1, $int2);
}
return($int2);
