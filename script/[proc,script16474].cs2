// 16474
[proc,script16474](int $int0)(boolean)
switch_int ($int0) {
    case 1 :
        return(keyheld_ctrl);
    case 2 :
        return(keyheld_alt);
    case 4 :
        return(keyheld_shift);
    case 8 :
        if (keyheld_ctrl = true) {
            return(false);
        }
        if (keyheld_alt = true) {
            return(false);
        }
        if (keyheld_shift = true) {
            return(false);
        }
}
return(true);
