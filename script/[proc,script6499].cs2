// 6499
[proc,script6499](int $int0)
def_int $int1 = calc(1 + enum_getoutputcount(comscenario_scenarios));
cc_deleteall(comscenario:description);
cc_deleteall(comscenario:type);
cc_deleteall(comscenario:difficulty);
cc_deleteall(comscenario:panel);
cc_deleteall(comscenario:panel_click);
def_int $int2 = ~script11225($int0, $int1);
~script11220($int2);
def_int $int3 = 0;
def_struct $struct4 = null;
def_int $int5 = 0;
while ($int3 < $int1) {
    $int5 = enum(int, int, comscenario_scenarios_autoint, $int3);
    $struct4 = enum(int, struct, comscenario_scenarios, $int5);
    ~comscenario_create_description($struct4, $int3, $int0);
    ~comscenario_create_information($struct4, $int3, $int0);
    ~comscenario_create_button($struct4, $int3, $int0);
    $int3 = calc($int3 + 1);
}
if ($int2 = 0) {
    if_sethide(false, comscenario:coming_soon);
} else {
    if_sethide(true, comscenario:coming_soon);
}
~comscenario_sort(comscenario:description, comscenario:type, comscenario:difficulty, comscenario:panel_click);
if_setscrollsize(0, calc($int2 * 40), comscenario:scenarios);
~rs3_scrollbar_vertical(comscenario:scrollbar, comscenario:scenarios);
