// 20780
[clientscript,script20780](component $component0)
if (~script16474(2) = true) {
    return;
}
if (~script6431 = true) {
    if (%construction_house_mousespace_room_added_from_door = 1) {
        if (~script20783(%construction_house_mousespace_locked_coord) = true) {
            ~script20779(%construction_house_mousespace_locked_coord);
            ~script20778(0);
        }
        return;
    }
    if (~script20783(%construction_house_mousespace_current_coord) = false) {
        return;
    }
    %construction_house_mousespace_locked_coord = %construction_house_mousespace_current_coord;
    ~script20779(%construction_house_mousespace_locked_coord);
    ~script20778(0);
} else {
    ~script20782;
}
