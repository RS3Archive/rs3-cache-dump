// 1567
[proc,script1567](boolean $boolean0)
if (~is_mobile_client = false) {
    $boolean0 = true;
}
switch_toplevelinterface (if_gettop) {
    case toplevel_v2 :
        if_sethide($boolean0, toplevel_v2:softkeyboard_close_layer);
    case lobbyscreen :
        if_sethide($boolean0, lobbyscreen:softkeyboard_close_layer);
    case loginscreen :
        if_sethide($boolean0, loginscreen:softkeyboard_close_layer);
}
