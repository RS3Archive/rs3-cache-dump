// 9095
[proc,script9095](struct $struct0, boolean $boolean1)(int)
def_int $int2 = struct_param($struct0, rewardshop_cost);
if (struct_param($struct0, rewardshop_cost_override) = true) {
    $int2 = ~script5352($struct0, $int2);
}
if ($boolean1 = true) {
    if (struct_param($struct0, rewardshop_cost_reduced) ! -1) {
        $int2 = struct_param($struct0, rewardshop_cost_reduced);
    } else {
        $int2 = ~script12719($struct0, $int2);
    }
}
if (~script5353(%rewardshop_struct) = 1 & ~script1282 = 5) {
    $int2 = addpercent($int2, -7);
}
return($int2);
