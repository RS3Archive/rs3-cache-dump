// 20315
[clientscript,script20315](dbrow $dbrow0, int $int1)
def_achievement $achievement2 = db_getfield($dbrow0, league_task:col2, 0);
if ($achievement2 = null) {
    cc_deleteall(league_child_tasks:tasks_progress_popup_contents);
    if_settrans(255, league_child_tasks:tasks_progress_popup_bg);
    if_setontimer(null, league_child_tasks:tasks_progress_popup);
    return;
}
def_graphic $graphic3 = ~script20307($dbrow0, 0);
def_string $string0 = ~script20131($dbrow0, $achievement2);
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = if_getwidth(league_child_tasks:tasks_progress_popup_contents);
if (if_find(league_child_tasks:tasks_progress_popup_contents) = true) {
    $int4, $int5 = ~script19613(0, $int4, $int5, $int6, "Task progressed:");
    $int4, $int5 = ~script19615(0, $int4, calc($int5 - 4), $int6, $string0);
}
if_setgraphic($graphic3, league_child_tasks:tasks_progress_popup_icon);
if_setsize(0, max(40, calc($int5 + 8)), ^setsize_minus, ^setsize_abs, league_child_tasks:tasks_progress_popup);
def_int $int7 = calc(clientclock - $int1);
def_int $int8 = 0;
if ($int7 <= 50) {
    $int8 = max(0, calc(255 - scale(255, 50, $int7)));
    if_settrans($int8, league_child_tasks:tasks_progress_popup_bg);
    if_settrans($int8, league_child_tasks:tasks_progress_popup_bg_outline);
    if_settrans($int8, league_child_tasks:tasks_progress_popup_icon);
    ~script4161(league_child_tasks:tasks_progress_popup_contents, $int8);
} else if ($int7 >= 300) {
    $int7 = calc($int7 - 300);
    $int8 = min(255, scale(255, 50, $int7));
    if_settrans($int8, league_child_tasks:tasks_progress_popup_bg);
    if_settrans($int8, league_child_tasks:tasks_progress_popup_bg_outline);
    if_settrans($int8, league_child_tasks:tasks_progress_popup_icon);
    ~script4161(league_child_tasks:tasks_progress_popup_contents, $int8);
    if ($int8 >= 255) {
        if_setontimer(null, league_child_tasks:tasks_progress_popup);
    }
}
