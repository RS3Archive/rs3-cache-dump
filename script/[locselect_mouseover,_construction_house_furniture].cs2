// 20787
[locselect_mouseover,_construction_house_furniture](coord $coord0)
if (%construction_house_mousespace_locked_coord ! null & ~script6431 = true & ~script20783(%construction_house_mousespace_locked_coord) = true) {
    ~script20779(%construction_house_mousespace_locked_coord);
} else if (%construction_house_mousespace_room_added_from_door = 1) {
    ~script20779(%construction_house_mousespace_locked_coord);
} else {
    %construction_house_mousespace_current_coord = $coord0;
}
locselect_findreset;
while (locselect_findnext = true) {
    loc_setalpha(127);
    loc_settint(0, 100, 100, 127);
}
