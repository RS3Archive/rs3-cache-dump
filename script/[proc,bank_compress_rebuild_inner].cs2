// 14359
[proc,bank_compress_rebuild_inner]
def_int $int0 = 0;
def_int $int1 = %bank_first_freeslot;
def_obj $obj2 = null;
def_int $int3 = 0;
def_string $string0 = ~bank_op_prefix;
def_string $string1 = ~script13685;
while ($int1 < 1820) {
    if (cc_find(bank:bank_inv, $int1) = true & cc_getinvobject = bank_empty_slot) {
        $int0, $obj2, $int3 = ~script14360(max($int0, calc($int1 + 1)));
        if (cc_find(bank:bank_inv, $int1) = true) {
            ~script13798($string0, $string1, $obj2, $int3);
        }
        if (cc_find(bank:bank_inv, $int0) = true) {
            ~script13798($string0, $string1, bank_empty_slot, $int3);
        }
    }
    $int1 = calc($int1 + 1);
}
