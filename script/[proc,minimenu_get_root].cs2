// 83
[proc,minimenu_get_root]()(component)
switch_toplevelinterface (if_gettop) {
    case default :
        return(toplevel_v2:minimenu_layer);
    case lobbyscreen :
        return(lobbyscreen:minimenu_layer);
    case loginscreen :
        return(loginscreen:minimenu_layer);
}
