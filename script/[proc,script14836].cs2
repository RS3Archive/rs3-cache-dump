// 14836
[proc,script14836](string $string0, int $int0)
if ($int0 <= clientclock) {
    if (compare(%p_dialog_input, $string0) ! 0) {
        ~script14829(%p_dialog_input);
        $string0 = %p_dialog_input;
    }
    $int0 = calc(clientclock + 25);
    if_setontimer("script14835($string0, $int0)", arch_journal:restoration_filter_search_layer);
}
