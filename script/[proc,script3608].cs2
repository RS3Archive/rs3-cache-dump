// 3608
[proc,script3608]()(dbrow)
return(null);
def_int $int0 = 0;
def_int $int1 = 0;
if (%runepass_cached_dbrow ! null) {
    $int0, $int1 = db_getfield(%runepass_cached_dbrow, runepass_table:col2, 0);
    if (date_runeday <= $int1) {
        return(%runepass_cached_dbrow);
    } else if (date_runeday <= db_getfield(%runepass_cached_dbrow, runepass_table:col3, 0)) {
        return(%runepass_cached_dbrow);
    }
}
return(null);
