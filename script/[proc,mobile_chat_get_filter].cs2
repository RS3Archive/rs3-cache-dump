// 15882
[proc,mobile_chat_get_filter](int $int0)(struct)
def_int $int1 = enum_getoutputcount(mobile_chat_filter_list);
if ($int0 < 0 | $int0 > $int1) {
    ~error("$filter_id = <tostring($int0)> was out of range. | $max=<tostring($int1)>");
    return(null);
}
def_int $intarray0($int1);
def_int $int2 = ~script753(intarray0, $int1, %mobile_chat_settings_channel_id);
if ($int2 = 0) {
    ~error("No filters where found for Channel <tostring(%mobile_chat_settings_channel_id)> (<enum(int, string, mobile_chat_overlay_channel_to_string, %mobile_chat_settings_channel_id)>)");
    return(null);
}
def_struct $struct3 = enum(int, struct, mobile_chat_filter_list, $intarray0($int0));
if ($struct3 = null) {
    ~error("Unable to find a struct from enum. Channel ID: <tostring(%mobile_chat_settings_channel_id)> | Array consists of <tostring($int2)> elements | looking for ID: <tostring($int0)>");
    return(null);
}
return($struct3);
