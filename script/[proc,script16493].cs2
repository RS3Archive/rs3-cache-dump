// 16493
[proc,script16493]
if (~script830 = false) {
    if (if_gethide(wheel_of_fortune:play_again_layer) = true) {
        if_sethide(false, wheel_of_fortune:ticker_tape_viewport);
    }
} else {
    if_sethide(true, wheel_of_fortune:ticker_tape_viewport);
}
