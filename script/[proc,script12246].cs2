// 12246
[proc,script12246](int $int0)(int)
if (++%prayer_oldest_temporary >= 20) {
    %prayer_oldest_temporary = calc(%prayer_oldest_temporary % 20);
}
~script12247(%prayer_oldest_temporary, $int0);
return(%prayer_oldest_temporary);
