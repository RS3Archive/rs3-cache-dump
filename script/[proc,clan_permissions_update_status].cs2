// 5135
[proc,clan_permissions_update_status]
if (~script5131 = 1) {
    if (%clan_permissions_chat_talk_varc = 1) {
        if_sethide(false, clansettings:cantalk_button_selected_layer);
    } else {
        if_sethide(true, clansettings:cantalk_button_selected_layer);
    }
    if (%clan_permissions_chat_kick_varc = 1) {
        if_sethide(false, clansettings:cankick_button_selected_layer);
    } else {
        if_sethide(true, clansettings:cankick_button_selected_layer);
    }
    if (%clan_permissions_lockkeep = 1) {
        if_sethide(false, clansettings:lockkeep_button_selected_layer);
    } else {
        if_sethide(true, clansettings:lockkeep_button_selected_layer);
    }
    if (%clan_permissions_lockwalls = 1) {
        if_sethide(false, clansettings:lockwalls_button_selected_layer);
    } else {
        if_sethide(true, clansettings:lockwalls_button_selected_layer);
    }
    if (%clan_permissions_accesskeep = 1) {
        if_sethide(false, clansettings:accesskeep_button_selected_layer);
    } else {
        if_sethide(true, clansettings:accesskeep_button_selected_layer);
    }
    if (%clan_permissions_accesswalls = 1) {
        if_sethide(false, clansettings:accesswalls_button_selected_layer);
    } else {
        if_sethide(true, clansettings:accesswalls_button_selected_layer);
    }
    if (%clan_permissions_recruit = 1) {
        if_sethide(false, clansettings:recruit_button_selected_layer);
    } else {
        if_sethide(true, clansettings:recruit_button_selected_layer);
    }
    if (%clan_permissions_startbattle = 1) {
        if_sethide(false, clansettings:startbattle_button_selected_layer);
    } else {
        if_sethide(true, clansettings:startbattle_button_selected_layer);
    }
    if (%clan_permissions_leadrcw = 1) {
        if_sethide(false, clansettings:leadrcw_button_selected_layer);
    } else {
        if_sethide(true, clansettings:leadrcw_button_selected_layer);
    }
    if (%clan_permissions_startvote = 1) {
        if_sethide(false, clansettings:startvote_button_selected_layer);
    } else {
        if_sethide(true, clansettings:startvote_button_selected_layer);
    }
    if (%clan_permissions_startmeeting = 1) {
        if_sethide(false, clansettings:startmeeting_button_selected_layer);
    } else {
        if_sethide(true, clansettings:startmeeting_button_selected_layer);
    }
    if (%clan_permissions_partytech = 1) {
        if_sethide(false, clansettings:partytech_button_selected_layer);
    } else {
        if_sethide(true, clansettings:partytech_button_selected_layer);
    }
    if (%clan_permissions_theatretech = 1) {
        if_sethide(false, clansettings:theatretech_button_selected_layer);
    } else {
        if_sethide(true, clansettings:theatretech_button_selected_layer);
    }
    if (%clan_permissions_addnotice = 1) {
        if_sethide(false, clansettings:addnotice_button_selected_layer);
    } else {
        if_sethide(true, clansettings:addnotice_button_selected_layer);
    }
    if (%clan_permissions_addsign = 1) {
        if_sethide(false, clansettings:addsign_button_selected_layer);
    } else {
        if_sethide(true, clansettings:addsign_button_selected_layer);
    }
    if (%clan_permissions_editfield = 1) {
        if_sethide(false, clansettings:editfield_button_selected_layer);
    } else {
        if_sethide(true, clansettings:editfield_button_selected_layer);
    }
    if (%clan_permissions_upgrade = 1) {
        if_sethide(false, clansettings:canupgrade_button_selected_layer);
    } else {
        if_sethide(true, clansettings:canupgrade_button_selected_layer);
    }
    if (%clan_permissions_downgrade = 1) {
        if_sethide(false, clansettings:candowngrade_button_selected_layer);
    } else {
        if_sethide(true, clansettings:candowngrade_button_selected_layer);
    }
    if (%clan_permissions_lockskillplots_varc = 1) {
        if_sethide(false, clansettings:canlockplots_button_selected_layer);
    } else {
        if_sethide(true, clansettings:canlockplots_button_selected_layer);
    }
    if (%clan_permissions_can_check_resources_varc = 1) {
        if_sethide(false, clansettings:cancheckresources_button_selected_layer);
    } else {
        if_sethide(true, clansettings:cancheckresources_button_selected_layer);
    }
    if (%clan_permissions_gather = 1) {
        if_sethide(false, clansettings:cangather_button_selected_layer);
    } else {
        if_sethide(true, clansettings:cangather_button_selected_layer);
    }
    if (%clan_permissions_language = 1) {
        if_sethide(false, clansettings:language_button_selected_layer);
    } else {
        if_sethide(true, clansettings:language_button_selected_layer);
    }
    if (%clan_permissions_removehabitat_varc = 1) {
        if_sethide(false, clansettings:removehabitat_button_selected_layer);
    } else {
        if_sethide(true, clansettings:removehabitat_button_selected_layer);
    }
    if (%clan_permissions_customiseavatar_varc = 1) {
        if_sethide(false, clansettings:customiseavatar_button_selected_layer);
    } else {
        if_sethide(true, clansettings:customiseavatar_button_selected_layer);
    }
    if (%clan_permissions_fillbuffslot_varc = 1) {
        if_sethide(false, clansettings:fillbuffslot_button_selected_layer);
    } else {
        if_sethide(true, clansettings:fillbuffslot_button_selected_layer);
    }
    if (%clan_permissions_settick = 1) {
        if_sethide(false, clansettings:settick_button_selected_layer);
    } else {
        if_sethide(true, clansettings:settick_button_selected_layer);
    }
    if (%clan_permissions_broadcastevent_varc = 1) {
        if_sethide(false, clansettings:broadcastevent_button_selected_layer);
    } else {
        if_sethide(true, clansettings:broadcastevent_button_selected_layer);
    }
    if (%clan_permissions_canchangebroadcastsettings_varc = 1) {
        if_sethide(false, clansettings:changebroadcastsettings_button_selected_layer);
    } else {
        if_sethide(true, clansettings:changebroadcastsettings_button_selected_layer);
    }
    if (%clan_permissions_cankick_varc = 1) {
        if_sethide(false, clansettings:kickmember_button_selected_layer);
    } else {
        if_sethide(true, clansettings:kickmember_button_selected_layer);
    }
}
