// 13174
[proc,script13174](int $int0, int $int1)
def_int $int2 = calc($int1 - $int0);
def_int $int3 = -1;
if ($int2 = 0) {
    return;
}
def_int $int4 = calc($int2 / abs($int2));
if (if_find(mtxmgt_store:items_layer) = true & .if_find(mtxmgt_store:ragdolls_scrollsize_layer) = true) {
    $int3 = cc_param(mtxmgt_first_visible_index);
    if ($int4 > 0) {
        $int2 = min($int2, calc(.cc_getscrollwidth - 8));
    } else {
        $int2 = max($int2, calc(0 - $int3));
    }
    $int3 = calc($int3 + $int2);
    cc_setparam(mtxmgt_first_visible_index, $int3);
    ~script13177;
}
