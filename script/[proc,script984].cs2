// 984
[proc,script984](struct $struct0, int $int1)
def_enum $enum2 = struct_param($struct0, rsraffle_prize_list);
def_struct $struct3 = enum(int, struct, $enum2, $int1);
if_settext("0", rsraffle:ticket_input_display);
if_settext("How many tickets would you like to enter into the lucky dip?", rsraffle:enter_description);
if_settext("Personal Entries: <tostring_localised(~script16027(%rsraffle_selected), true)>", rsraffle:enter_entries);
~script13969(rsraffle:enter_panel_title_background, null, rs3tli_subtitle_holder_basic_background_redgold, struct_param($struct3, rsraffle_prize_name));
~script13971(rsraffle:confirm_button_build, rsraffle:confirm_button_click, rs3tli_positive_button, "Confirm", true);
~script13969(rsraffle:return_button_build, rsraffle:return_button_click, rs3tli_neutral_button, "Cancel");
