// 15775
[proc,script15775](dbrow $dbrow0)(string)
def_int $int1 = 0;
def_int $int2 = db_getfieldcount($dbrow0, requirements:col2);
def_stat $stat3 = null;
def_int $int4 = 0;
def_string $string0 = "";
while ($int1 < $int2) {
    $stat3, $int4 = db_getfield($dbrow0, requirements:col2, $int1);
    if (stat_base($stat3) < $int4) {
        $string0 = ~script17238($string0, "Base level <tostring($int4)> <enum(stat, string, stat_to_string, $stat3)>");
    }
    $int1 = calc($int1 + 1);
}
return($string0);
