// 877
[proc,lorepoints_restore](int $int0, int $int1)
def_int $int2 = ~script859;
def_int $int3 = scale($int1, 100, $int2);
if (%lore_points < $int2) {
    %lore_points = min($int2, calc(%lore_points + $int0 + $int3));
} else {
    %lore_points = max($int2, calc(%lore_points - $int0 - $int3));
}
