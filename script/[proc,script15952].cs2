// 15952
[proc,script15952]
if (~script6431 = true) {
    %zoom = max(min(%zoom, 7500), 3000);
} else {
    %zoom = max(min(%zoom, 7500), 3000);
}
~script8776;
cam2_setpositionangularinterpolation(300);
