// 13131
[clientscript,script13131](component $component0, component $component1, int $int2, int $int3)
def_int $int4 = 0;
def_int $int5 = 0;
if (if_find($component0) = true) {
    $int4 = cc_param(rs3tli_scrollbar_button_step);
    $int5 = cc_param(rs3tli_scrollbar_button_cooldown);
}
if ($int5 > 0 & cc_find($component0, $int3) = true) {
    if (calc(clientclock - cc_param(rs3tli_scrollbar_last_click)) < $int5) {
        return;
    } else {
        cc_setparam(rs3tli_scrollbar_last_click, clientclock);
    }
}
if ($int2 = 1) {
    $int4 = ~invert($int4);
}
def_int $int6 = calc(if_getscrollx($component1) + $int4);
if (cc_find($component0, 1) = true) {
    ~scrollbar_horizontal_doscroll($component0, $component1, $int6, 1);
}
