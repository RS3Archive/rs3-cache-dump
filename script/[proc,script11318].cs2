// 11318
[proc,script11318](struct $struct0)(int)
def_int $int1 = 1;
def_int $int2 = calc(~script11073($struct0) - clientclock);
if (calc($int2 / 50 % 2) = 1) {
    $int1 = 0;
}
def_int $int3 = 0;
if ($int1 = 1) {
    $int3 = min(112, max(0, calc(112 - scale(calc($int2 % 50), 50, 112))));
} else {
    $int3 = min(112, max(0, scale(calc($int2 % 50), 50, 112)));
}
return($int3);
