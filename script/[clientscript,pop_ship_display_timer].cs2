// 7281
[clientscript,pop_ship_display_timer]
if (++%pop_voyage_list_timer_tick > 3000) {
    %pop_voyage_list_timer_started = calc(%pop_voyage_list_timer_started + 1);
    %pop_voyage_list_timer_tick = 0;
}
~pop_ship_display_timer;
