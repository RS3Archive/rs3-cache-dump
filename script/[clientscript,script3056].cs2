// 3056
[clientscript,script3056](int $int0, string $string0, string $string1, int $int1)
switch_int ($int0) {
    case 1 :
        if (friend_test(removetags($string1)) = false) {
            friend_add(removetags($string1));
        }
    case 2 :
        if (friend_test(removetags($string1)) = false) {
            ignore_add(removetags($string1));
        }
    case 3 :
        ~lobbyscreen_input("Send message to <$string0>", "", 0, $string1, "");
    case 5 :
        %lobbyscreen_report_abuse_name = removetags($string0);
        ~script3190;
    case 6 :
        ~script15232(1, $int1);
}
