// 232
[clientscript,script232](int $int0)
def_int $int1 = calc(0 - 1);
def_dbrow $dbrow2 = null;
while (++$int1 <= 3) {
    db_find(minigame_ability:col0, $int1);
    $dbrow2 = db_findnext;
    if ($dbrow2 ! null) {
        if (~script16811($dbrow2) >= 0 | ~script16812($dbrow2) = false) {
            ~script16808($dbrow2, false);
            ~script16814($dbrow2);
        } else {
            ~script16808($dbrow2, true);
        }
    }
}
