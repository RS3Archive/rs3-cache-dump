// 12713
[clientscript,script12713](component $component0)
def_int $int1 = 0;
def_int $int2 = 1;
def_string $string0 = "";
$int1, $int2 = ~script12711;
if_settext("<tostring($int1)>%", trh54_guaranteed_quality:purple_gem_percentage);
if_setparam_int(rs3tli_component_target_height, $int2, trh54_guaranteed_quality:purple_gem_charge_layer);
if (%trh54_super_charge = 500) {
    if_sethide(true, trh54_guaranteed_quality:purple_gem_percentage);
    if (if_find(trh54_guaranteed_quality:purple_gem_charge) = true) {
        cc_setparam(rs3tli_component_rotation_type, 4);
        cc_setparam(rs3tli_component_sound_id, 1);
        cc_setparam(rs3tli_component_sound_event, 0);
    }
    if (if_find(trh54_guaranteed_quality:purple_gem) = true) {
        cc_setparam(rs3tli_component_rotation_type, 4);
    }
    if (if_find(trh54_guaranteed_quality:purple_gem_charge_glow) = true) {
        cc_setparam(rs3tli_component_rotation_type, 4);
        cc_setparam(rs3tli_component_transparency_type, 1);
        cc_setparam(rs3tli_component_transparency_min, 0);
        cc_setparam(rs3tli_component_transparency_max, 100);
    }
} else {
    if_sethide(false, trh54_guaranteed_quality:purple_gem_percentage);
    if (if_find(trh54_guaranteed_quality:purple_gem_charge) = true) {
        cc_setparam(rs3tli_component_rotation_type, 1);
        cc_setparam(rs3tli_component_sound_id, -1);
    }
    if (if_find(trh54_guaranteed_quality:purple_gem) = true) {
        cc_setparam(rs3tli_component_rotation_type, 1);
    }
    if (if_find(trh54_guaranteed_quality:purple_gem_charge_glow) = true) {
        cc_setparam(rs3tli_component_rotation_type, 1);
        cc_setparam(rs3tli_component_transparency_type, -1);
    }
}
