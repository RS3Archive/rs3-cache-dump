// 12938
[proc,script12938](int $int0)(struct)
def_dbrow $dbrow1 = enum(int, dbrow, event_weekends_rows, calc($int0 - 1));
if ($dbrow1 ! null) {
    return(enum(int, struct, event_weekend_dateicon, db_getfield($dbrow1, event_weekend:col1, 0)));
}
return(null);
