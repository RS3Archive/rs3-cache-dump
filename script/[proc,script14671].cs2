// 14671
[proc,script14671](enum $enum0)(int)
def_int $int1 = enum_getoutputcount($enum0);
def_dbrow $dbrow2 = null;
def_int $int3 = -1;
while (++$int3 < $int1) {
    $dbrow2 = enum(int, dbrow, $enum0, $int3);
    if (~script14669($dbrow2) = 1) {
        return($int3);
    }
}
return(-1);
