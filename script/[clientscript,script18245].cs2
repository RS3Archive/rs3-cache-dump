// 18245
[clientscript,script18245]
def_enum $enum0 = ~script18158(%omge_job_interface_skill);
if ($enum0 = null) {
    return;
}
cc_deleteall(jobs_main:button_graphics);
cc_deleteall(jobs_main:button_icons);
cc_deleteall(jobs_main:button_stamps);
cc_deleteall(jobs_main:button_labels);
cc_deleteall(jobs_main:button_clickboxes);
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = enum_getoutputcount($enum0);
def_struct $struct4 = enum(int, struct, $enum0, 1);
def_int $int5 = 0;
while ($int1 < $int3) {
    ~script18240($int1);
    $int1 = calc(1 + $int1);
}
$int1 = 0;
def_int $int6 = 0;
while ($int5 = 0) {
    $int1 = calc(struct_param($struct4, task_unique_id) - 1);
    $int6 = 0;
    if (~script15771(%omge_job_interface_skill, struct_param($struct4, task_unique_id)) = 1) {
        $int6 = 1;
    }
    if ($int6 = 1) {
        ~script18242($struct4, $int1, $int2);
        $int2 = calc($int2 + 1);
    } else {
        if (cc_find(jobs_main:button_graphics, $int1) = true) {
            cc_sethide(true);
        }
        if (cc_find(jobs_main:button_icons, $int1) = true) {
            cc_sethide(true);
        }
        if (cc_find(jobs_main:button_labels, $int1) = true) {
            cc_sethide(true);
        }
        if (cc_find(jobs_main:button_stamps, $int1) = true) {
            cc_sethide(true);
        }
        if (cc_find(jobs_main:button_clickboxes, $int1) = true) {
            cc_sethide(true);
        }
    }
    if (struct_param($struct4, task_next_id) ! 0) {
        $struct4 = enum(int, struct, $enum0, struct_param($struct4, task_next_id));
    } else {
        $int5 = 1;
    }
}
