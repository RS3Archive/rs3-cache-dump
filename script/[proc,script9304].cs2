// 9304
[proc,script9304](struct $struct0)(dbrow)
def_dbrow $dbrow1 = null;
def_dbrow $dbrow2 = null;
def_enum $enum3 = null;
if ($struct0 ! null) {
    $enum3 = struct_param($struct0, trh_droptable_enum);
    $dbrow2 = struct_param($struct0, trh_droptable_f2p);
    if ($enum3 ! null) {
        $dbrow1 = ~script18540($enum3);
    } else if ($dbrow2 ! null & playermember = false) {
        $dbrow1 = $dbrow2;
    } else {
        $dbrow1 = struct_param($struct0, trh_droptable);
    }
}
$dbrow1 = ~script9825($struct0, $dbrow1);
return($dbrow1);
