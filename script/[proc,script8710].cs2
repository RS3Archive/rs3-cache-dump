// 8710
[proc,script8710](int $int0, int $int1, int $int2)(int)
def_int $int3 = 10;
if ($int0 <= 16) {
    return(0);
} else if (calc($int0 + $int1) >= calc($int2 - 16)) {
    return(4095);
} else {
    $int0 = calc($int0 * $int3);
    $int1 = calc($int1 * $int3);
    $int2 = calc($int2 * $int3);
    $int2 = calc($int2 / 2);
    $int0 = calc($int0 + $int1 / 2 - $int2);
    $int0 = interpolate(calc(0 * $int3), calc(4095 * $int3), calc(0 - $int2), $int2, $int0);
    if (calc($int0 % $int3) > 0) {
        return(max(0, min(calc($int0 / $int3 + 1), 4095)));
    }
    return(max(0, min(calc($int0 / $int3), 4095)));
}
