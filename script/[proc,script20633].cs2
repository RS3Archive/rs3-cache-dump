// 20633
[proc,script20633](int $int0)
cc_deleteall(marketplace_store:items_holder);
cc_deleteall(marketplace_store:scrollbar_layer);
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 0;
def_int $int5 = 0;
if (string_length(%marketplace_store_last_search) > 0) {
    $int1 = calc($int1 + 25);
}
def_int $int6 = db_filter_find(~script20644($int0, true), marketplace_store_item:col0, false, -1, 0);
if ($int6 > 0) {
    $int5 = $int1;
    $int1 = calc($int1 + 20);
    if ($int0 ! 0) {
        $int2 = ~script20635($int1);
        $int3 = ~script20642($int2);
        $int1 = ~script20637($int1, $int3, $int2);
    } else {
        $int3, $int1 = ~script20639($int1);
    }
    if ($int3 > 0) {
        ~script20641("Featured", $int5);
    } else {
        $int1 = $int5;
    }
    $int4 = calc($int4 + $int3);
}
def_int $int7 = db_filter_find(~script20644($int0, false), marketplace_store_item:col0, false, -1, 0);
if ($int7 > 0) {
    if ($int0 ! 0) {
        $int5 = $int1;
        $int1 = calc($int1 + 20);
        $int2 = ~script20635($int1);
        $int3 = ~script20642($int2);
        $int1 = ~script20637($int1, $int3, $int2);
        if ($int3 > 0) {
            ~script20641(~script20651($int0), $int5);
        } else {
            $int1 = $int5;
        }
    } else {
        $int1, $int3 = ~script20640($int1);
    }
    $int4 = calc($int4 + $int3);
}
if (string_length(%marketplace_store_last_search) > 0) {
    ~cc_add_text(marketplace_store:items_holder, if_getnextsubid(marketplace_store:items_holder), 11, 0, ^setpos_abs_left, ^setpos_abs_top, 22, 20, ^setsize_minus, ^setsize_abs, text_body_large, "Displaying <tostring($int4)> <~text_plural($int4, "result", "results")> for '<%marketplace_store_last_search>'.");
}
~script20634($int1);
