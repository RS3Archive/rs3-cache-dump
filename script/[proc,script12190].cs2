// 12190
[proc,script12190]
def_int $int0 = 0;
def_int $int1 = 0;
while ($int1 < 9) {
    if (cc_find(invent_gizmo:work_layer, $int1) = true & cc_getgraphic ! null) {
        $int0 = calc($int0 + 1);
    }
    $int1 = calc($int1 + 1);
}
def_string $string0 = null;
def_int $int2 = ~invent_gizmo_slot_count(%invent_gizmo_item_client);
if ($int0 > $int2) {
    switch_int ($int2) {
        case 5 :
            $string0 = "You can only fit up to five materials into the gizmo.";
        case 9 :
            $string0 = "You can only fit up to nine materials into the gizmo.";
        case default :
            $string0 = "You can only fit so many materials into the gizmo.";
    }
    if_sethide(false, invent_gizmo:create_button_disabled_layer);
    if_setonmouserepeat("script3876($string0, event_com, -1)", invent_gizmo:create_button_disabled_layer);
} else if ($int0 = 0) {
    if_sethide(false, invent_gizmo:create_button_disabled_layer);
    $string0 = "You must have at least one material in the gizmo.";
    if_setonmouserepeat("script3876($string0, event_com, -1)", invent_gizmo:create_button_disabled_layer);
} else if (~script12191(%invent_gizmo_item_client) = 0) {
    if_sethide(false, invent_gizmo:create_button_disabled_layer);
    $string0 = "You do not have enough materials in stock to make this gizmo.";
    if_setonmouserepeat("script3876($string0, event_com, -1)", invent_gizmo:create_button_disabled_layer);
} else {
    if_sethide(true, invent_gizmo:create_button_disabled_layer);
}
def_int $int3 = 0;
def_component $component4 = invent_gizmo:predict_layer;
if ($int0 = 0) {
    cc_deleteall($component4);
    $int3, $int3 = ~script12086($component4, 0, 5, 5, calc(if_getwidth($component4) - 10), "Drag or click on a material from the list to add it to the gizmo above.<br><br>You can put up to <tostring($int2)> materials into a gizmo, and you can put the same material in several times.<br><br>Common materials are used in stacks of five per slot.<br><br>Higher Invention skill will help you get higher rank positive perks.", rs3tli_default_font_dynamic);
    return;
}
$int1 = 0;
while (cc_find(invent_gizmo:predict_layer, $int1) = true) {
    cc_setcolour(0x808080);
    $int1 = calc($int1 + 1);
}
