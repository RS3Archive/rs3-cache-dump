// 2772
[clientscript,script2772](component $component0, int $int1)
def_int $int2 = calc(spline_length(0) - 2);
if (%tutorial3_cutscene_tracker ! 2 | $int1 > $int2) {
    if_setoncamfinished(null, $component0);
    return;
}
switch_int (randominc(2)) {
    case 0 :
        sound_synth(demon_footstep1, 1, 0);
    case 1 :
        sound_synth(demon_footstep2, 1, 0);
    case 2 :
        sound_synth(demon_footstep3, 1, 0);
}
cam_movealong(0, $int1, 2500, 2400, 1, $int1);
if ($int1 < $int2) {
    if_setoncamfinished("script2772($component0, calc($int1 + 1))", $component0);
}
