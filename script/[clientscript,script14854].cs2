// 14854
[clientscript,script14854](int $int0, int $int1)
def_dbrow $dbrow2 = ~script19772(%arch_relic_management_loadout_selected, $int1);
switch_int ($int0) {
    case 1 :
        ~script14852($int1);
        if (enum_getreversecount(dbrow, arch_relic_list, $dbrow2) > 0) {
            ~script14856(enum_getreverseindex(dbrow, int, arch_relic_list, $dbrow2, 0));
        }
}
