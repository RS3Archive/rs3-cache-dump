// 15927
[proc,script15927](int $int0)
def_int $int1 = calc((30 + 2) * 19);
def_int $int2 = calc((30 + 2) * (%share_preset_last_preset - 1));
if_setscrollsize(0, $int1, bank:preset_list);
if ($int0 = 1) {
    if_setscrollpos(0, $int2, bank:preset_list);
}
if_setsize(2, $int1, ^setsize_minus, ^setsize_abs, bank:preset_highlight_container);
~rs3_scrollbar_vertical(bank:preset_list_scrollbar, bank:preset_list);
