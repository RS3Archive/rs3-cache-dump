// 4505
[clientscript,script4505](int $int0, int $int1)
def_int $int2 = -1;
if ($int1 = 0 | $int1 = 2) {
    while (cc_find(igc_main:calendar_layer, ++$int2) = true) {
        if (cc_param(clock_events_id) = $int0) {
            cc_setgraphic(null);
            cc_setsize(0, 0, ^setsize_abs, ^setsize_abs);
        }
    }
    $int2 = -1;
}
