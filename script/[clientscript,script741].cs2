// 741
[clientscript,script741]
def_int $int0 = %scab_component_clicked;
if ($int0 = 0) {
    return;
}
def_int $int1 = ~script739($int0);
def_model $model2 = enum(int, model, scab_rune_normal_enum, $int1);
if (testbit(%scab_correct_choices_bit, $int1) = ^true) {
    return;
}
if ($int0 = %scab_choice1 | $int0 = %scab_choice2 | $int0 = %scab_choice3) {
    return;
}
if_setmodel($model2, enum(int, component, scab_component_enum, $int0));
~script737($int0);
if (%scab_correct_choices_total = 12) {
}
if (%scab_total_tries_remaining = 0) {
    mes("The mechanism issues forth a whine and shuts down.");
    ~script675;
}
