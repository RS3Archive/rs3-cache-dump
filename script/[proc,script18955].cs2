// 18955
[proc,script18955](int $int0, int $int1)
if ($int1 = 1) {
    $int0 = 0;
} else {
    $int0 = ~clamp($int0, 1, 25);
}
def_int $int2 = 0;
while ($int2 <= 7) {
    highlight_set_category_scale($int2, $int0);
    if ($int1 = 1) {
        unknown_command_65(true);
    } else {
        unknown_command_65(false);
    }
    if ($int2 = 0) {
        if (~script18943 = false) {
            highlight_set_category_scale($int2, 0);
            highlight_set_localplayer_silhouette_mode(1);
        } else if ($int1 = 1) {
            highlight_set_localplayer_silhouette_mode(2);
        } else {
            highlight_set_localplayer_silhouette_mode(1);
        }
    }
    $int2 = calc($int2 + 1);
}
