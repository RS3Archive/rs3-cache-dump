// 17245
[proc,script17245](dbrow $dbrow0)(int, string)
def_int $int1 = db_getfield($dbrow0, requirements:col7, 0);
def_int $int2 = ~comlevel;
if ($int2 < $int1) {
    return(1, "Combat level: <tostring($int2)> / <tostring($int1)>");
}
return(0, "<str=FFFFFE>Combat level: <tostring($int2)> / <tostring($int1)>");
