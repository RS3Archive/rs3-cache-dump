// 18859
[proc,script18859](int $int0)(graphic)
db_find(calendar_category:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 ! null) {
    return(db_getfield($dbrow1, calendar_category:col1, 0));
}
return(null);
