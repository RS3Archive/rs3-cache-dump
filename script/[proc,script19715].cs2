// 19715
[proc,script19715](int $int0, int $int1)
def_int $int2 = 1;
def_string $string0 = "Custom";
def_string $string1 = "";
if (enum_hasoutput(int, toplevel_v2_layout_ids_save, $int0) = true) {
    $int0 = enum_getreverseindex(int, int, toplevel_v2_layout_ids_save, $int0, 0);
    $string0 = enum(int, string, toplevel_v2_layout_names_save, $int0);
} else {
    $string1 = enum(int, string, share_preset_layout_names, $int1);
}
def_int $int3 = 1;
if (if_find(toplevel_v2_edit_mode:dropdown_name) = true) {
    cc_list_clear;
    if ($int1 = 0) {
        cc_list_addentry(0, "As: <$string0>", -1);
        cc_list_setisselected(0, true, false);
    } else {
        cc_list_addentry(0, $string1, -1);
    }
    while ($int2 <= 255) {
        $string1 = enum(int, string, share_preset_layout_names, $int2);
        if (string_length($string1) > 0) {
            cc_list_addentry($int2, "As: <$string1>", -1);
            if ($int2 = $int1) {
                cc_list_setisselected($int3, true, false);
            }
            $int3 = calc($int3 + 1);
        }
        $int2 = calc($int2 + 1);
    }
}
