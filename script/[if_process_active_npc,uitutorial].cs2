// 16389
[if_process_active_npc,uitutorial]
if (~script16412 = true) {
    if (%uitutorial_built_height = -1) {
        %uitutorial_built_height = get_entity_overlay_height;
    }
    ~script16392(get_entity_screen_position(%uitutorial_built_height), get_entity_bounding_box);
}
