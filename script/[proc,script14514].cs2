// 14514
[proc,script14514](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    return(0);
}
def_int $int1 = db_getfieldcount($dbrow0, collection_log:col7);
def_dbrow $dbrow2 = null;
def_int $int3 = -1;
switch_dbrow ($dbrow0) {
    case collection_log_slayer :
        while (++$int3 < $int1) {
            $dbrow2 = db_getfield($dbrow0, collection_log:col7, $int3);
            if ($dbrow2 ! collection_slayer_general & ~script14502($dbrow2) ! 2) {
                return(0);
            }
        }
    case default :
        while (++$int3 < $int1) {
            if (~script14502(db_getfield($dbrow0, collection_log:col7, $int3)) ! 2) {
                return(0);
            }
        }
}
return(1);
