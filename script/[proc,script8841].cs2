// 8841
[proc,script8841](int $int0, int $int1)
if ($int0 <= 0) {
    return;
}
def_int $int2 = ~script8854;
if ($int1 = 1) {
    if ($int0 = 22) {
        if ($int2 ! $int0) {
            ~script8843($int2, 0);
        }
        ~script8837(%keyboard_manager_primary_enum, 0);
    } else {
        if ($int2 ! $int0) {
            ~script8843($int2, 0);
            ~script8837(%keyboard_manager_primary_enum, 1);
        }
        ~script8848;
        ~script9955($int0);
    }
    ~script8849($int0);
    ~script8843($int0, $int1);
} else {
    ~script8850($int0);
    ~script8843($int0, $int1);
    if ($int2 = $int0) {
        ~script8837(%keyboard_manager_primary_enum, 1);
        $int2 = ~script8854;
        ~script8841($int2, 1);
    }
}
