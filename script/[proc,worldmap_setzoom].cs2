// 1372
[proc,worldmap_setzoom](int $int0, int $int1)
def_int $int2 = %worldmap_zoom;
%worldmap_zoom = $int0;
worldmap_setzoom(%worldmap_zoom);
%worldmap_zoom = worldmap_getzoom;
if_settext("<tostring(%worldmap_zoom)>%", worldmap_v2_ui:zoom_level);
if ($int2 ! $int0 | $int1 = 1) {
    ~script8104;
}
