// 11855
[proc,script11855](component $component0)
def_int $int1 = %lifepoints;
def_int $int2 = ~script2915;
if (%toplevel_v2_legacy_combat = 1) {
    if ($int1 >= 10) {
        $int1 = calc($int1 / 10);
    } else if ($int1 > 0) {
        $int1 = 1;
    } else {
        $int1 = 0;
    }
    $int2 = max(1, calc($int2 / 10));
}
def_int $int3 = max(0, scale($int1, $int2, if_getwidth(toplevel_v2_combat_bar_mobile:health_bar)));
if_setsize($int3, if_getheight($component0), ^setsize_abs, ^setsize_abs, $component0);
