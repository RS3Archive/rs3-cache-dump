// 15924
[proc,script15924](int $int0, int $int1, int $int2)
def_string $string0 = "";
def_string $string1 = "";
def_boolean $boolean3 = false;
def_int $int4 = $int0;
if ($int0 = 10) {
    $int4 = 19;
}
$string1, $string0, $boolean3 = ~script9784($int4);
~script10410(bank:share_quick_build, bank:share_quick_click, rs3tli_30px_button_3x3, $int1, $int2, 30, 30, $int0, false, $string1);
if (cc_find(bank:share_quick_click, $int0) = true) {
    cc_setposition($int1, $int2, ^setpos_abs_left, ^setpos_abs_top);
    cc_setsize(30, 30, ^setsize_abs, ^setsize_abs);
    cc_setonmouserepeat("script8780(event_com, $int4)");
    if ($boolean3 = false) {
        cc_setop(1, "Load");
        cc_setop(2, "Save");
        cc_setop(3, "Settings");
        cc_setonop("script13941($int4, event_op)");
    } else {
        cc_clearops;
        cc_setop(1, "Load");
        cc_setonop(null);
        ~cc_add_rect(bank:share_quick_disabled, $int0, $int1, $int2, ^setpos_abs_left, ^setpos_abs_top, 30, 30, ^setsize_abs, ^setsize_abs, rect_disabled_overlay_dark);
    }
}
if ($int4 = 19) {
    ~script4512(bank:share_quick_build, 24, 24, calc($int1 + 3), calc($int2 + 3), ~script15934(child_tab_summon_familiar, icon_mobile_familiar));
}
