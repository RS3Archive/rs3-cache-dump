// 13941
[clientscript,script13941](int $int0, int $int1)
def_int $int2 = ~script15861($int0);
if ($int2 = -1) {
    return;
}
switch_int ($int1) {
    case default :
        if ($int2 <= 0) {
            %share_preset_menu_open = calc(1 - %share_preset_menu_open);
            if (%share_preset_last_preset = 0) {
                %share_preset_last_preset = 1;
            }
        } else if ($int2 >= 100) {
            ~script15977;
        } else {
            return;
        }
    case 2 :
        %share_preset_last_preset = $int2;
    case 3 :
        %share_preset_menu_open = 1;
        %share_preset_last_preset = $int2;
}
%varclientbit_45226 = %share_preset_menu_open;
~script13943;
