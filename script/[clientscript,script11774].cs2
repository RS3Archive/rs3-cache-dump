// 11774
[clientscript,script11774]
def_enum $enum0 = test_local_note_int_to_dbrow;
def_int $int1 = 0;
def_int $int2 = enum_getoutputcount($enum0);
def_dbrow $dbrow3 = null;
def_int $int4 = -1;
def_int $int5 = -1;
def_string $string0 = "";
mes_typed(^chattype_console, 0, "~~~ Printing Event Info ~~~");
while ($int1 < $int2) {
    $dbrow3 = enum(int, dbrow, $enum0, $int1);
    if ($dbrow3 ! null) {
        $string0 = "Event: <db_getfield($dbrow3, local_notification_event:col0, 0)>";
        $int4, $int5 = ~script3526($dbrow3);
        if ($int4 ! -1) {
            unknown_command_44($int4);
            if (notifications_islocalscheduled($int4) = 1) {
                $string0 = append($string0, " : Scheduled - ID: <tostring($int4)> - Time: <tostring($int5)>");
            } else {
                $string0 = append($string0, " : NOT Scheduled (Means it has already fired but not been cleared) - ID: <tostring($int4)>");
            }
        } else {
            $string0 = append($string0, " : No ID, so NOT scheduled");
        }
        mes_typed(^chattype_console, 0, $string0);
    }
    $int1 = calc($int1 + 1);
}
