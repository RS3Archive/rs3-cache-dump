// 14576
[proc,script14576]()(int)
def_int $int0 = enum_getoutputcount(%collector_board_list);
def_dbrow $dbrow1 = null;
def_int $int2 = -1;
while (++$int2 < $int0) {
    $dbrow1 = enum(int, dbrow, %collector_board_list, $int2);
    if (~script14575($dbrow1) = 1) {
        return($int2);
    }
}
return(-1);
