// 18858
[proc,script18858]()(int)
def_int $int0 = 0;
def_int $int1 = db_listall(calendar_category);
def_int $int2 = 0;
while ($int2 < $int1) {
    if (~script18856($int2) = true) {
        $int2 = calc($int2 + 1);
    } else {
        $int0 = calc($int0 + (~script18863($int2) + 1));
        $int2 = calc($int2 + 1);
    }
}
return($int0);
