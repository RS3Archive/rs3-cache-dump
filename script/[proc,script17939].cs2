// 17939
[proc,script17939](int $int0, int $int1)(int)
switch_int ($int0) {
    case 8256 :
        switch_int ($int1) {
            case 0 :
                return(80412726);
            case 1 :
                return(80347174);
            case 3 :
                return(26411027);
            case default :
                ~error("Missing mtx_bp3 plugin tab is <tostring($int1)>");
        }
    case default :
        ~error("Missing pluging, unknown layout.");
}
return(-1);
