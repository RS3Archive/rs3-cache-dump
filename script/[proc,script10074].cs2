// 10074
[proc,script10074]()(component)
switch_toplevelinterface (if_gettop) {
    case default :
        return(toplevel_v2:viewport);
    case lobbyscreen :
        return(lobbyscreen:base);
    case loginscreen :
        return(loginscreen:base);
}
