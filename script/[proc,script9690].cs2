// 9690
[proc,script9690](int $int0)
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = if_getwidth(toplevel_v2:twitch_window);
def_int $int4 = if_getheight(toplevel_v2:twitch_window);
if ($int0 = 26 & %toplevel_v2_legacy_interface = 0 & %toplevel_v2_customization_lock = 0) {
    $int1, $int2 = ~script8726(8, $int0);
    if ($int1 = -1 & $int2 = -1) {
        if ($int3 <= 200 & $int4 <= 135) {
            if_sethide(true, twitch_stream:minimise_button);
            if_sethide(false, twitch_stream:maximise_button);
        } else {
            if_sethide(false, twitch_stream:minimise_button);
            if_sethide(true, twitch_stream:maximise_button);
            return;
        }
    } else {
        if_sethide(true, twitch_stream:maximise_button);
        if_sethide(true, twitch_stream:minimise_button);
        return;
    }
}
