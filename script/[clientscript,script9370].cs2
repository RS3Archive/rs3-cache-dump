// 9370
[clientscript,script9370](int $int0)
def_enum $enum1 = null;
def_int $int2 = -1;
def_int $int3 = -1;
if (activeclansettings_find_affined(0) = true & ~script9371(%clan_event_current_varp) = 1) {
    switch_int (%clan_event_current_varp) {
        case 1 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_1_varp);
            $int2 = %clan_event_specific_1_varp;
            $int3 = %clan_event_who_1_varp;
        case 2 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_2_varp);
            $int2 = %clan_event_specific_2_varp;
            $int3 = %clan_event_who_2_varp;
        case 3 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_3_varp);
            $int2 = %clan_event_specific_3_varp;
            $int3 = %clan_event_who_3_varp;
        case 4 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_4_varp);
            $int2 = %clan_event_specific_4_varp;
            $int3 = %clan_event_who_4_varp;
        case 5 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_5_varp);
            $int2 = %clan_event_specific_5_varp;
            $int3 = %clan_event_who_5_varp;
        case 6 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_6_varp);
            $int2 = %clan_event_specific_6_varp;
            $int3 = %clan_event_who_6_varp;
        case 7 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_7_varp);
            $int2 = %clan_event_specific_7_varp;
            $int3 = %clan_event_who_7_varp;
        case 8 :
            $enum1 = enum(int, enum, clan_noticeboard_event_subtype_id, %clan_event_type_8_varp);
            $int2 = %clan_event_specific_8_varp;
            $int3 = %clan_event_who_8_varp;
    }
    switch_int ($int0) {
        case 45940841 :
            %clan_noticeboard_broadcast_date = calc((%clan_noticeboard_broadcast_date + 1) % 2);
        case 45940833 :
            %clan_noticeboard_broadcast_reltime = calc((%clan_noticeboard_broadcast_reltime + 1) % 2);
        case 45940825 :
            %clan_noticeboard_broadcast_gametime = calc((%clan_noticeboard_broadcast_gametime + 1) % 2);
        case 45940817 :
            %clan_noticeboard_broadcast_world = calc((%clan_noticeboard_broadcast_world + 1) % 2);
        case 45940809 :
            %clan_noticeboard_broadcast_place = calc((%clan_noticeboard_broadcast_place + 1) % 2);
        case 45940801 :
            %clan_noticeboard_broadcast_type = calc((%clan_noticeboard_broadcast_type + 1) % 2);
        case 45940793 :
            if ($enum1 ! null & $int2 > -1) {
                %clan_noticeboard_broadcast_subtype = calc((%clan_noticeboard_broadcast_subtype + 1) % 2);
            } else {
                %clan_noticeboard_broadcast_subtype = 0;
            }
        case 45940785 :
            %clan_noticeboard_broadcast_attendees = calc((%clan_noticeboard_broadcast_attendees + 1) % 2);
            if (%clan_noticeboard_broadcast_attendees = 0) {
                %clan_noticeboard_broadcast_ranklimit = 0;
            }
        case 45940777 :
            if (%clan_noticeboard_broadcast_attendees = 1 & $int3 > 0) {
                %clan_noticeboard_broadcast_ranklimit = calc((%clan_noticeboard_broadcast_ranklimit + 1) % 2);
            } else {
                %clan_noticeboard_broadcast_ranklimit = 0;
            }
        case 45940769 :
            %clan_noticeboard_broadcast_mandatory = calc((%clan_noticeboard_broadcast_mandatory + 1) % 2);
        case 45940761 :
            %clan_noticeboard_broadcast_moreinfo = calc((%clan_noticeboard_broadcast_moreinfo + 1) % 2);
    }
    ~script9369;
    ~script9374;
}
