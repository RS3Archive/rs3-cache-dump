// 3185
[clientscript,script3185](int $int0, char $char1, component $component2)
switch_int ($int0) {
    case 84 :
        ~script3183;
        return;
    case 13 :
        ~script3180;
        return;
    case 96, 97, 98, 99, 102, 103 :
        %lobby_caret = ~script1553($int0, %lobby_caret, %lobbyscreen_input);
        ~script1879(lobbyscreen_report_abuse_stage1:name, lobbyscreen_report_abuse_stage1:name_caret, %lobbyscreen_input);
        return;
    case default :
        if (char_isprintable($char1) = true | $int0 = 85 | $int0 = 101) {
            if ($int0 = -1 & string_length(removetags(append_char("", $char1))) = 0) {
                return;
            }
            %lobbyscreen_input, %lobby_caret = ~script802(%lobby_caret, %lobbyscreen_input, 2, $int0, $char1);
            if_settext(%lobbyscreen_input, $component2);
            ~script1879(lobbyscreen_report_abuse_stage1:name, lobbyscreen_report_abuse_stage1:name_caret, %lobbyscreen_input);
        }
}
