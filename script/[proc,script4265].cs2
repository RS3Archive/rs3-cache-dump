// 4265
[proc,script4265](component $component0, int $int1)(int, int)
def_int $int2 = 233;
def_int $int3 = 0;
if (if_find($component0) = true) {
    $int2, $int3 = ~script3874($int1);
    if ($int2 ! -1 & cc_findbycategory($component0, $int2, $int3) = 1) {
        cc_deleteallnested;
        cc_delete;
    }
    if (if_find($component0) = true) {
        $int2 = 233;
        $int3 = if_getnextcategorysubid($int2, $component0);
        ~script3858($int1, $int2, $int3);
        ~cc_add_child_layer($int2, $int3, 0, 0, ^setpos_abs_left, ^setpos_abs_top, 0, 0, ^setsize_minus, ^setsize_minus);
        cc_sethide(true);
    }
}
return($int2, $int3);
