// 10223
[twitch_event,webcam_device_info](int $int0, int $int1, string $string0)
def_int $int2 = -1;
def_string $string1 = "";
def_string $string2 = "";
if (%varclientbit_23069 = 1 & %twitch_webcam_options_id ! -1 & string_length(%twitch_webcam_idname) > 0) {
    $int2, $string1, $string2 = ttv_webcam_getdevice_byuniquename(%twitch_webcam_idname);
    if ($int2 ! -1) {
        return;
    } else {
        ~script10145;
    }
    mes("A new webcam has been detected.");
}
