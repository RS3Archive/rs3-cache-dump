// 12375
[proc,script12375](int $int0, int $int1)(int)
def_int $int2 = 0;
if ($int1 = 14) {
    $int2 = calc($int2 + ~script5566(%gwd2_rep_seren, enum(int, int, gwd2_rep_unlock_reqs, 14), 1));
    $int2 = calc($int2 + ~script5566(%gwd2_rep_sliske, enum(int, int, gwd2_rep_unlock_reqs, 14), 1));
    $int2 = calc($int2 + ~script5566(%gwd2_rep_zamorak, enum(int, int, gwd2_rep_unlock_reqs, 14), 1));
    $int2 = calc($int2 + ~script5566(%gwd2_rep_zaros, enum(int, int, gwd2_rep_unlock_reqs, 14), 1));
    if ($int2 >= 4) {
        return(1);
    } else {
        return(0);
    }
}
switch_int ($int0) {
    case 1 :
        return(~script5566(%gwd2_rep_seren, enum(int, int, gwd2_rep_unlock_reqs, $int1), 1));
    case 2 :
        return(~script5566(%gwd2_rep_sliske, enum(int, int, gwd2_rep_unlock_reqs, $int1), 1));
    case 3 :
        return(~script5566(%gwd2_rep_zamorak, enum(int, int, gwd2_rep_unlock_reqs, $int1), 1));
    case 4 :
        return(~script5566(%gwd2_rep_zaros, enum(int, int, gwd2_rep_unlock_reqs, $int1), 1));
}
return(0);
