// 17928
[proc,script17928](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    ~error("Invalid pass.");
    return(0);
}
if ($dbrow0 = dbrow_7906) {
    return(%bp3_viewed_pass_currency);
}
if (db_getrowtable($dbrow0) ! bp3_table) {
    ~error("Invalid pass.");
    return(0);
}
return(var_reference_get(db_getfield($dbrow0, bp3_table:col14, 0)));
