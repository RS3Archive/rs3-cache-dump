// 13166
[proc,script13166](int $int0)
def_int $int1 = -1;
if ($int0 ! -1) {
    $int1 = ~script13164($int0);
    if ($int1 = -1) {
        ~script13230;
        return;
    }
}
def_int $int2 = -1;
if ($int1 ! -1) {
    $int2 = calc($int1 + 1);
    $int1 = calc($int1 - 1);
} else {
    $int2 = enum_getoutputcount(mtxmgt_store_filter_button_structs);
}
def_struct $struct3 = null;
def_struct $struct4 = null;
def_string $string0 = "";
def_string $string1 = "";
while (++$int1 < $int2) {
    $struct3 = enum(int, struct, mtxmgt_store_filter_button_structs, $int1);
    if ($struct3 ! null) {
        $struct4, $string0, $string1 = ~script13167(~script13165($int1));
        ~script10015(struct_param($struct3, rs3tli_button_on_layer), struct_param($struct3, rs3tli_button_over_layer), struct_param($struct3, rs3tli_button_hit_layer), struct_param($struct3, rs3tli_button_selected_layer), struct_param($struct3, rs3tli_button_disabled_layer), $struct4);
        if (string_length($string1) > 0) {
            if_setonmouserepeat("script3876($string1, event_com, event_comsubid)", struct_param($struct3, rs3tli_button_disabled_layer));
        } else {
            if_setonmouserepeat(null, struct_param($struct3, rs3tli_button_disabled_layer));
        }
        if (string_length($string0) > 0) {
            if_setonmouserepeat("script3876($string0, event_com, event_comsubid)", struct_param($struct3, rs3tli_button_active_layer));
        } else {
            if_setonmouserepeat(null, struct_param($struct3, rs3tli_button_active_layer));
        }
    }
}
