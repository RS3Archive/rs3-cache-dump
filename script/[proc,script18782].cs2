// 18782
[proc,script18782](int $int0)(string)
db_find(headicons_emote:col3, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Attempting to get name for an emote with invalid id = <tostring($int0)>.");
    return(null);
}
return(~script18302($dbrow1));
