// 803
[clientscript,firedup_map_onload]
def_component $component0 = null;
switch_int (%firedup_whereami) {
    case 1 :
        $component0 = firedup_beacon_map:firedup_player_icon_1;
    case 2 :
        $component0 = firedup_beacon_map:firedup_player_icon_2;
    case 3 :
        $component0 = firedup_beacon_map:firedup_player_icon_3;
    case 4 :
        $component0 = firedup_beacon_map:firedup_player_icon_4;
    case 5 :
        $component0 = firedup_beacon_map:firedup_player_icon_5;
    case 6 :
        $component0 = firedup_beacon_map:firedup_player_icon_6;
    case 7 :
        $component0 = firedup_beacon_map:firedup_player_icon_7;
    case 8 :
        $component0 = firedup_beacon_map:firedup_player_icon_8;
    case 9 :
        $component0 = firedup_beacon_map:firedup_player_icon_9;
    case 10 :
        $component0 = firedup_beacon_map:firedup_player_icon_10;
    case 11 :
        $component0 = firedup_beacon_map:firedup_player_icon_11;
    case 12 :
        $component0 = firedup_beacon_map:firedup_player_icon_12;
    case default :
        return;
}
if_sethide(false, $component0);
if_setonmouserepeat("script8799(enum(int, string, firedup_map_you_are_here, %firedup_whereami), event_com, event_comsubid)", $component0);
switch_int (%firedup_whereami) {
    case 1 :
        if (%firedup_bird1 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 2 :
        if (%firedup_bird2 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 3 :
        if (%firedup_bird3 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 4 :
        if (%firedup_bird4 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 5 :
        if (%firedup_bird5 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 6 :
        if (%firedup_bird6 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 7 :
        if (%firedup_bird7 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 8 :
        if (%firedup_bird8 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 9 :
        if (%firedup_bird9 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 10 :
        if (%firedup_bird10 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 11 :
        if (%firedup_bird11 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case 12 :
        if (%firedup_bird12 = 1) {
            ~script804;
        } else {
            ~firedup_map_info(%firedup_whereami);
        }
    case default :
        return;
}
