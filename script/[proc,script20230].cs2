// 20230
[proc,script20230]
~script20211(0);
cc_deleteall(league_onboarding:contents_confirm_fixed);
if (if_find(league_onboarding:contents_confirm_fixed) = true) {
    ~script20231;
}
cc_deleteall(league_onboarding:contents_confirm_scrolling);
if (if_find(league_onboarding:contents_confirm_scrolling) = true) {
    ~script20233;
}
