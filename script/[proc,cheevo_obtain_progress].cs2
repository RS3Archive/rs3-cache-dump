// 13280
[proc,cheevo_obtain_progress](struct $struct0)(int)
if ($struct0 = null) {
    return(-65535);
}
if (struct_param($struct0, cheevo_unique_id) <= -1) {
    if (struct_param($struct0, task_unique_id) = 4090) {
        return(-65535);
    }
    ~error("Struct ID is -1 - <struct_param($struct0, cheevo_name)> [<struct_param($struct0, cheevo_details)>] <tostring(struct_param($struct0, task_unique_id))> <struct_param($struct0, task_name)> <struct_param($struct0, task_details)>");
    return(-1);
}
def_int $int1 = 0;
def_int $int2 = 0;
if (struct_param($struct0, cheevo_is_meta) = 1) {
    return(~script13567($struct0));
} else {
    $int1 = ~cheevo_get_progress($struct0);
    if ($int1 = -1) {
        return(-1);
    }
    $int2 = ~cheevo_get_check_var_total($struct0);
    if ($struct0 = cheevo_atelf_impling) {
        $int1 = testbit($int1, 18);
        $int2 = 1;
    }
    if ($int1 >= $int2) {
        return(-2);
    } else if (~cheevo_requirements_met($struct0) = 0) {
        return(-1);
    } else if ($int1 > 0) {
        if (struct_param($struct0, cheevo_check_type) = 1) {
            $int1 = scale(bitcount($int1), bitcount($int2), 10000);
        } else {
            $int1 = scale($int1, $int2, 10000);
        }
        if ($int1 >= 10000) {
            return(-2);
        } else if (~date_runeday < struct_param($struct0, cheevo_gracedate)) {
            return(-3);
        } else {
            return($int1);
        }
    }
}
if (~date_runeday < struct_param($struct0, cheevo_gracedate)) {
    return(-3);
} else {
    return(0);
}
