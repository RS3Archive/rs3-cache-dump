// 1234
[clientscript,script1234](int $int0)
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = enum_getoutputcount(toplevel_v2_panel_id_listing);
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = 0;
def_int $int7 = 0;
def_int $int8 = 0;
def_int $int9 = 0;
def_int $int10 = 0;
def_int $int11 = 0;
def_int $int12 = 0;
def_int $int13 = 0;
def_int $int14 = 0;
def_struct $struct15 = null;
switch_int ($int0) {
    case 8 :
        mes_typed(^chattype_console, 0, "Active Layout");
    case 9 :
        mes_typed(^chattype_console, 0, "Visible Layout");
    case 6 :
        mes_typed(^chattype_console, 0, "Custom1 Layout");
    case 7 :
        mes_typed(^chattype_console, 0, "Custom2 Layout");
    case 12 :
        mes_typed(^chattype_console, 0, "Custom3 Layout");
    case 13 :
        mes_typed(^chattype_console, 0, "Custom4 Layout");
    case default :
        mes_typed(^chattype_console, 0, "Invalid Layout");
        return;
}
while ($int2 < $int3) {
    $int1 = enum(int, int, toplevel_v2_panel_id_listing, $int2);
    $struct15 = ~script10405($int1);
    if ($struct15 ! null) {
        if (calc($int2 % 15) = 0) {
            mes_typed(^chattype_console, 0, "");
            mes_typed(^chattype_console, 0, "Window Name / X / Y / X-align / Y-align / W / H / prev_tab / next_tab / vis / lightweight");
        }
        $int4, $int5, $int8, $int9, $int6, $int7, $int10, $int11, $int12, $int13, $int14 = ~script8701($int0, $int1);
        mes_typed(^chattype_console, 0, "<struct_param($struct15, toplevel_v2_window_name)> / <tostring($int4)> / <tostring($int5)> / <tostring($int8)> / <tostring($int9)> / <tostring($int6)> / <tostring($int7)> / <tostring($int10)> / <tostring($int11)> / <tostring($int12)> / <tostring($int13)>");
    }
    $int2 = calc($int2 + 1);
}
