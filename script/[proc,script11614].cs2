// 11614
[proc,script11614](struct $struct0)
def_enum $enum1 = struct_param($struct0, mtx_front_end_confirm_modular_pet_models);
def_enum $enum2 = struct_param($struct0, mtx_front_end_confirm_modular_pet_models_default);
def_int $int3 = 1;
def_model $model4 = null;
if ($enum1 ! null) {
    while ($int3 <= 7) {
        $model4 = enum(int, model, $enum1, $int3);
        if ($model4 ! null & $model4 ! 6awe1_invis_dummy_loc) {
            if_npc_setcustombodymodel($int3, $model4, mtx_front_end_confirm:pet_model);
        } else if ($enum2 ! null) {
            $model4 = enum(int, model, $enum2, $int3);
            if ($model4 ! null & $model4 ! 6awe1_invis_dummy_loc) {
                if_npc_setcustombodymodel($int3, $model4, mtx_front_end_confirm:pet_model);
            }
        }
        $int3 = calc($int3 + 1);
    }
}
def_material $material5 = struct_param($struct0, mtx_front_end_confirm_modular_pet_retex);
if ($material5 ! null) {
    if_npc_setcustomretex(4, $material5, mtx_front_end_confirm:pet_model);
}
