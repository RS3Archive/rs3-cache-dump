// 885
[clientscript,script885]
def_struct $struct0 = ~script13032;
switch_struct ($struct0) {
    case timed_event10 :
        ~script10542;
    case timed_event11 :
        ~script879;
    case timed_event16 :
        ~script4605;
    case timed_event_easter_2023 :
        if_setsize(742, 404, ^setsize_abs, ^setsize_abs, timed_event:universe);
}
def_boolean $boolean1 = struct_param($struct0, timed_event_teleport_button);
if (struct_param($struct0, timed_event_compact_countdown) > 0) {
    if_sethide(false, timed_event:compact_countdown);
    if_sethide(~negate_bool($boolean1), timed_event:teleport_button);
    if_sethide(true, timed_event:full_countdown);
    if_sethide(true, timed_event:classic_teleport_button_layer);
    if_sethide(true, timed_event:event_ended_layer);
} else {
    if_sethide(true, timed_event:compact_countdown);
    if_sethide(false, timed_event:full_countdown);
    if_sethide(~negate_bool($boolean1), timed_event:teleport_button);
    if_sethide(true, timed_event:classic_teleport_button_layer);
    if_sethide(false, timed_event:event_ended_layer);
}
