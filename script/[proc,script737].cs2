// 737
[proc,script737](int $int0)
def_model $model1 = null;
if (%scab_choice1 = 0) {
    %scab_choice1 = $int0;
    return;
}
if (%scab_choice2 = 0) {
    %scab_choice2 = $int0;
    return;
}
if (%scab_choice3 = 0) {
    %scab_choice3 = $int0;
    if (~script739(%scab_choice1) = ~script739(%scab_choice2) & ~script739(%scab_choice1) = ~script739(%scab_choice3)) {
        $model1 = enum(int, model, scab_rune_recessed_enum, ~script739(%scab_choice1));
        if_setmodel($model1, enum(int, component, scab_component_enum, %scab_choice1));
        if_setmodel($model1, enum(int, component, scab_component_enum, %scab_choice2));
        if_setmodel($model1, enum(int, component, scab_component_enum, %scab_choice3));
        %scab_correct_choices_bit = setbit(%scab_correct_choices_bit, ~script739(%scab_choice1));
        %scab_choice1 = 0;
        %scab_choice2 = 0;
        %scab_choice3 = 0;
        %scab_correct_choices_total = calc(%scab_correct_choices_total + 1);
        %scab_total_tries_remaining = calc(%scab_total_tries_remaining - 1);
        ~script738;
    }
    return;
}
if_setmodel(scab_blank_rune, enum(int, component, scab_component_enum, %scab_choice1));
if_setmodel(scab_blank_rune, enum(int, component, scab_component_enum, %scab_choice2));
if_setmodel(scab_blank_rune, enum(int, component, scab_component_enum, %scab_choice3));
%scab_choice1 = $int0;
%scab_choice2 = 0;
%scab_choice3 = 0;
%scab_total_tries_remaining = calc(%scab_total_tries_remaining - 1);
~script738;
