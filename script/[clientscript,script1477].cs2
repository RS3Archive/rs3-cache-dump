// 1477
[clientscript,script1477]
def_string $string0 = "";
if (%wc_update09_difficulty = 0) {
    $string0 = "Quick task:";
} else {
    $string0 = "Large task:";
}
~script9554(wc_update09_sawmill_overlay:bespoke_window_background, wc_update09_sawmill_overlay:bespoke_window_border, wc_update09_sawmill_overlay:bespoke_window_content, $string0, toplevel_v2_bespoke_window_skin_data);
if_settext("<tostring(%wc_update09_actual_short)> / <tostring(calc((%wc_update09_target_short + 1) * 5))>", wc_update09_sawmill_overlay:short_cut_text);
if_settext("<tostring(%wc_update09_actual_long)> / <tostring(calc((%wc_update09_target_long + 1) * 5))>", wc_update09_sawmill_overlay:long_cut_text);
if_settext("<tostring(%wc_update09_actual_diag)> / <tostring(calc((%wc_update09_target_diag + 1) * 5))>", wc_update09_sawmill_overlay:diagonal_cut_text);
if_settext("<tostring(%wc_update09_actual_tooth)> / <tostring(calc((%wc_update09_target_tooth + 1) * 5))>", wc_update09_sawmill_overlay:tooth_cut_text);
if_settext("<tostring(%wc_update09_actual_groove)> / <tostring(calc((%wc_update09_target_groove + 1) * 5))>", wc_update09_sawmill_overlay:groove_cut_text);
if_settext("<tostring(%wc_update09_actual_curve)> / <tostring(calc((%wc_update09_target_curve + 1) * 5))>", wc_update09_sawmill_overlay:curved_cut_text);
