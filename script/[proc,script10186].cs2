// 10186
[proc,script10186](int $int0, int $int1)
if ($int0 > $int1 | $int1 > 20) {
    return;
}
def_int $int2 = $int0;
while ($int2 <= $int1) {
    if ($int2 <= 15 & cc_find(bank:tab_highlights, $int2) = true) {
        cc_sethide(true);
    }
    if (cc_find(bank:tab_separators, $int2) = true) {
        cc_sethide(true);
    }
    if (cc_find(bank:tab_dividers, $int2) = true) {
        cc_sethide(true);
    }
    $int2 = calc($int2 + 1);
}
