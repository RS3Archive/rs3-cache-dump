// 12191
[proc,script12191](obj $obj0)(int)
if (oc_param($obj0, invent_gizmo_costless) = true) {
    return(1);
}
def_dbrow $dbrowarray0(9);
def_int $intarray1(9);
def_int $int1 = 9;
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 0;
def_dbrow $dbrow5 = null;
while ($int1-- > 0) {
    if (cc_find(invent_gizmo:work_layer, $int1) = true) {
        $dbrow5 = cc_param(invent_client_material);
        if ($dbrow5 ! null) {
            $int4 = 1;
            $int2 = -1;
            while (++$int2 < $int3) {
                if ($dbrow5 = $dbrowarray0($int2)) {
                    $intarray1($int2) = calc($intarray1($int2) + $int4);
                    $int2 = $int3;
                    $int4 = 0;
                }
            }
            if ($int4 > 0) {
                $intarray1($int3) = $int4;
                $dbrowarray0($int3) = $dbrow5;
                $int3 = calc($int3 + 1);
            }
        }
    }
}
def_int $int6 = -1;
def_string $string0 = "";
def_string $string1 = "";
$int1 = -1;
while (++$int1 < $int3) {
    if ($dbrowarray0($int1) ! null & $intarray1($int1) > 0 & ~invent_material_count(db_getfield($dbrowarray0($int1), invent_material_table:col0, 0)) < calc($intarray1($int1) * ~script12065($dbrowarray0($int1)))) {
        return(0);
    }
}
return(1);
