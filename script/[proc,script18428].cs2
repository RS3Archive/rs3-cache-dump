// 18428
[proc,script18428](int $int0, dbrow $dbrow1)(int)
def_dbrow $dbrow2 = null;
if ($dbrow1 = null) {
    $dbrow1 = ~script18427;
}
if ($int0 > 0 & $dbrow1 ! null & ~script18415($int0, $dbrow1) = true) {
    $dbrow2 = db_getfield($dbrow1, buff_select_event_day:col0, calc($int0 - 1));
    if ($dbrow2 ! null) {
        return(db_getfield($dbrow2, buff_select_event_buff:col0, 0));
    }
}
return(-1);
