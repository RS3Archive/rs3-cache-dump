// 299
[proc,script299](int $int0, dbrow $dbrow1)(int, int)
def_int $int2 = 0;
def_int $int3 = db_getfieldcount($dbrow1, runepass_table:col15);
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = -1;
def_int $int7 = -1;
def_int $int8 = -1;
while ($int2 < $int3) {
    $int6, $int7, $int4, $int5 = db_getfield($dbrow1, runepass_table:col15, $int2);
    if ($int6 = $int0 & $int8 = -1) {
        $int8 = $int2;
    }
    if ($int6 ! $int0 & $int8 ! -1) {
        return($int8, calc($int2 - $int8));
    }
    $int2 = calc($int2 + 1);
}
if ($int8 ! -1 & $int6 = $int0) {
    return($int8, calc($int2 - 1 - $int8));
}
~error("Runepass just ran out yak_path points at button <tostring($int6)>, but we wanted data for button <tostring($int0)> ");
return(0, 0);
