// 13194
[clientscript,script13194](component $component0, int $int1, int $int2)
if ($int2 = 1 & keyheld_shift = true | $int2 = 5) {
    if (cc_find($component0, $int1) = true & compare(cc_getop(5), "") ! 0) {
        if_triggerop(mtxmgt_store:command_layer, $int1, 5);
    }
} else if ($int2 = 1 & keyheld_ctrl = true | $int2 = 2) {
    if (cc_find($component0, $int1) = true & compare(cc_getop(2), "") ! 0) {
        if_triggerop(mtxmgt_store:command_layer, $int1, 2);
    }
} else if ($int2 = 1) {
    if (enum_getreversecount(int, mtxmgt_store_filter_slot_dropdown, $int1) > 0) {
        %varclientbit_35135 = $int1;
        ~script13169(null, 2);
    }
} else if ($int2 = 3) {
    if (cc_find($component0, $int1) = true & compare(cc_getop(3), "") ! 0) {
        if_triggerop(mtxmgt_store:command_layer, $int1, 3);
    }
} else if ($int2 = 4 & cc_find($component0, $int1) = true & compare(cc_getop(4), "") ! 0) {
    if_triggerop(mtxmgt_store:command_layer, $int1, 4);
}
