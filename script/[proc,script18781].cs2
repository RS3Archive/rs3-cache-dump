// 18781
[proc,script18781](int $int0)(graphic)
db_find(headicons_emote:col3, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Attempting to get graphic for an emote with invalid id = <tostring($int0)>.");
    return(null);
}
return(~script18301($dbrow1));
