// 8177
[proc,script8177]
if (%toplevel_v2_edit_mode = true) {
    if (%toplevel_v2_hud_hidden = 0) {
        mes("You can't do that in Edit Mode.");
    }
    return;
}
if (%cutscene_status > 0) {
    mes("You can't do that right now.");
    return;
}
if (~script6431 = true & ~script14439 = true) {
    return;
}
if (worldlist_fetch = false) {
}
if_sethide(false, toplevel_v2:escape_menu);
if_sethide(true, escape_menu:logout_popup);
if_sethide(true, escape_menu_mobile:logout_popup);
~script6739(9);
~script8841(1, 1);
~script13831(1);
if (~script6431 = true) {
    ~script15063(true);
    ~script14987(false);
    ~script15949;
} else {
    ~script17044(escape_menu:parents_intern_notes);
}
