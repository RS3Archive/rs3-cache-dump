// 10996
[proc,script10996](struct $struct0, int $int1)(seq)
switch_struct ($struct0) {
    case mtx116_joker_walk_override_struct :
        return(~script2735($int1));
    case mtx_batch83_nomad_walk_override :
        return(~script11013($int1));
    case mtx93_barbarian_walk_override :
        return(~script956($int1));
    case mtx93_hunter_walk_override :
        return(~script11844($int1));
    case mtx93_revenant_walk_override :
        return(~script11845($int1));
    case default :
        if (struct_param($struct0, mtxmgt_preview_anim) ! null) {
            return(struct_param($struct0, mtxmgt_preview_anim));
        } else {
            return(bas_getanim_ready(default_human));
        }
}
