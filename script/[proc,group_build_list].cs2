// 10350
[proc,group_build_list]
cc_deleteall(group_parent:cat_build);
cc_deleteall(group_parent:cat_click);
cc_deleteall(group_parent:sys_build);
cc_deleteall(group_parent:sys_click);
def_int $int0 = 0;
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = if_getwidth(group_parent:cat_build);
def_int $int5 = ~script10495(3);
def_int $int6 = enum_getoutputcount(group_list_order);
def_int $int7 = 0;
def_enum $enum8 = null;
def_string $string0 = "";
def_boolean $boolean9 = false;
def_fontmetrics $fontmetrics10 = museo_sans_11pt_regular;
$fontmetrics10, $int5 = ~script10432(0);
while ($int3 < $int6) {
    cc_create(group_parent:sys_click, ^iftype_text, $int3);
    cc_setsize(0, 20, ^setsize_minus, ^setsize_abs);
    cc_setposition(0, 0, ^setpos_abs_left, ^setpos_abs_top);
    cc_setop(1, "Select");
    cc_setop(2, "View");
    cc_setonop("script10360($int3)");
    cc_sethide(true);
    $int3 = calc($int3 + 1);
}
$int6 = enum_getoutputcount(group_category_order);
$int3 = 0;
def_int $int11 = 0;
while ($int3 < $int6) {
    $int0 = enum(int, int, group_category_order, $int3);
    $enum8 = enum(int, enum, group_category_to_enum, $int0);
    if ($enum8 ! null) {
        $int11 = $int1;
        $boolean9 = ~script10357($int0);
        ~script13980(group_parent:cat_build, null, rs3_dropdown, 0, $int1, $int4, -1, false, enum(int, string, group_category_to_name, $int0));
        $int1 = calc($int1 + (24 + 3));
        cc_create(group_parent:cat_click, ^iftype_text, $int3);
        cc_setsize(0, 22, ^setsize_minus, ^setsize_abs);
        cc_setposition(0, $int11, ^setpos_abs_left, ^setpos_abs_top);
        if ($boolean9 = true) {
            cc_setop(1, "Collapse");
        } else {
            cc_setop(1, "Expand");
        }
        cc_setonop("script10358($int0)");
        if ($boolean9 = true) {
            $int1, $int2 = ~script10354($enum8, $int1, $int4, $int2);
        }
    }
    $int3 = calc($int3 + 1);
}
if ($int2 ! 0) {
    $int2 = max(0, calc($int2 - if_getheight(group_parent:scrolling_area) / 2));
}
if ($int1 > if_getheight(group_parent:scrolling_area)) {
    if_setscrollsize(0, $int1, group_parent:scrolling_area);
    if_setscrollpos(0, $int2, group_parent:scrolling_area);
} else {
    if_setscrollsize(0, 0, group_parent:scrolling_area);
    if_setscrollpos(0, 0, group_parent:scrolling_area);
}
~scrollbar_vertical(group_parent:scrollbar_layer, group_parent:scrolling_area, "scrollbar_dragger_v2,3", "scrollbar_dragger_v2,0", "scrollbar_dragger_v2,1", "scrollbar_dragger_v2,2", "scrollbar_v2,0", "scrollbar_v2,1");
