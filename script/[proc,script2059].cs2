// 2059
[proc,script2059]
if_setgraphic("aif_pop_ship_icons,0", pop_voyage_list:voyage_select_ship_1);
if_setgraphic("aif_pop_ship_icons,0", pop_voyage_list:voyage_select_ship_2);
if_setgraphic("aif_pop_ship_icons,0", pop_voyage_list:voyage_select_ship_3);
if_setgraphic("aif_pop_ship_icons,0", pop_voyage_list:voyage_select_ship_4);
def_int $int0 = ~script4186;
if_sethide(false, pop_voyage_list:voyage_send_no);
def_string $string0 = "";
def_string $string1 = "";
def_string $string2 = "";
def_string $string3 = "";
if_setop(1, "View ship", pop_voyage_list:voyage_ship_gone_icon_2);
if_setop(1, "View ship", pop_voyage_list:voyage_ship_gone_icon_3);
if_setop(1, "View ship", pop_voyage_list:voyage_ship_gone_icon_4);
if (%pop_voyage_1_done > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_1);
    if_setgraphic("aif_pop_ship_icons,3", pop_voyage_list:voyage_ship_gone_icon_1);
    if_settext("Returned", pop_voyage_list:voyage_ship_state_1);
    $string0 = "This ship needs to be welcomed home.";
} else if (%pop_voyage_1_asynch > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_1);
    if_setgraphic("aif_pop_ship_icons,7", pop_voyage_list:voyage_ship_gone_icon_1);
    if (%pop_voyage_1_id = 32723 | %pop_voyage_1_id = 32724 | %pop_voyage_1_id = 32722) {
        if_settext("Damaged", pop_voyage_list:voyage_ship_state_1);
    } else {
        if_settext("Sailing", pop_voyage_list:voyage_ship_state_1);
    }
    $string0 = "This ship is currently on a mission.";
} else {
    if_sethide(true, pop_voyage_list:voyage_ship_gone_1);
    if_settext("Ready", pop_voyage_list:voyage_ship_state_1);
    if (%pop_voyage_list_ship = 1) {
        if_setgraphic("aif_pop_ship_icons,2", pop_voyage_list:voyage_select_ship_1);
        if (~script7294(1) ! -1) {
            if_sethide(true, pop_voyage_list:voyage_send_no);
        } else {
            $string0 = "This ship currently has no captain assigned.";
        }
    }
}
if ($int0 < 2) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_2);
    if_setgraphic("aif_pop_ship_icons,11", pop_voyage_list:voyage_ship_gone_icon_2);
    if_settext("No Ship", pop_voyage_list:voyage_ship_state_2);
    $string1 = "You do not have a ship in this port bay.";
    if_clearops(pop_voyage_list:voyage_ship_gone_icon_2);
} else if (%pop_voyage_2_done > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_2);
    if_setgraphic("aif_pop_ship_icons,3", pop_voyage_list:voyage_ship_gone_icon_2);
    if_settext("Returned", pop_voyage_list:voyage_ship_state_2);
    $string1 = "This ship needs to be welcomed home.";
} else if (%pop_voyage_2_asynch > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_2);
    if_setgraphic("aif_pop_ship_icons,7", pop_voyage_list:voyage_ship_gone_icon_2);
    if (%pop_voyage_2_id = 32723 | %pop_voyage_2_id = 32724 | %pop_voyage_2_id = 32722) {
        if_settext("Damaged", pop_voyage_list:voyage_ship_state_2);
    } else {
        if_settext("Sailing", pop_voyage_list:voyage_ship_state_2);
    }
    $string1 = "This ship is currently on a mission.";
} else {
    if_sethide(true, pop_voyage_list:voyage_ship_gone_2);
    if_settext("Ready", pop_voyage_list:voyage_ship_state_2);
    if (%pop_voyage_list_ship = 2) {
        if_setgraphic("aif_pop_ship_icons,2", pop_voyage_list:voyage_select_ship_2);
        if (~script7294(2) ! -1) {
            if_sethide(true, pop_voyage_list:voyage_send_no);
        } else {
            $string1 = "This ship currently has no captain assigned.";
        }
    }
}
if ($int0 < 3) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_3);
    if_setgraphic("aif_pop_ship_icons,11", pop_voyage_list:voyage_ship_gone_icon_3);
    if_settext("No Ship", pop_voyage_list:voyage_ship_state_3);
    $string2 = "You do not have a ship in this port bay.";
    if_clearops(pop_voyage_list:voyage_ship_gone_icon_3);
} else if (%pop_voyage_3_done > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_3);
    if_setgraphic("aif_pop_ship_icons,3", pop_voyage_list:voyage_ship_gone_icon_3);
    if_settext("Returned", pop_voyage_list:voyage_ship_state_3);
    $string2 = "This ship needs to be welcomed home.";
} else if (%pop_voyage_3_asynch > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_3);
    if_setgraphic("aif_pop_ship_icons,7", pop_voyage_list:voyage_ship_gone_icon_3);
    if (%pop_voyage_3_id = 32723 | %pop_voyage_3_id = 32724 | %pop_voyage_3_id = 32722) {
        if_settext("Damaged", pop_voyage_list:voyage_ship_state_3);
    } else {
        if_settext("Sailing", pop_voyage_list:voyage_ship_state_3);
    }
    $string2 = "This ship is currently on a mission.";
} else {
    if_sethide(true, pop_voyage_list:voyage_ship_gone_3);
    if_settext("Ready", pop_voyage_list:voyage_ship_state_3);
    if (%pop_voyage_list_ship = 3) {
        if_setgraphic("aif_pop_ship_icons,2", pop_voyage_list:voyage_select_ship_3);
        if (~script7294(3) ! -1) {
            if_sethide(true, pop_voyage_list:voyage_send_no);
        } else {
            $string2 = "This ship currently has no captain assigned.";
        }
    }
}
if ($int0 < 4) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_4);
    if_setgraphic("aif_pop_ship_icons,11", pop_voyage_list:voyage_ship_gone_icon_4);
    if_settext("No Ship", pop_voyage_list:voyage_ship_state_4);
    if_clearops(pop_voyage_list:voyage_ship_gone_icon_4);
    $string3 = "You do not have a ship in this port bay.";
} else if (%pop_voyage_4_done > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_4);
    if_setgraphic("aif_pop_ship_icons,3", pop_voyage_list:voyage_ship_gone_icon_4);
    if_settext("Returned", pop_voyage_list:voyage_ship_state_4);
    $string3 = "This ship needs to be welcomed home.";
} else if (%pop_voyage_4_asynch > 0) {
    if_sethide(false, pop_voyage_list:voyage_ship_gone_4);
    if_setgraphic("aif_pop_ship_icons,7", pop_voyage_list:voyage_ship_gone_icon_4);
    if (%pop_voyage_4_id = 32723 | %pop_voyage_4_id = 32724 | %pop_voyage_4_id = 32722) {
        if_settext("Damaged", pop_voyage_list:voyage_ship_state_4);
    } else {
        if_settext("Sailing", pop_voyage_list:voyage_ship_state_4);
    }
    $string3 = "This ship is currently on a mission.";
} else {
    if_sethide(true, pop_voyage_list:voyage_ship_gone_4);
    if_settext("Ready", pop_voyage_list:voyage_ship_state_4);
    if (%pop_voyage_list_ship = 4) {
        if_setgraphic("aif_pop_ship_icons,2", pop_voyage_list:voyage_select_ship_4);
        if (~script7294(4) ! -1) {
            if_sethide(true, pop_voyage_list:voyage_send_no);
        } else {
            $string3 = "This ship currently has no captain assigned.";
        }
    }
}
switch_int (%pop_voyage_list_ship) {
    case 1 :
        if_setonmouserepeat("script8799($string0, event_com, -1)", pop_voyage_list:voyage_send_no);
    case 2 :
        if_setonmouserepeat("script8799($string1, event_com, -1)", pop_voyage_list:voyage_send_no);
    case 3 :
        if_setonmouserepeat("script8799($string2, event_com, -1)", pop_voyage_list:voyage_send_no);
    case 4 :
        if_setonmouserepeat("script8799($string3, event_com, -1)", pop_voyage_list:voyage_send_no);
}
~script2022;
