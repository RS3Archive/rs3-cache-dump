// 16959
[proc,trh186_overlay_update]
def_dbrow $dbrow0 = ~trh_chest_get_dbrow(60);
def_component $component1 = null;
def_int $int2 = 0;
while (++$int2 <= 6) {
    $component1 = ~script10947($int2);
    if (~script10948($int2) = 1) {
        if_sethide(true, $component1);
    } else {
        if_sethide(false, $component1);
    }
    if_setopcursor(1, wof_trh_key_cursor, $component1);
    ~script2690($component1, $dbrow0);
}
