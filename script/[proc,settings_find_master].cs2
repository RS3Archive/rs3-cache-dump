// 2513
[proc,settings_find_master](dbrow $dbrow0)(int)
def_enum $enum1 = ~script20382;
def_int $int2 = 0;
def_int $int3 = enum_getoutputcount($enum1);
def_int $int4 = 0;
def_int $int5 = 0;
def_dbrow $dbrow6 = null;
while ($int2 < $int3) {
    $dbrow6 = enum(int, dbrow, $enum1, $int2);
    $int4 = 0;
    $int5 = db_getfieldcount($dbrow6, settings_category:col3);
    while ($int4 < $int5) {
        if (db_getfield($dbrow6, settings_category:col3, $int4) = $dbrow0) {
            return(enum_getreverseindex(dbrow, int, settings_all_categories, $dbrow6, 0));
        }
        $int4 = calc($int4 + 1);
    }
    $int2 = calc($int2 + 1);
}
return(1023);
