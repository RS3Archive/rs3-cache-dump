// 11957
[proc,script11957](int $int0, string $string0)
def_int $int1 = 0;
if (if_find(friendschatsetup:chat_frame) = true) {
    if (cc_param(friends_sort_mode) = -1) {
        return;
    }
    if (cc_param(friends_sort_mode) = $int0 & compare(%friend_lastfilter, $string0) = 0) {
        return;
    }
    %friend_lastfilter = $string0;
    cc_setparam(friends_sort_mode, $int0);
    switch_int ($int0) {
        case 0 :
            $int1 = 0;
        case 1 :
            $int1 = cc_param(friends_sort_atoz);
        case 2 :
            $int1 = cc_param(friends_sort_ztoa);
        case 3 :
            $int1 = cc_param(friends_sort_rank_atoz);
        case 4 :
            $int1 = cc_param(friends_sort_rank_ztoa);
        case default :
            return;
    }
} else {
    return;
}
def_int $int2 = 0;
def_int $int3 = friend_count;
def_boolean $boolean4 = false;
if ($int3 > 1) {
    while ($int1 ! -1) {
        if (cc_find(friendschatsetup:rank, $int1) = true) {
            cc_setposition(0, $int2, ^setpos_abs_left, ^setpos_abs_top);
        }
        if (cc_find(friendschatsetup:icon, $int1) = true) {
            cc_setposition(0, $int2, ^setpos_abs_left, ^setpos_abs_top);
        }
        if (cc_find(friendschatsetup:name, $int1) = true) {
            cc_setposition(0, $int2, ^setpos_abs_left, ^setpos_abs_top);
            switch_int ($int0) {
                case 0 :
                    if (++$int1 >= $int3) {
                        $int1 = -1;
                    }
                case 1 :
                    $int1 = cc_param(friends_sort_atoz);
                case 2 :
                    $int1 = cc_param(friends_sort_ztoa);
                case 3 :
                    $int1 = cc_param(friends_sort_rank_atoz);
                case 4 :
                    $int1 = cc_param(friends_sort_rank_ztoa);
                case default :
                    return;
            }
            $boolean4 = cc_gethide;
        } else {
            return;
        }
        if ($boolean4 = false) {
            $int2 = calc($int2 + 16);
        }
    }
}
if (if_getscrolly(friendschatsetup:chat_scroller) > $int2) {
    if_setscrollpos(0, 0, friendschatsetup:chat_scroller);
}
if ($int2 > if_getheight(friendschatsetup:chat_scroller)) {
    if_setscrollsize(0, $int2, friendschatsetup:chat_scroller);
} else {
    if_setscrollsize(0, 0, friendschatsetup:chat_scroller);
}
~rs3_scrollbar_vertical(friendschatsetup:scrollbar, friendschatsetup:chat_scroller);
