// 12291
[proc,script12291](int $int0)(string)
if ($int0 < 0) {
    return("");
}
def_int $int1 = calc($int0 / 10);
$int0 = calc($int0 % 10);
def_int $int2 = calc($int1 / 86400);
$int1 = calc($int1 % 86400);
def_int $int3 = calc($int1 / 3600);
$int1 = calc($int1 % 3600);
def_int $int4 = calc($int1 / 60);
$int1 = calc($int1 % 60);
if ($int2 > 0) {
    if ($int3 > 0) {
        return("<tostring($int2)>d <tostring($int3)>h");
    } else {
        return("<tostring($int2)>d");
    }
} else if ($int3 > 0) {
    if ($int4 > 0) {
        return("<tostring($int3)>h <tostring($int4)>m");
    } else {
        return("<tostring($int3)>h");
    }
} else if ($int4 >= 5) {
    return("<tostring($int4)>m");
} else if ($int4 > 0) {
    if ($int1 > 0) {
        return("<tostring($int4)>m <tostring($int1)>s");
    } else {
        return("<tostring($int4)>m");
    }
} else {
    if ($int0 > 0) {
        return("<tostring($int1)>.<tostring($int0)>s");
    }
    if ($int1 > 0) {
        return("<tostring($int1)>s");
    }
    return("");
}
