// 18932
[proc,script18932](dbrow $dbrow0)(int)
def_int $int1 = 0;
switch_dbrow ($dbrow0) {
    case uitutorial_helpbutton_adventures_calendar_4 :
        if (cc_find(calendar_main:entries_container, 0) = false) {
            $int1 = 1;
        }
    case default :
        $int1 = db_getfield(%uitutorial_current_step, uitutorial_step:col9, 0);
}
return($int1);
