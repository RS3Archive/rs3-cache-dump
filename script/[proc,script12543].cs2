// 12543
[proc,script12543](int $int0)
def_int $int1 = enum(int, int, toplevel_v2_layout_ids, $int0);
if ($int1 = 15) {
    $int1 = ~script16635($int1, 0);
}
if (~script8699($int1) = 0) {
    ~script1296("That layout doesn't have any data saved to it yet. Pick another one.");
} else if (~script15532(0) = true) {
    ~script1296("Switching presets is disabled within the tutorial.");
} else if (%toplevel_v2_mobile_interface = 1) {
    ~script1296("That feature is disabled on Mobile.");
} else if (%toplevel_v2_legacy_interface = 1) {
    ~script1296("That feature is disabled in Classic Mode.");
} else {
    %toplevel_v2_edit_mode_last_dropdown_layout = $int1;
    %interface_sharing_chosen_layout_id = -1;
    ~script8741($int1);
    ~script19723($int1);
    ~script20526(1002, struct_param(toplevel_v2_window_data_ribbon, toplevel_v2_window_layer));
    ~script8144;
}
