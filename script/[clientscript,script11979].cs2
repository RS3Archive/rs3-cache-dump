// 11979
[clientscript,script11979]
~script11093(%rewardshop_tab);
if (%rewardshop_selected > 0) {
    if_sethide(false, rewardshop:confirm_buy);
    sound_vorbis_volume(select_shop_item, 1, 0, 150);
} else {
    if_sethide(true, rewardshop:confirm_buy);
}
