// 7779
[proc,clan_cup_state](struct $struct0)(int)
def_int $int1 = date_runeday;
if ($int1 >= struct_param($struct0, clan_cup_conclusion_runeday)) {
    return(5);
}
if ($int1 >= struct_param($struct0, clan_cup_result_runeday)) {
    return(4);
}
if ($int1 >= struct_param($struct0, clan_cup_end_runeday)) {
    return(3);
}
if ($int1 >= struct_param($struct0, clan_cup_start_runeday)) {
    return(2);
}
if ($int1 >= struct_param($struct0, clan_cup_start_reminder_runeday)) {
    return(1);
} else {
    return(5);
}
