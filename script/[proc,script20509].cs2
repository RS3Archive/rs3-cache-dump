// 20509
[proc,script20509](int $int0)
def_int $int1 = 0;
def_int $int2 = enum_getoutputcount(toplevel_v2_layout_names_save);
def_string $string0 = enum(int, string, toplevel_v2_layout_names_save, $int0);
def_string $string1 = "";
def_int $int3 = 0;
def_int $int4 = 0;
if (if_find(toplevel_v2_edit_mode:dropdown_save_slot) = true) {
    cc_list_clear;
    while ($int1 < $int2) {
        $int3 = enum(int, int, toplevel_v2_layout_ids_save, $int1);
        $string1 = enum(int, string, share_preset_layout_names, ~script19718($int3));
        if (string_length($string1) = 0) {
            $string1 = enum(int, string, toplevel_v2_layout_names_save, $int1);
        }
        if (string_length($string1) > 0) {
            cc_list_addentry($int3, "To: <$string1>", -1);
            if ($int0 = $int3) {
                cc_list_setisselected($int4, true, false);
            }
            $int4 = calc($int4 + 1);
        }
        $int1 = calc($int1 + 1);
    }
}
