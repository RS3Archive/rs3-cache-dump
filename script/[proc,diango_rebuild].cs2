// 829
[proc,diango_rebuild]
~diango_items(diango_items:holiday_items);
if (%p_dialog_mode = 8) {
    ~diango_search_timer("", clientclock, diango_items:text_input_box, diango_items:holiday_items, 0);
}
