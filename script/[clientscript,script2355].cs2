// 2355
[clientscript,script2355](int $int0)
def_model $model1 = null;
if ($int0 = 0) {
    if (%pop_captain_temp_legs = enum_getoutputcount(pop_captain_legs)) {
        if (~script2270 = 1) {
            $model1 = enum(int, model, pop_captain_legs_female, 0);
        } else {
            $model1 = enum(int, model, pop_captain_legs, 0);
        }
    } else if (~script2270 = 1) {
        $model1 = enum(int, model, pop_captain_legs_female, %pop_captain_temp_legs);
    } else {
        $model1 = enum(int, model, pop_captain_legs, %pop_captain_temp_legs);
    }
} else if (%pop_captain_temp_legs = 0) {
    if (~script2270 = 1) {
        $model1 = enum(int, model, pop_captain_legs_female, enum_getoutputcount(pop_captain_legs));
    } else {
        $model1 = enum(int, model, pop_captain_legs, enum_getoutputcount(pop_captain_legs));
    }
} else if (~script2270 = 1) {
    $model1 = enum(int, model, pop_captain_legs_female, %pop_captain_temp_legs);
} else {
    $model1 = enum(int, model, pop_captain_legs, %pop_captain_temp_legs);
}
if_setmodel($model1, pop_captain_customise:legs);
