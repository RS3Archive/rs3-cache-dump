// 7538
[proc,script7538](string $string0, string $string1, struct $struct0, int $int1)
def_int $int2 = struct_param($struct0, slayer_contract_level);
def_string $string2 = "Level <tostring($int2)> Slayer";
cc_deleteall(slayer_contracts_menu:confirm_button);
def_int $int3 = 0;
def_int $int4 = 1;
def_stat $stat5 = struct_param($struct0, slayer_contract_extra_stat_1);
def_int $int6 = struct_param($struct0, slayer_contract_extra_level_1);
if (stat_base(slayer) < $int2) {
    $string2 = append("<col=ff0000>", append($string2, "</col>"));
    if_sethide(false, slayer_contracts_menu:positive_button_disabled_layer);
    if_setonclick("script7539($int2)", slayer_contracts_menu:positive_button_active_layer);
    $int4 = 0;
}
if ($stat5 ! null) {
    if (stat_base($stat5) < $int6) {
        $string2 = "<$string2><br><col=ff0000>Level <tostring($int6)> <enum(stat, string, stat_to_string, $stat5)></col>";
        $int4 = 0;
    } else {
        $string2 = "<$string2><br>Level <tostring($int6)> <enum(stat, string, stat_to_string, $stat5)>";
    }
}
if ($int4 = 1) {
    $int3 = 0;
    while ($int3 < $int1) {
        cc_create(slayer_contracts_menu:confirm_button, ^iftype_rectangle, $int3);
        cc_sethide(true);
        $int3 = calc($int3 + 1);
    }
    cc_create(slayer_contracts_menu:confirm_button, ^iftype_rectangle, $int1);
    cc_setsize(0, 0, ^setsize_minus, ^setsize_minus);
    cc_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_centre);
    cc_settrans(255);
    cc_setop(1, "Confirm");
    cc_setonop("script7540(event_op)");
    if_sethide(true, slayer_contracts_menu:positive_button_disabled_layer);
    if_setonclick(null, slayer_contracts_menu:positive_button_active_layer);
}
if_settext(append($string0, append($string2, $string1)), slayer_contracts_menu:confirm_text);
def_int $int7 = calc(paraheight(if_gettext(slayer_contracts_menu:confirm_text), if_getwidth(slayer_contracts_menu:confirm_text), museo_sans_11pt_regular) * 13 + 2);
if_setsize(if_getwidth(slayer_contracts_menu:confirm_container), max(calc($int7 + 44), 130), ^setsize_abs, ^setsize_abs, slayer_contracts_menu:confirm_container);
