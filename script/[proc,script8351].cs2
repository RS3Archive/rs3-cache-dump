// 8351
[proc,script8351](int $int0)(boolean)
def_int $int1 = 1;
while ($int0 > 31) {
    $int0 = calc($int0 - 32);
    $int1 = calc($int1 + 1);
}
switch_int ($int1) {
    case 1 :
        if (testbit(%toplevel_v2_redraw_tabs_initialized_1, $int0) = ^false) {
            %toplevel_v2_redraw_tabs_initialized_1 = setbit(%toplevel_v2_redraw_tabs_initialized_1, $int0);
            return(false);
        } else {
            return(true);
        }
    case 2 :
        if (testbit(%toplevel_v2_redraw_tabs_initialized_2, $int0) = ^false) {
            %toplevel_v2_redraw_tabs_initialized_2 = setbit(%toplevel_v2_redraw_tabs_initialized_2, $int0);
            return(false);
        } else {
            return(true);
        }
    case 3 :
        if (testbit(%toplevel_v2_redraw_tabs_initialized_3, $int0) = ^false) {
            %toplevel_v2_redraw_tabs_initialized_3 = setbit(%toplevel_v2_redraw_tabs_initialized_3, $int0);
            return(false);
        } else {
            return(true);
        }
    case 4 :
        if (testbit(%toplevel_v2_redraw_tabs_initialized_4, $int0) = ^false) {
            %toplevel_v2_redraw_tabs_initialized_4 = setbit(%toplevel_v2_redraw_tabs_initialized_4, $int0);
            return(false);
        } else {
            return(true);
        }
    case default :
        return(true);
}
