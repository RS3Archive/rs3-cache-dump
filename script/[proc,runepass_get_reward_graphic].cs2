// 3638
[proc,runepass_get_reward_graphic](struct $struct0, obj $obj1, int $int2)(graphic)
if ($obj1 ! null & $obj1 ! runepass_null) {
    if ($int2 = 1) {
        return(oc_param($obj1, runepass_reward_icon));
    } else {
        return(oc_param($obj1, runepass_reward_s_icon));
    }
}
if ($struct0 ! null & $struct0 ! runepass_null) {
    if ($int2 = 1) {
        return(struct_param($struct0, runepass_reward_icon));
    } else {
        return(struct_param($struct0, runepass_reward_s_icon));
    }
}
return(null);
