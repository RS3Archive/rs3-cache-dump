// 5879
[proc,wof_startup]
%wof_slot_spinner = 0;
def_int $int0 = 82116608;
def_int $int1 = max(0, ~script6950);
def_string $string0 = "Whether to use a single key, or to use one after another.";
def_struct $struct2 = ~trh_promo_current;
if (platformtype ! ^platformtype_default & platformtype ! ^platformtype_jagex) {
    if_setsize(48, 48, ^setsize_abs, ^setsize_abs, wheel_of_fortune:wheel_close_button_layer);
    if_setposition(1, 1, ^setpos_abs_right, ^setpos_abs_top, wheel_of_fortune:wheel_close_button_layer);
    if_setonclick("script7774($string0, wheel_of_fortune:multi_wrapper, -1, 0)", wheel_of_fortune:multi_wrapper);
}
switch_struct ($struct2) {
    case trh87_promo :
        if_sethide(true, wheel_of_fortune:chests_layer);
        if_sethide(true, wheel_of_fortune:ticker_tape_viewport);
        if_sethide(true, wheel_of_fortune:foreground_layer);
        if_sethide(true, wheel_of_fortune:foreground_grey);
        if_setgraphic(trh87_easter_background_leaf, wheel_of_fortune:background);
    case trh104_promo :
        if_sethide(true, wheel_of_fortune:chests_layer);
        if_sethide(true, wheel_of_fortune:ticker_tape_viewport);
        if_sethide(true, wheel_of_fortune:foreground_layer);
        if_sethide(true, wheel_of_fortune:foreground_grey);
        if_setgraphic(trh104_background, wheel_of_fortune:background);
        if_setgraphic(~trh104_get_map_graphic, trh104_overlay:background_map);
    case default :
        if_sethide(false, wheel_of_fortune:chests_layer);
        if_sethide(false, wheel_of_fortune:ticker_tape_viewport);
        if_sethide(false, wheel_of_fortune:foreground_grey);
        if ($struct2 = trh93_promo | $struct2 = trh99_promo) {
            if_sethide(true, wheel_of_fortune:foreground_layer);
        }
}
~trh_options_onload;
if (%wof_reward_slot > 0 | %wof_result_slot_client > 0) {
    ~wof_show_reward;
} else if ($int1 > 0) {
    ~wof_show_wheel;
} else {
    ~wof_show_wheel;
}
~script3904;
%wof_result_slot_client = -1;
if_setonvarctransmit("script5880{wof_rsspintoken_balance}", wheel_of_fortune:spins_left_text);
if_setonvartransmit("script5880{wof_main_5, wof_main_8}", wheel_of_fortune:spins_left_text);
%wof_in_progress = 0;
~script5881;
