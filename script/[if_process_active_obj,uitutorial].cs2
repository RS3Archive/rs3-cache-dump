// 16390
[if_process_active_obj,uitutorial]
if (~script16412 = true) {
    if (%uitutorial_built_height = -1) {
        %uitutorial_built_height = get_obj_overlay_height;
    }
    ~script16392(get_obj_screen_position(%uitutorial_built_height), get_obj_bounding_box);
}
