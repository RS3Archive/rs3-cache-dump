// 16946
[proc,script16946](int $int0)(dbrow)
db_listall(trh188_ingredient);
def_dbrow $dbrow1 = db_findnext;
while ($dbrow1 ! null) {
    if (db_getfield($dbrow1, trh188_ingredient:col0, 0) = $int0) {
        return($dbrow1);
    } else {
        $dbrow1 = db_findnext;
    }
}
return(null);
