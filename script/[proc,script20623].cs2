// 20623
[proc,script20623](int $int0)
if ($int0 > ~script20470) {
    ~error("Unknown tab ID when trying to trigger fakeop.");
}
if_triggerop(marketplace_currency_banner:marketplace_tab_fakeop_layer, $int0, 1);
