// 17235
[proc,script17235](dbrow $dbrow0)(string)
def_int $int1 = db_getfield($dbrow0, requirements:col7, 0);
def_int $int2 = ~comlevel;
if ($int2 < $int1) {
    return("Combat level: <tostring($int2)> / <tostring($int1)>");
}
return("");
