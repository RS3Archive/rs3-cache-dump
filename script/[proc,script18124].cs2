// 18124
[proc,script18124](int $int0)(boolean)
if (%bp3_splash_parent_layer ! null) {
    if (if_gethide(bp3_splash_embedded:bonds_overlay_layer) = false) {
        if_triggerop(bp3_splash_embedded:bonds_fake_op, enum_getreverseindex(component, int, bonds_redeem_generic_fakeop_id_to_component, bonds_redeem_generic_embedded:mainmodal_window_close_button, 0), 1);
        return(true);
    } else {
        if_triggerop(bp3_splash_embedded:close_button, -1, 1);
        return(true);
    }
}
switch_int ($int0) {
    case 0 :
        if (if_gethide(bp3_track_tab:claim_all_layer) = false) {
            if_triggerop(bp3_track_tab:claim_all_back_button, 1, 1);
            return(true);
        }
    case 1 :
        if (if_gethide(bp3_missions_tab:skill_selection_holder) = false) {
            ~script18091;
            return(true);
        }
    case 3 :
    case 2 :
        if (if_gethide(mtx_front_end_confirm:confirm_buy_universe) = false) {
            if_triggerop(mtx_front_end_confirm:confirm_buy_close, -1, 1);
            return(true);
        }
}
return(false);
