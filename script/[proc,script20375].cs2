// 20375
[proc,script20375](int $int0)(obj, int)
def_struct $struct1 = ~trh_promo_current;
if ($struct1 = trh194_promo) {
    if (%halloween_progress_bar_complete_once = 1) {
        return(~script20365($int0));
    } else {
        return(~script20364($int0));
    }
} else if ($struct1 = trh175_promo) {
    if (%halloween_progress_bar_complete_once = 1) {
        return(~script20368($int0));
    } else {
        return(~script20367($int0));
    }
} else if ($struct1 = trh168_promo) {
    return(~script20366($int0));
} else {
    ~error("Unknown promotion when finding milestone reward for halloween reward track");
}
