// 8069
[proc,script8069](int $int0)(component, int)
def_int $int1 = ~script2390($int0);
def_int $int2 = ~script2385($int0, %varclientbit_42113);
if ($int2 >= ~script9929 | $int2 = -1) {
    if (%toplevel_v2_legacy_interface = 1) {
        if (if_gethide(toplevel_v2:escape_menu) = true) {
            return(~script8069(9));
        }
        return(escape_menu:parents_layer, -1);
    }
    if ($int2 ! -1) {
        if (if_gethide(toplevel_v2:ribbon_extra_context_menu_layer) = false) {
            return(toplevel_v2_ribbon_extra:window_click, $int1);
        }
        return(toplevel_v2_ribbon:extra_button, -1);
    }
    return(null, -1);
} else if (%toplevel_v2_legacy_interface = 0 & %toplevel_v2_mobile_interface = 0 & $int1 ! -1) {
    $int1 = $int0;
}
return(toplevel_v2_ribbon:window_click, $int1);
