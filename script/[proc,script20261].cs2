// 20261
[proc,script20261](int $int0)(int)
if (%league_relic_cache_current_tier = 0) {
    return(-1);
}
def_int $int1 = 0;
def_int $int2 = 0;
if (%league_relic_cache_current_tier ! 63) {
    $int2 = %league_relic_cache_current_tier;
}
while ($int1 <= $int2) {
    if (~script20142($int1) = true) {
        return($int1);
    }
    $int1 = calc($int1 + 1);
}
return(min($int1, calc($int0 - 1)));
