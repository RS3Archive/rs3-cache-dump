// 10988
[proc,cheevo_get_tooltip](achievement $achievement0)(string)
def_string $string0 = "";
def_string $string1 = "";
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 0;
def_int $int4 = 1;
def_boolean $boolean5 = false;
if (achievement_allprereqmet($achievement0) = false) {
    $int4 = achievement_largest_prereq($achievement0);
    while ($int3 < $int4) {
        $int1 = achievement_stat_prereq_count($achievement0, $int3);
        $boolean5 = achievement_prereq_met($achievement0, $int3);
        $int2 = 0;
        while ($int2 < $int1) {
            $string0 = achievement_stat_prereq_description($achievement0, $int3, $int2);
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_stat_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_varp_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            $string0 = achievement_varp_prereq_description($achievement0, $int3, $int2);
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_varp_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_varbit_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            $string0 = achievement_varbit_prereq_description($achievement0, $int3, $int2);
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_varbit_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_varp_testbit_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            $string0 = achievement_varp_testbit_prereq_description($achievement0, $int3, $int2);
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_varp_testbit_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_varbit_testbit_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            $string0 = achievement_varbit_testbit_prereq_description($achievement0, $int3, $int2);
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_varbit_testbit_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_quest_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            $string0 = "Quest: <quest_getname(achievement_quest_prereq($achievement0, $int3, $int2))>";
            if (compare($string0, "") ! 0) {
                if ($boolean5 = true | achievement_quest_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int2 = 0;
        $int1 = achievement_achievement_prereq_count($achievement0, $int3);
        while ($int2 < $int1) {
            if (achievement_category($achievement0) = league) {
                $string0 = ~achievement_description(achievement_achievement_prereq($achievement0, $int3, $int2));
            } else {
                $string0 = achievement_getname(achievement_achievement_prereq($achievement0, $int3, $int2));
            }
            if (compare($string0, "") ! 0) {
                $string1 = append($string1, "Achievement: ");
                if ($boolean5 = true | achievement_achievement_prereq_met($achievement0, $int3, $int2) = true) {
                    $string1 = append($string1, "<str><$string0></str>");
                } else {
                    $string1 = append($string1, $string0);
                }
                $string1 = append($string1, "<br>");
            }
            $int2 = calc($int2 + 1);
        }
        $int3 = calc($int3 + 1);
    }
    if (compare($string1, "") ! 0) {
        $string1 = substring($string1, 0, calc(string_length($string1) - string_length("<br>")));
        $string1 = append("Requirements to Unlock:<br>", $string1);
    }
} else if ($achievement0 = cheevo_milestone_elr1 | $achievement0 = cheevo_milestone_menaphos | $achievement0 = cheevo_milestone_prif) {
    $int1 = achievement_quest_req_count($achievement0);
    while ($int2 < $int1) {
        if (achievement_quest_req_met($achievement0, $int2) = false) {
            $string1 = append($string1, "Quest Start:<br>");
            $string1 = append($string1, "<quest_param(achievement_quest_req($achievement0, $int2), quest_start_point)><br><br>");
            $string1 = append($string1, "Description:<br>");
            $string1 = append($string1, quest_param(achievement_quest_req($achievement0, $int2), quest_description));
        }
        if (calc($int2 + 1) < $int1 & compare($string1, "") ! 0) {
            $string1 = append($string1, "<br>");
        }
        $int2 = calc($int2 + 1);
    }
    $int1 = achievement_achievement_req_count($achievement0);
    while ($int2 < $int1) {
        if (achievement_achievement_req_met($achievement0, $int2) = false) {
            $string1 = append($string1, "Achievement: ");
            $string1 = append($string1, "<achievement_getname(achievement_achievement_req($achievement0, $int2))><br>");
        }
        if (calc($int2 + 1) < $int1 & compare($string1, "") ! 0) {
            $string1 = append($string1, "<br>");
        }
        $int2 = calc($int2 + 1);
    }
    if (compare($string1, "") ! 0) {
        $string1 = substring($string1, 0, calc(string_length($string1) - string_length("<br>")));
        $string1 = append("Requirements to Complete:<br>", $string1);
    }
}
return($string1);
