// 6340
[proc,script6340](int $int0)(boolean)
switch_int ($int0) {
    case 0, 1 :
        if (%carni < 20 | %carni >= 25) {
            return(false);
        }
    case 2 :
        if (%carni < 20 | %carni >= 42) {
            return(false);
        }
    case 11 :
        if (%carni < 20 | %carni >= 53 | %carni = 51) {
            return(false);
        }
    case 3 :
        if (%carni < 100) {
            return(false);
        }
    case 10 :
        if (%carni < 100 | inv_total(inv, carni_treasurechest_keyring) > 0) {
            return(false);
        }
    case 4 :
        if (%carni < 100 | %carni_rewards_sweets = 1) {
            return(false);
        }
    case 5 :
        if (%carni < 100 | %carni_riches ! 1 | %carni_reward_usedriches = 1) {
            return(false);
        }
    case 6 :
        if (%carni < 100 | %carni_riches < 2 | %carni_reward_usedriches = 1) {
            return(false);
        }
    case 7 :
        if (%carni < 100 | %carni_rewards_oakplanks = 1) {
            return(false);
        }
    case 8 :
        if (%carni < 100 | %carni_rewards_teakplanks = 1) {
            return(false);
        }
    case 9 :
        if (%carni < 100 | %carni_rewards_mahoganyplanks = 1) {
            return(false);
        }
    case 12 :
        if (%carni < 100 | %carni_rewards_lamp1 = 1) {
            return(false);
        }
    case 13 :
        if (%carni < 100 | %carni_rewards_lamp2 = 1) {
            return(false);
        }
    case default :
        return(false);
}
return(true);
