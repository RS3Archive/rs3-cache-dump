// 8536
[proc,script8536](int $int0)
if ($int0 = 1) {
    if (%meslayermode = 0 | if_hassuboverlay(toplevel_v2_chatoverlay:open_overlay, meslayer) = false | if_gethide(toplevel_v2_chatoverlay:root) = true) {
        ~script8841(10, 0);
        ~script1594;
    } else {
        %keyboard_manager_listening_input_subsystem = 10;
        if_setonkey("meslayer_onkey(event_keycode, event_keychar)", meslayer:mes_text2);
    }
} else {
    %combatv2_bar_delay_on = 1;
    if (%keyboard_manager_listening_input_subsystem = 10) {
        %keyboard_manager_listening_input_subsystem = 0;
    }
    if_setonkey(null, meslayer:mes_text2);
    ~script1594;
}
