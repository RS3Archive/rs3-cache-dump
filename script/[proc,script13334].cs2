// 13334
[proc,script13334](enum $enum0, int $int1)
cc_deleteall(achievements_sub:build_layer);
if_sethide(true, achievements_sub:cheevo_details);
def_int $int2 = 0;
if ($enum0 = null) {
    ~cheevo_setup_scrollbar($int2, $int1);
    return;
}
def_struct $struct3 = null;
def_int $int4 = enum_getoutputcount($enum0);
def_int $intarray0($int4);
def_int $intarray1($int4);
def_int $int5 = 0;
def_int $int6 = 0;
def_int $int7 = 0;
def_int $int8 = -65535;
def_int $int9 = 0;
def_int $int10 = 20000;
def_int $int11 = 15000;
if (%cheevo_toggle_complete < %cheevo_toggle_locked) {
    $int10 = 15000;
    $int11 = 20000;
}
while ($int2 < $int4) {
    $struct3 = enum(int, struct, $enum0, $int2);
    if (struct_param($struct3, cheevo_chain) > 0) {
        $struct3 = ~script13283($struct3);
    }
    $intarray0($int2) = $int2;
    $int8 = ~cheevo_obtain_progress($struct3);
    if ($enum0 = cheevo_grace_list) {
        $int9 = struct_param($struct3, cheevo_gracedate);
        if (date_runeday > $int9) {
            $int8 = -65535;
        }
    }
    switch_int ($int8) {
        case -65535 :
            $intarray1($int2) = calc($int2 + 50000);
            $int7 = calc($int7 + 1);
        case -2 :
            $intarray1($int2) = calc($int2 + $int10);
            $int5 = calc($int5 + 1);
        case -1 :
            if (struct_param($struct3, cheevo_hidden) = 0) {
                $intarray1($int2) = calc($int2 + $int11);
                $int6 = calc($int6 + 1);
            } else {
                $intarray1($int2) = calc($int2 + 50000);
                $int7 = calc($int7 + 1);
            }
        case default :
            if (struct_param($struct3, cheevo_hidden) = 0) {
                $intarray1($int2) = calc($int2 + 10000);
            } else {
                $intarray1($int2) = calc($int2 + 50000);
                $int7 = calc($int7 + 1);
            }
    }
    ~cheevo_build_slot($int2, $struct3, $int8);
    $int2 = calc($int2 + 1);
}
array_sort($int4, 1, 0);
$int2 = 0;
$int4 = calc($int4 - $int7);
if (%cheevo_toggle_complete = 1) {
    $int4 = calc($int4 - $int5);
}
if (%cheevo_toggle_locked = 1) {
    $int4 = calc($int4 - $int6);
}
while ($int2 < $int4) {
    ~cheevo_update_slot($int2, $intarray0($int2));
    $int2 = calc($int2 + 1);
}
~cheevo_setup_scrollbar($int2, $int1);
