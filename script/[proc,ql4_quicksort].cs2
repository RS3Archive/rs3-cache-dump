// 2163
[proc,ql4_quicksort](intarray $intarray0, enum $enum1, int $int2, int $int3)
def_int $int4 = calc(($int2 + $int3) / 2);
def_int $int5 = $intarray0($int4);
$intarray0($int4) = $intarray0($int3);
$intarray0($int3) = $int5;
def_int $int6 = $int2;
def_int $int7 = $int2;
def_int $int8 = -1;
def_string $string0 = null;
def_string $string1 = null;
$string1 = lowercase(~quest_sortname(enum(int, quest, $enum1, $int5)));
if (compare($string1, "") = 0) {
    $string1 = lowercase(~quest_name(enum(int, quest, $enum1, $int5)));
}
while ($int7 < $int3) {
    $string0 = lowercase(~quest_sortname(enum(int, quest, $enum1, $intarray0($int7))));
    if (compare($string0, "") = 0) {
        $string0 = lowercase(~quest_name(enum(int, quest, $enum1, $intarray0($int7))));
    }
    if (compare($string0, $string1) < calc($int7 & 1)) {
        $int8 = $intarray0($int7);
        $intarray0($int7) = $intarray0($int6);
        $intarray0($int6) = $int8;
        $int6 = calc($int6 + 1);
    }
    $int7 = calc($int7 + 1);
}
$intarray0($int3) = $intarray0($int6);
$intarray0($int6) = $int5;
if ($int2 < calc($int6 - 1)) {
    ~ql4_quicksort(intarray0, $enum1, $int2, calc($int6 - 1));
}
if (calc($int6 + 1) < $int3) {
    ~ql4_quicksort(intarray0, $enum1, calc($int6 + 1), $int3);
}
