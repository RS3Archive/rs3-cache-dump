// 18908
[proc,script18908]
def_int $int0 = ~script18854;
def_int $int1 = ~script18855($int0);
def_int $int2 = db_listall(calendar_category);
def_int $int3 = 0;
def_int $int4 = 0;
def_dbrow $dbrow5 = null;
cc_deleteall(calendar_main:entries_container);
%varclientbit_55448 = 0;
if (if_find(calendar_main:entries_container) = true) {
    while ($int4 < $int2) {
        db_find(calendar_category:col0, $int4);
        $dbrow5 = db_findnext;
        if (~script18856($int4) = false) {
            $int3 = ~script18909($int4, $dbrow5, $int0, $int1, $int3);
        }
        $int4 = calc($int4 + 1);
    }
}
