// 15973
[proc,script15973](int $int0, int $int1)(string)
def_int $int2 = ~script12422(6, 10, $int0);
def_int $int3 = ~script16472($int0);
def_int $int4 = calc($int2 / 60);
if ($int4 > 0) {
    $int2 = calc($int2 % 60);
    if ($int2 > 0 | $int3 > 0) {
        if ($int1 = 1 & $int3 > 0) {
            return("<tostring($int4)>m <tostring($int2)>.<tostring($int3)>s");
        }
        if ($int2 > 0) {
            return("<tostring($int4)>m <tostring($int2)>s");
        }
    }
    return("<tostring($int4)>m");
}
if ($int1 = 1 & $int3 > 0) {
    return("<tostring($int2)>.<tostring($int3)>s");
}
return("<tostring($int2)>s");
