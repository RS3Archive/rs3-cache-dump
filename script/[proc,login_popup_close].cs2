// 2954
[proc,login_popup_close](int $int0)
if (if_gethide(loginscreen:login_progress_popup) = false) {
    ~script15687(%loginscreen_focus_last);
    ~script15664(loginscreen:login_progress_text, loginscreen:popup_throbber, loginscreen:popup_throbber_layer);
}
if (if_gethide(loginscreen:popup) = false) {
    if_settext("", loginscreen:popup_big_button);
    ~script15664(loginscreen:popup_text, loginscreen:login_progress_popup_throbber, loginscreen:login_progress_popup_throbber_layer);
    if_sethide(true, loginscreen:popup_big_button);
    if_sethide(true, loginscreen:popup);
}
if ($int0 = 1) {
    ~loginscreen_load_overlay(-1);
    ~login_open(%loginscreen_focus_last);
}
