// 1859
[proc,script1859](int $int0)
if ($int0 = 1) {
    if_setonkey("worldmap_search_onkey(event_keycode, event_keychar, event_com)", worldmap_v2_ui:findbox_text);
    if_settext(%worldmap_findtext, worldmap_v2_ui:findbox_text);
    ~script1566(0, %worldmap_findtext, 50);
} else {
    if_setonkey(null, worldmap_v2_ui:findbox_text);
    ~script1594;
}
