// 17144
[proc,script17144](component $component0, dbrow $dbrow1)
def_int $int2 = -1;
if ($dbrow1 ! null) {
    if_setmodel(db_getfield($dbrow1, trh_chest:col4, 0), $component0);
    while (++$int2 < db_getfieldcount($dbrow1, trh_chest:col6)) {
        if_setretex($int2, db_getfield($dbrow1, trh_chest:col6, $int2), $component0);
    }
}
