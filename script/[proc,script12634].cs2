// 12634
[proc,script12634](int $setposh0, int $setposv1)
def_int $int2 = cc_param(rs3tli_component_target_width);
def_int $int3 = cc_param(rs3tli_component_target_height);
def_int $int4 = cc_param(rs3tli_component_original_width);
def_int $int5 = cc_param(rs3tli_component_original_height);
def_int $int6 = -1;
def_int $int7 = -1;
def_int $int8 = -1;
switch_int (cc_param(rs3tli_component_resize_type)) {
    case 0 :
        if ($int2 = -1 & $int3 = -1) {
            return;
        } else if ($int2 = -1) {
            $int2 = cc_getwidth;
        } else if ($int3 = -1) {
            $int3 = cc_getheight;
        }
        cc_setsize($int2, $int3, ^setsize_abs, ^setsize_abs);
    case 1 :
        if ($int2 = -1 & $int3 = -1) {
            return;
        } else if ($int2 = -1) {
            $int2 = cc_getwidth;
        } else if ($int3 = -1) {
            $int3 = cc_getheight;
        }
        cc_setsize(~script12327(cc_getwidth, $int2), ~script12327(cc_getheight, $int3), ^setsize_abs, ^setsize_abs);
    case 3 :
        if ($int2 = -1 & $int3 = -1) {
            return;
        } else if ($int2 = -1) {
            $int2 = cc_getwidth;
        } else if ($int3 = -1) {
            $int3 = cc_getheight;
        }
        $int8 = abs(cc_param(rs3tli_component_resize_step));
        $int7 = calc($int2 - cc_getwidth);
        if ($int7 ! 0) {
            $int2 = calc(~script12421($int7) * min(abs($int7), $int8));
        } else {
            $int2 = 0;
        }
        $int7 = calc($int3 - cc_getheight);
        if ($int7 ! 0) {
            $int3 = calc(~script12421($int7) * min(abs($int7), $int8));
        } else {
            $int3 = 0;
        }
        cc_setsize(calc(cc_getwidth + $int2), calc(cc_getheight + $int3), ^setsize_abs, ^setsize_abs);
    case 2 :
        if ($setposh0 = ^setpos_abs_centre & $setposv1 = ^setpos_abs_centre) {
            if ($int4 = -1) {
                $int4 = cc_getwidth;
                cc_setparam(rs3tli_component_original_width, $int4);
            }
            if ($int5 = -1) {
                $int5 = cc_getheight;
                cc_setparam(rs3tli_component_original_height, $int5);
            }
            $int6 = enum(int, int, sin_deg2percentage_enum, calc(~script12722(cc_param(rs3tli_component_resize_playback_rate), cc_param(rs3tli_component_resize_offset)) % 360));
            $int2 = calc($int4 + scale(cc_param(rs3tli_component_resize_delta_width), 100, $int6));
            $int3 = calc($int5 + scale(cc_param(rs3tli_component_resize_delta_height), 100, $int6));
            $int2 = max(1, $int2);
            $int3 = max(1, $int3);
            if ($int6 >= 0) {
                if (calc($int4 % 2) ! calc($int2 % 2)) {
                    $int2 = calc($int2 - 1);
                }
                if (calc($int5 % 2) ! calc($int3 % 2)) {
                    $int3 = calc($int3 - 1);
                }
            } else {
                if (calc($int4 % 2) ! calc($int2 % 2)) {
                    $int2 = calc($int2 + 1);
                }
                if (calc($int5 % 2) ! calc($int3 % 2)) {
                    $int3 = calc($int3 + 1);
                }
            }
            cc_setsize($int2, $int3, ^setsize_abs, ^setsize_abs);
        }
}
