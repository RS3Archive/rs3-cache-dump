// 15487
[proc,trh_promo_on_date](int $int0)(struct)
def_int $int1 = 0;
def_struct $struct2 = null;
def_enum $enum3 = null;
def_int $int4 = enum_getoutputcount(trh_current_promos);
while ($int1 < $int4) {
    $struct2 = enum(int, struct, trh_current_promos, $int1);
    $enum3 = struct_param($struct2, trh_promo_dates);
    if ($enum3 ! null & enum_getreversecount(int, $enum3, $int0) > 0) {
        return($struct2);
    } else if ($enum3 = null & date_runeday >= struct_param($struct2, trh_promo_start) & date_runeday <= struct_param($struct2, trh_promo_end)) {
        return($struct2);
    }
    $int1 = calc($int1 + 1);
}
return(null);
