// 7665
[proc,script7665](int $int0)
def_int $int1 = 0;
if ($int0 = 1) {
    $int1 = calc(paraheight(%bugreport_input, if_getwidth(bugreport_interface:input_text), museo_sans_11pt_regular) * 12 + 12);
    if ($int1 < if_getheight(bugreport_interface:input_text_layer)) {
        if_sethide(true, bugreport_interface:scrollbar_build_layer);
    } else {
        if_sethide(false, bugreport_interface:scrollbar_build_layer);
    }
    if_setscrollsize(if_getscrollwidth(bugreport_interface:input_text_layer), $int1, bugreport_interface:input_text_layer);
    ~scrollbar_vertical(bugreport_interface:scrollbar_build_layer, bugreport_interface:input_text_layer, "aif_scrollbar_dragger_4,3", "aif_scrollbar_dragger_4,0", "aif_scrollbar_dragger_4,1", "aif_scrollbar_dragger_4,2", "aif_scrollbar_arrow_4,1", "aif_scrollbar_arrow_4,0");
    ~script958(bugreport_interface:input_text, bugreport_interface:input_caret, %bugreport_input, $int0);
} else if ($int0 = 2) {
    $int1 = calc(paraheight(%bugreport_repro, if_getwidth(bugreport_interface:input_repro), museo_sans_11pt_regular) * 12 + 12);
    if ($int1 < if_getheight(bugreport_interface:repro_text_layer)) {
        if_sethide(true, bugreport_interface:scrollbar_build_layer_repro);
    } else {
        if_sethide(false, bugreport_interface:scrollbar_build_layer_repro);
    }
    if_setscrollsize(if_getscrollwidth(bugreport_interface:repro_text_layer), $int1, bugreport_interface:repro_text_layer);
    ~scrollbar_vertical(bugreport_interface:scrollbar_build_layer_repro, bugreport_interface:repro_text_layer, "aif_scrollbar_dragger_4,3", "aif_scrollbar_dragger_4,0", "aif_scrollbar_dragger_4,1", "aif_scrollbar_dragger_4,2", "aif_scrollbar_arrow_4,1", "aif_scrollbar_arrow_4,0");
    ~script958(bugreport_interface:input_repro, bugreport_interface:repro_caret, %bugreport_repro, $int0);
}
return;
