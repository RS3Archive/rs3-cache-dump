// 15497
[proc,script15497](int $int0)
def_int $int1 = ~script15460;
def_int $int2 = 1;
if ($int0 = 1) {
    cc_deleteall(wheel_of_fortune:fake_op);
    cc_create(wheel_of_fortune:fake_op, ^iftype_graphic, 0);
}
def_component $component3 = null;
while ($int2 <= $int1) {
    if ($int0 = 1) {
        cc_create(wheel_of_fortune:fake_op, ^iftype_graphic, $int2);
    }
    $component3 = enum(int, component, trh173_mosaic_crystals_fakeop_id_to_component, $int2);
    if ($component3 ! null) {
        ~script15505($component3);
    }
    $int2 = calc($int2 + 1);
}
