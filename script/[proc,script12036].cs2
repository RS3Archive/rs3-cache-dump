// 12036
[proc,script12036](component $component0, int $int1, string $string0)
def_int $int2 = 0;
if (if_find($component0) = true) {
    if (cc_param(friends_sort_mode) = -1) {
        return;
    }
    if (cc_param(friends_sort_mode) = $int1 & compare(%friend_lastfilter, $string0) = 0) {
        return;
    }
    %friend_lastfilter = $string0;
    cc_setparam(friends_sort_mode, $int1);
    switch_int ($int1) {
        case 0 :
            $int2 = 0;
            ~script14979("rs3_icon_sort,4");
        case 1 :
            $int2 = cc_param(friends_sort_atoz);
            ~script14979("rs3_icon_sort,1");
        case 2 :
            $int2 = cc_param(friends_sort_ztoa);
            ~script14979("rs3_icon_sort,6");
        case default :
            return;
    }
} else {
    return;
}
def_int $int3 = 5;
def_int $int4 = ignore_count;
def_boolean $boolean5 = false;
def_component $component6 = ~script12023($component0);
def_component $component7 = ~script12024($component0);
def_boolean $boolean8 = ~script6431;
def_int $int9 = enum(fontmetrics, int, toplevel_v2_font_heights, museo_sans_11pt_regular);
if ($boolean8 = true) {
    $int9 = enum(fontmetrics, int, toplevel_v2_font_heights, museo_sans_17pt_bold);
}
$int9 = calc($int9 + 4);
while ($int2 ! -1) {
    if (cc_find($component7, $int2) = true) {
        cc_setposition(0, $int3, ^setpos_abs_left, ^setpos_abs_top);
    }
    if (cc_find($component6, $int2) = true) {
        cc_setposition(0, $int3, ^setpos_abs_left, ^setpos_abs_top);
        switch_int ($int1) {
            case 0 :
                if (++$int2 >= $int4) {
                    $int2 = -1;
                }
            case 1 :
                $int2 = cc_param(friends_sort_atoz);
            case 2 :
                $int2 = cc_param(friends_sort_ztoa);
            case default :
                return;
        }
        $boolean5 = cc_gethide;
    } else {
        return;
    }
    if ($boolean5 = false) {
        $int3 = calc($int3 + $int9);
    }
}
~script3516($int3, ~script12021($component0), ~ignore_scrollbar_layer($component0));
