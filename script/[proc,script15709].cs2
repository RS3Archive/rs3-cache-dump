// 15709
[proc,script15709](int $int0)(component)
def_toplevelinterface $toplevelinterface1 = if_gettop;
switch_toplevelinterface (if_gettop) {
    case loginscreen :
        return(loginscreen:infobox_layer);
    case lobbyscreen :
        return(lobbyscreen:infobox_layer);
    case default :
        if ($int0 = 1) {
            return(toplevel_v2:infobox_top_layer);
        } else {
            return(toplevel_v2:infobox_bottom_layer);
        }
}
