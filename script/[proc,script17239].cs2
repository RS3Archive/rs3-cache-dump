// 17239
[proc,script17239](dbrow $dbrow0)(boolean, string)
if ($dbrow0 = null) {
    return(true, "");
}
if (db_getrowtable($dbrow0) ! requirements) {
    ~error("Invalid dbrow, pretending requirements are not met.");
    return(false, "");
}
def_string $string0 = "";
def_string $string1 = "";
def_int $int1 = 0;
def_int $int2 = 0;
if (db_getfield($dbrow0, requirements:col0, 0) = true) {
    if (map_members = false) {
        $int2 = calc($int2 + 1);
        $string1 = "Membership is required.";
    } else {
        $string1 = "<str=FFFFFE>Membership is required.";
    }
}
if (db_getfieldcount($dbrow0, requirements:col1) > 0) {
    $int1, $string1 = ~script17240($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col2) > 0) {
    $int1, $string1 = ~script17241($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col3) > 0) {
    $int1, $string1 = ~script17287($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col4) > 0) {
    $int1, $string1 = ~script17242($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col5) > 0) {
    $int1, $string1 = ~script17243($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfield($dbrow0, requirements:col6, 0) ! -1) {
    $int1, $string1 = ~script17244($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfield($dbrow0, requirements:col7, 0) ! -1) {
    $int1, $string1 = ~script17245($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col8) > 0) {
    $int1, $string1 = ~script17246($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if (db_getfieldcount($dbrow0, requirements:col9) > 0) {
    $int1, $string1 = ~script17247($dbrow0);
    $string0 = ~script17238($string0, $string1);
    $int2 = calc($int2 + $int1);
}
if ($int2 > 0) {
    return(false, $string0);
}
return(true, $string0);
