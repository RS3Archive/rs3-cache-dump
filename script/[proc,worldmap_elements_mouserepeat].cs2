// 7590
[proc,worldmap_elements_mouserepeat](mapelement $mapelement0)
def_category $category1 = mec_category($mapelement0);
def_struct $struct2 = null;
def_string $string0 = null;
def_string $string1 = null;
def_int $int3 = -1;
def_quest $quest4 = null;
def_coord $coord5 = null;
def_int $int6 = -1;
if ($category1 ! null | compare($string1, null) ! 0) {
    $string1 = mec_param($mapelement0, mep_tooltip);
    $struct2 = enum(category, struct, worldmap_mec2data, $category1);
    if ($struct2 ! null) {
        $string0 = struct_param($struct2, worldmap_key_title);
    }
    switch_category ($category1) {
        case agility_shortcut_icon :
            if (compare($string1, null) ! 0) {
                $string0 = "<$string0><br><$string1>";
            } else if (mec_param($mapelement0, mep_tooltip_int) ! -1) {
                $string0 = "<$string0><br>Level <tostring(mec_param($mapelement0, mep_tooltip_int))> Agility";
            }
        case dungeonexit_icon :
            $string0 = "Map link";
            $coord5 = mec_param($mapelement0, mep_link_coord);
            if ($coord5 ! null) {
                $string0 = "Map link<br>To <worldmap_getmapname(worldmap_getmap($coord5))>";
            }
        case dungeonentrance_icon :
            $coord5 = mec_param($mapelement0, mep_link_coord);
            if ($coord5 ! null) {
                $string0 = "Dungeon link<br>To <worldmap_getmapname(worldmap_getmap($coord5))>";
            }
        case quest_start_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                $quest4 = enum(int, quest, ql4_quests, $int3);
                if ($quest4 ! null) {
                    ~script7080($quest4, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                    return;
                }
            }
        case mining_site_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script2555(mec_param($mapelement0, membersonly), mec_param($mapelement0, mep_cheevo), mec_param($mapelement0, mep_info_id), mec_param($mapelement0, mep_info_id2), mec_param($mapelement0, mep_info_id3), mec_param($mapelement0, mep_info_id4), mec_param($mapelement0, mep_info_id5), mec_param($mapelement0, mep_info_id6), mec_param($mapelement0, mep_info_id7), mec_param($mapelement0, mep_info_id8), mec_param($mapelement0, mep_tooltip), worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case hunting_site_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script20705(mec_param($mapelement0, membersonly), mec_param($mapelement0, mep_cheevo), mec_param($mapelement0, mep_info_id), mec_param($mapelement0, mep_info_id2), mec_param($mapelement0, mep_info_id3), mec_param($mapelement0, mep_info_id4), mec_param($mapelement0, mep_info_id5), mec_param($mapelement0, mep_info_id6), mec_param($mapelement0, mep_info_id7), mec_param($mapelement0, mep_info_id8), mec_param($mapelement0, mep_tooltip), worldmap_v2_ui:mapelement_mouseover_layer, 0, 21);
                return;
            }
        case uncommon_trees_icon, rare_trees_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script13730($int3, 14, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case fishing_point_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script13730($int3, 15, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case skill_divination_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script13730($int3, 16, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case farming_patch_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script13730($int3, 17, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case dino_game_site_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script20706($int3, mec_param($mapelement0, membersonly), mec_param($mapelement0, mep_tooltip), worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case archaeology_site_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script14730($int3, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case archaeology_excavation_site_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script14731($int3, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            }
        case transportation_icon_fairyring :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                $string0 = ~script3109($int3, 93192196, 0);
            }
        case home_teleport_destination :
            $coord5 = enum(int, coord, thas_index_to_coord, mec_param($mapelement0, mep_info_id));
            if ($coord5 ! null) {
                $int6 = mec_param($mapelement0, mep_tooltip_int);
                if ($int6 = 1) {
                    if (~script6431 = true) {
                        return;
                    }
                    $string0 = "Click to teleport to this lodestone.";
                } else {
                    $string0 = "This lodestone is not yet active.";
                }
                $string0 = "<enum(coord, string, thas_coord_to_string, $coord5)><br><$string0>";
            }
        case agility_training_icon, combat_training_icon, construction_training_icon, cooking_training_icon, crafting_training_icon, dungeoneering_training_icon, farming_training_icon, firemaking_training_icon, fletching_training_icon, herblore_training_icon, hunter_training_icon, mining_training_icon, prayer_training_icon, runecrafting_training_icon, slayer_training_icon, smithing_training_icon, summoning_training_icon, thieving_training_icon, woodcutting_training_icon, runecrafting_altar_icon :
            $int3 = mec_param($mapelement0, mep_info_id);
            if ($int3 ! -1) {
                ~script13730($int3, 19, worldmap_v2_ui:mapelement_mouseover_layer, 0);
                return;
            } else if (compare($string1, null) ! 0) {
                $string0 = $string1;
            }
        case default :
            if (compare($string1, null) ! 0) {
                $string0 = $string1;
            }
    }
    if (compare($string0, null) ! 0 & compare($string0, "") ! 0) {
        ~script16334($mapelement0, $string0, worldmap_v2_ui:mapelement_mouseover_layer, 0);
    }
}
