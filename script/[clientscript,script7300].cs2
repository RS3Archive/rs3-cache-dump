// 7300
[clientscript,script7300]
def_int $int0 = 0;
def_int $int1 = 0;
$int0, $int1 = ~script4187;
def_string $string0 = "";
if ($int0 = 0) {
    $string0 = "<tostring($int1)>m";
} else {
    $string0 = "<tostring($int0)>h";
}
if (%pop_crew_rerolls > 0) {
    if_settext("Rerolls available: <tostring(%pop_crew_rerolls)>", pop_crew:crew_rerolls);
} else {
    if_settext("New crew in <$string0>", pop_crew:crew_rerolls);
}
~script13993(pop_crew:add_crew_button, null, rs3tli_3x3_button, "Add 5 Rerolls (<tostring(%pop_port_item_1_quantity)>)");
~script13994(pop_crew:add_crew_no, null, rs3tli_3x3_button, "Add 5 Rerolls (<tostring(%pop_port_item_1_quantity)>)", true);
~script1137;
