// 12436
[clientscript,script12436]
def_int $int0 = 8;
if (map_members = false) {
    $int0 = 5;
}
if (%pagepos < $int0) {
    ~script12439;
    %pagepos = calc(%pagepos + 1);
    if_sethide(false, enum(int, component, nxtwt_page_component, %pagepos));
    ~script12438;
} else if (map_members = false) {
    ~message_player("<col=E12323>You must be on a members' world to see the last 3 pages.", 2, 1);
}
