// 7310
[proc,pop_crew_available](int $int0)(int)
def_int $int1 = ~script7311($int0);
if (~pop_ship_available($int1) = 1) {
    return(1);
}
def_int $int2 = 0;
switch_int ($int1) {
    case 1 :
        $int2 = calc($int0 - 0);
        if (testbit(%pop_voyage_1_spaces, $int2) = ^false) {
            return(1);
        }
    case 2 :
        $int2 = calc($int0 - 6);
        if (testbit(%pop_voyage_2_spaces, $int2) = ^false) {
            return(1);
        }
    case 3 :
        $int2 = calc($int0 - 12);
        if (testbit(%pop_voyage_3_spaces, $int2) = ^false) {
            return(1);
        }
    case 4 :
        $int2 = calc($int0 - 18);
        if (testbit(%pop_voyage_4_spaces, $int2) = ^false) {
            return(1);
        }
    case default :
        return(1);
}
return(0);
