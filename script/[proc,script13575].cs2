// 13575
[proc,script13575](string $string0, int $int0, fontmetrics $fontmetrics1, int $int2)(string)
if (compare($string0, "") = 0) {
    return($string0);
}
def_string $string1 = "";
def_int $int3 = 0;
if (paraheight($string0, $int0, $fontmetrics1) > $int2) {
    while ($int3 < $int2) {
        if (compare($string1, "") = 0) {
            $string1 = paraline($string0, $int0, $fontmetrics1, $int3);
        } else {
            $string1 = append($string1, append(" ", paraline($string0, $int0, $fontmetrics1, $int3)));
        }
        $int3 = calc($int3 + 1);
    }
    if (compare($string1, "") ! 0) {
        if (string_length($string1) > string_length("(...)")) {
            $string1 = substring($string1, 0, calc(string_length($string1) - string_length("(...)")));
        }
        $string1 = append($string1, " (...)");
    }
    return($string1);
}
return($string0);
