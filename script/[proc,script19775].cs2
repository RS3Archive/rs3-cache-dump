// 19775
[proc,script19775](int $int0)(model, string, int, int, seq)
def_model $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model);
def_string $string0 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_caption);
def_seq $seq2 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_animation);
def_int $int3 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_zoom);
def_int $int4 = 1;
def_int $int5 = 0;
if (struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_can_be_different) = 1) {
    switch_int ($int0) {
        case 0 :
            if (stat_base(cooking) < 40) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 1 :
            if (%handsand_quest < 160) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 2 :
            if (%misc_quest < 100) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 3 :
            if (%luc2_main_quest > 370) {
                $int5 = 1;
            }
        case 5 :
            if (%ics_little_var < 26) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 6 :
            if (%luc2_hero_part_1_vis = 1) {
                $int5 = 1;
            }
        case 7 :
            if (%luc2_main_quest > 370) {
                $int5 = 1;
            }
        case 9 :
            if (stat_base(fishing) < 15) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 11 :
            if (%dream_prog < 16 | %luc2_cyrisus_recruit = 1) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 12 :
            if (%peng_quest < 135) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            } else if (%peng_rak_quest = 140) {
                $int5 = 1;
            }
        case 28 :
            if (%dream_prog < 28) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 14 :
            if (%abyssal_miniquest < 4) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 16 :
            if (%lunarfm_spells_unlocked < 8) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 18 :
            if (%mourning_quest < 2) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            } else if (%seren_roving_eluned_state = 3) {
                $int5 = 1;
            }
        case 20 :
            if (%elfcity_access = 0) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 27 :
            if (%quest_fort_slayer_main < 120) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
        case 22 :
            if (%dino_onboarding_progress < 50) {
                $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_model_blank);
                $int4 = 0;
            }
    }
    if ($int5 = 1) {
        $model1 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_different_model);
        $int4 = 1;
        $string0 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_different_caption);
        $int3 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_different_model_zoom);
        $seq2 = struct_param(enum(int, struct, contact_spell_enum, $int0), contact_spell_different_animation);
    }
}
return($model1, $string0, $int4, $int3, $seq2);
