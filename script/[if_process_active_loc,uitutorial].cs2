// 16391
[if_process_active_loc,uitutorial]
if (~script16412 = true) {
    if (%uitutorial_built_height = -1) {
        %uitutorial_built_height = get_loc_overlay_height;
    }
    ~script16392(get_loc_screen_position(%uitutorial_built_height), get_loc_bounding_box);
}
