// 14353
[proc,script14353](int $int0, int $int1)(int)
def_int $int2 = $int0;
while ($int2 < $int1) {
    if (cc_find(bank:bank_inv, $int2) = true & cc_getinvobject = bank_empty_slot) {
        return($int2);
    }
    $int2 = calc($int2 + 1);
}
return(-1);
