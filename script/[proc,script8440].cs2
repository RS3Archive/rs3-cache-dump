// 8440
[proc,script8440](component $component0, string $string0, string $string1, string $string2, string $string3)
if (cc_find($component0, 7) = true) {
    if (compare("", $string0) ! 0) {
        cc_setop(1, $string0);
        cc_setonmouserepeat("script8799($string0, event_com, event_comsubid)");
        cc_setonmouseleave("script8805");
    } else {
        cc_clearops;
        cc_setonmouserepeat(null);
        cc_setonmouseleave(null);
    }
}
if (cc_find($component0, 8) = true) {
    if (compare("", $string1) ! 0) {
        cc_setop(1, $string1);
        cc_setonmouserepeat("script8799($string1, event_com, event_comsubid)");
        cc_setonmouseleave("script8805");
    } else {
        cc_clearops;
        cc_setonmouserepeat(null);
        cc_setonmouseleave(null);
    }
}
if (cc_find($component0, 9) = true) {
    if (compare("", $string2) ! 0) {
        cc_setop(1, $string2);
        cc_setonmouserepeat("script8799($string2, event_com, event_comsubid)");
        cc_setonmouseleave("script8805");
    } else {
        cc_clearops;
        cc_setonmouserepeat(null);
        cc_setonmouseleave(null);
    }
}
if (cc_find($component0, 10) = true) {
    if (compare("", $string3) ! 0) {
        cc_setop(1, $string3);
        cc_setonmouserepeat("script8799($string3, event_com, event_comsubid)");
        cc_setonmouseleave("script8805");
    } else {
        cc_clearops;
        cc_setonmouserepeat(null);
        cc_setonmouseleave(null);
    }
}
if (cc_find($component0, 11) = true) {
    $string3 = "Filter the display.";
    cc_setop(1, "View filters");
    cc_setonmouserepeat("script8799($string3, event_com, event_comsubid)");
    cc_setonmouseleave("script8805");
}
