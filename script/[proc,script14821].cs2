// 14821
[proc,script14821]
def_dbrow $dbrow0 = ~script19772(%arch_relic_loadout, 1);
def_dbrow $dbrow1 = ~script19772(%arch_relic_loadout, 2);
def_dbrow $dbrow2 = ~script19772(%arch_relic_loadout, 3);
~arch_relic_update_info(arch_journal:relic_slot_1_icon_graphic, null, null, arch_journal:relic_slot_1_name, arch_journal:relic_slot_1_description, $dbrow0);
~arch_relic_update_info(arch_journal:relic_slot_2_icon_graphic, null, null, arch_journal:relic_slot_2_name, arch_journal:relic_slot_2_description, $dbrow1);
~arch_relic_update_info(arch_journal:relic_slot_3_icon_graphic, null, null, arch_journal:relic_slot_3_name, arch_journal:relic_slot_3_description, $dbrow2);
if (map_members = false) {
    if ($dbrow0 ! null & db_getfield($dbrow0, arch_relic:col3, 0) = true) {
        if_sethide(false, arch_journal:relic_slot_1_members_layer);
    }
    if ($dbrow1 ! null & db_getfield($dbrow1, arch_relic:col3, 0) = true) {
        if_sethide(false, arch_journal:relic_slot_2_members_layer);
    }
    if ($dbrow2 ! null & db_getfield($dbrow2, arch_relic:col3, 0) = true) {
        if_sethide(false, arch_journal:relic_slot_3_members_layer);
    }
}
