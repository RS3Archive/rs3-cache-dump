// 8450
[proc,script8450](struct $struct0)(int)
def_enum $enum1 = null;
def_int $int2 = 0;
def_int $int3 = 1;
if ($struct0 ! null) {
    $enum1 = struct_param($struct0, rewardshop_item_list);
    while ($int2 < enum_getoutputcount($enum1)) {
        if (enum(int, struct, $enum1, $int2) = null) {
            $int3 = calc($int3 + 1);
        }
        $int2 = calc($int2 + 1);
    }
}
return($int3);
