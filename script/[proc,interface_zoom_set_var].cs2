// 4204
[proc,interface_zoom_set_var](int $int0)
def_int $int1 = %mobile_zoom;
%mobile_zoom = $int0;
if ($int0 = 0) {
    if ($int1 = 5) {
        if_setsize(960, 540, ^setsize_abs, ^setsize_abs, struct_param(toplevel_v2_window_data_full_modal, toplevel_v2_window_layer));
    }
    ~script15063(false);
} else {
    ~script15063(true);
}
