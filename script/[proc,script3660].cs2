// 3660
[proc,script3660](int $int0)
if_setposition(0, calc($int0 / 2), ^setpos_abs_left, ^setpos_abs_top, runepass:back_parallax_low);
if_setposition(0, calc($int0 * 2), ^setpos_abs_left, ^setpos_abs_top, runepass:back_parallax_high);
def_dbrow $dbrow1 = ~script3608;
if ($dbrow1 = null) {
    return;
}
def_int $int2 = 0;
def_int $int3 = calc(db_getfield($dbrow1, runepass_table:col21, 0) / 2 - 1);
$int2, $int2, $int3 = db_getfield($dbrow1, runepass_table:col14, $int3);
$int3 = calc($int3 - 400 - 20);
if ($int0 <= $int3) {
    $int0 = calc($int0 * 10515 / (2 * $int3));
} else {
    $int0 = calc(10515 / 2 + ($int0 - $int3) * 10515 / (2 * (10515 - $int3)));
}
~runepass_update_slider($int0, 10515);
