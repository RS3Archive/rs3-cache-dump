// 16540
[proc,script16540]
~script16541;
def_int $int0 = ~script8403(inv_drop_areas:content_layer);
if ($int0 < 0 | $int0 > if_getwidth(inv_drop_areas:content_layer)) {
    return;
}
def_int $int1 = ~script8404(inv_drop_areas:content_layer);
if ($int1 < 0 | $int1 > if_getheight(inv_drop_areas:content_layer)) {
    ~script16541;
    return;
}
if ($int1 >= if_gety(inv_drop_areas:drop_layer) & $int1 <= calc(if_gety(inv_drop_areas:drop_layer) + if_getheight(inv_drop_areas:drop_layer))) {
    if (if_getgraphic(inv_drop_areas:drop_bg) ! prizemount_circle_red) {
        ~script16542(inv_drop_areas:drop_icon, inv_drop_areas:drop_bg, null, true, 1);
    }
} else if ($int1 >= if_gety(inv_drop_areas:alch_layer) & $int1 <= calc(if_gety(inv_drop_areas:alch_layer) + if_getheight(inv_drop_areas:alch_layer))) {
    if (if_getgraphic(inv_drop_areas:alch_bg) ! prizemount_circle_red) {
        ~script16542(inv_drop_areas:alch_icon, inv_drop_areas:alch_bg, null, true, 1);
    }
} else if ($int1 >= if_gety(inv_drop_areas:diss_layer) & $int1 <= calc(if_gety(inv_drop_areas:diss_layer) + if_getheight(inv_drop_areas:diss_layer)) & if_getgraphic(inv_drop_areas:diss_bg) ! prizemount_circle_red) {
    ~script16542(inv_drop_areas:diss_icon, inv_drop_areas:diss_bg, null, true, 1);
}
