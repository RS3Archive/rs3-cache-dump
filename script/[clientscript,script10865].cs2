// 10865
[clientscript,script10865]
def_int $int0 = scale(180, 30, min(30, %xmas2014_snowboard_power));
def_int $int1 = if_getheight(xmas2014_snowboard:power_bar_active);
def_int $int2 = if_getheight(xmas2014_snowboard:power_bar);
def_int $int3 = 1;
def_int $int4 = scale(255, 180, calc(180 - $int0));
def_int $int5 = scale(255, 180, $int0);
def_int $int6 = 0;
if ($int2 = $int0 & $int1 = $int0) {
    if_setontimer(null, xmas2014_snowboard:power_bar_container);
} else {
    if ($int2 < $int0) {
        if (calc($int0 - $int2) > 10) {
            $int3 = 5;
        }
        if_setsize(16, calc($int2 + $int3), ^setsize_abs, ^setsize_abs, xmas2014_snowboard:power_bar);
    } else if ($int2 > $int0) {
        if_setsize(16, calc($int2 - $int3), ^setsize_abs, ^setsize_abs, xmas2014_snowboard:power_bar);
    }
    $int3 = 1;
    if ($int1 < $int0) {
        if_setsize(16, calc($int1 + $int3), ^setsize_abs, ^setsize_abs, xmas2014_snowboard:power_bar_active);
    } else if ($int1 > $int0) {
        if (calc($int1 - $int0) > 10) {
            $int3 = 5;
        }
        if_setsize(16, calc($int1 - $int3), ^setsize_abs, ^setsize_abs, xmas2014_snowboard:power_bar_active);
    }
    if_setcolour(~rgb_to_hex($int4, $int5, $int6), xmas2014_snowboard:power_bar_active);
}
