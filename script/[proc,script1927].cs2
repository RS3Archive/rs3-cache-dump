// 1927
[proc,script1927](int $int0, int $int1, struct $struct2)
def_int $int3 = 0;
if (%qps_shop_last_tab = 1) {
    if_setsize(calc(227 + 84 + 180 * 6), 0, ^setsize_abs, ^setsize_minus, qps:tab2_track_layer_build);
    if_setsize(calc(227 + 84 + 180 * 6), 0, ^setsize_abs, ^setsize_minus, qps:tab2_track_layer_click);
    if_setscrollsize(calc(227 + 84 + 180 * 6), 0, qps:tab2_track_layer);
    ~scrollbar_resize_horizontal(qps:tab2_track_scrollbar, qps:tab2_track_layer, if_getscrollx(qps:tab2_track_layer));
    $int3 = max(0, calc($int0 * 180 - 84));
    ~script1624(qps:tab2_track_scrollbar, qps:tab2_track_layer, $int3, 1, 15, 0);
}
if (cc_find(qps:info_panel_build, 0) = true) {
    ~script1930(qps:info_panel_build, qps:info_panel_click, $int0, $int1, $struct2);
} else {
    ~script1928(qps:info_panel_build, qps:info_panel_click, $int0, $int1, $struct2);
}
~script1932;
