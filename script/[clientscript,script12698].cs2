// 12698
[clientscript,script12698]
def_int $int0 = 0;
def_int $int1 = 0;
def_string $string0 = "Are you sure you want to catch these crablets for XP?";
def_int $int2 = ~script12690;
def_int $int3 = 188;
def_int $int4 = 0;
def_stat $stat5 = null;
def_enum $enum6 = null;
def_int $int7 = 1;
while ($int4 < 5) {
    $int0, $int1 = ~script12691($int4);
    if (testbit(%lampforge_selected, $int4) = ^true) {
        switch_int ($int0) {
            case 1 :
                $stat5 = ~int_to_stat($int1);
                $string0 = append($string0, "<br><tostring_localised(calc(~script12692($stat5, $int2) / 10), true)> <enum(stat, string, stat_to_string, $stat5)> XP");
                $int3 = calc($int3 + 12);
            case 2 :
                $enum6 = enum(int, enum, lampforge_statgroups, $int1);
                while ($int7 <= enum_getoutputcount($enum6)) {
                    $stat5 = ~int_to_stat(enum(int, int, $enum6, $int7));
                    if ($stat5 = invention & %invent_skill_unlocked = 0) {
                        $string0 = append($string0, "<br><col=ff0000>You are not eligible to gain Invention XP.</col>");
                    } else {
                        $string0 = append($string0, "<br><col=ffcb05><tostring_localised(calc(~script12692($stat5, $int2) / 10), true)> <enum(stat, string, stat_to_string, $stat5)> XP</col>");
                    }
                    $int7 = calc($int7 + 1);
                    $int3 = calc($int3 + 12);
                }
        }
    }
    $int7 = 1;
    $int4 = calc($int4 + 1);
}
if_setsize(356, $int3, ^setsize_abs, ^setsize_abs, lampforge:confirm_box);
if_settext($string0, lampforge:confirm_info_text);
if_sethide(false, lampforge:confirmation_layer);
