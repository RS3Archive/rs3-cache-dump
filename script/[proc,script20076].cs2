// 20076
[proc,script20076](int $int0, int $int1)
def_int $int2 = -1;
while (++$int2 < $int0) {
    if (cc_find(marketplace_preview:variant_buttons_layer, $int2) = true) {
        if ($int2 = $int1) {
            cc_button_settoggled(true);
        } else {
            cc_button_settoggled(false);
        }
    }
}
