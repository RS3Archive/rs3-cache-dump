// 13274
[proc,script13274](int $int0)(int)
def_int $int1 = 1;
def_int $int2 = enum_getoutputcount(stat_to_int);
def_stat $stat3 = null;
while ($int1 <= $int2) {
    $stat3 = enum(int, stat, int_to_stat, $int1);
    if ($stat3 ! null) {
        $stat3 = enum(stat, stat, stat_f2p_list, $stat3);
        if ($stat3 ! null & stat_base($stat3) < $int0) {
            return(0);
        }
    }
    $int1 = calc($int1 + 1);
}
return(1);
