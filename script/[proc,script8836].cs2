// 8836
[proc,script8836](enum $enum0)
if (%keyboard_manager_primary_enum ! $enum0) {
    ~script8837(%keyboard_manager_primary_enum, 0);
    %keyboard_manager_primary_enum = $enum0;
}
~script8842;
~script8837(%keyboard_manager_primary_enum, 1);
