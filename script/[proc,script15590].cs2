// 15590
[proc,script15590](int $int0)
def_component $component1 = graphics_options_v2_client:scrollbar;
def_component $component2 = graphics_options_v2_client:advanced_build_area;
def_int $int3 = enum_getoutputcount(graphics_options_nxt);
def_int $int4 = calc(1 + $int3);
def_int $int5 = calc($int4 / 2);
if (calc($int4 % 2) ! 0) {
    $int5 = calc($int5 + 1);
}
def_int $int6 = calc($int5 * 28 + ($int5 - 1) * 3);
if (~is_mobile_client = false) {
    if ($int0 ! 0 & $int0 ! 4) {
        if_setscrollpos(0, 0, $component2);
        if_setsize(0, if_gety($component2), ^setsize_minus, ^setsize_minus, $component2);
        cc_deleteall($component1);
        if ($int6 > if_getheight($component2)) {
            if_setsize(16, if_gety($component2), ^setsize_minus, ^setsize_minus, $component2);
            if_setscrollsize(0, $int6, $component2);
            ~rs3_scrollbar_vertical($component1, $component2);
        }
    }
    ~graphics_options_rebuild(getwindowmode, ~graphics_options_reviewoptions, $int0);
} else {
    ~script2684;
}
