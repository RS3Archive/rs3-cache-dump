// 14670
[proc,script14670](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    return(-1);
}
def_int $int1 = ~script14673($dbrow0);
if ($int1 = 0) {
    if (db_getfield($dbrow0, arch_researcher:col9, 0) = true) {
        return(3);
    }
    return(2);
}
if (%arch_qualification <= db_getfield($dbrow0, arch_researcher:col1, 0)) {
    return(2);
}
if (~script14674($dbrow0) = 0) {
    return(1);
}
return(0);
