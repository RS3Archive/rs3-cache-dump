// 20143
[proc,script20143](dbrow $dbrow0, int $int1)(boolean)
def_int $int2 = 0;
while ($int2 < $int1) {
    if (~league_relic_active(db_getfield($dbrow0, league_relic_tier:col1, $int2)) = true) {
        return(true);
    }
    $int2 = calc($int2 + 1);
}
return(false);
