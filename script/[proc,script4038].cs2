// 4038
[proc,script4038](int $int0)
def_string $string0 = enum(int, string, pop_map_island_names, $int0);
def_string $string1 = enum(int, string, pop_map_island_tooltips, $int0);
switch_int ($int0) {
    case 2 :
        if (%pop_adventurer_1_completed > 0) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 11 :
        if (%pop_adventurer_4_completed > 1) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 17 :
        if (%pop_adventurer_1_completed > 2) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 19 :
        if (%pop_adventurer_6_completed > 2) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 24 :
        if (%pop_adventurer_6_completed > 3) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 25 :
        if (%pop_adventurer_5_completed > 3) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 26 :
        if (%pop_adventurer_4_completed > 3) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 28 :
        if (%pop_adventurer_1_completed > 3) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 31 :
        if (%pop_adventurer_3_completed > 4) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 32 :
        if (%pop_adventurer_5_completed > 4) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 34 :
        if (%pop_adventurer_quin_completed > 2) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 36 :
        if (%pop_adventurer_quin_completed > 1) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
    case 42 :
        if (%pop_adventurer_3_completed > 5) {
            $string1 = enum(int, string, pop_map_island_tooltips_after, $int0);
        }
}
if_settext($string0, pop_map:map_island_name);
if_settext($string1, pop_map:map_island_details);
def_int $int1 = enum(int, int, pop_map_island_to_zone_number, $int0);
~script2370($int1);
