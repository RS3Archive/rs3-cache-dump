// 19595
[proc,script19595](int $int0, int $int1)(int)
def_int $int2 = $int0;
while ($int2 < $int1) {
    if (cc_find(group_ironman_storage:clan_inv, $int2) = true & cc_getinvobject = bank_empty_slot) {
        return($int2);
    }
    $int2 = calc($int2 + 1);
}
return(-1);
