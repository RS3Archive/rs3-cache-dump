// 15890
[proc,script15890](enum $enum0)(int)
switch_enum ($enum0) {
    case fonts_notosans_regular :
        return(70);
    case fonts_cinzel_bold, fonts_notosans_bold :
        return(50);
    case default :
        return(0);
}
