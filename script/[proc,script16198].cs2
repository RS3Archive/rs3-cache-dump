// 16198
[proc,script16198](boolean $boolean0, int $int1)
if ($int1 ! 82116674) {
    if_setenabled($boolean0, wheel_of_fortune:probabilities_button);
}
if ($int1 ! 82116654) {
    if_setenabled($boolean0, wheel_of_fortune:get_keys_button);
}
if ($int1 ! 82116677) {
    if_sethide($boolean0, wheel_of_fortune:countdown_layer_disabled);
}
if ($int1 ! 82116873) {
    if ($boolean0 = false) {
        %wof_hotkey = 0;
        ~script8841(29, 0);
        ~wof_button_disable;
    } else {
        ~script10877;
        %wof_hotkey = 1;
        ~script8841(29, 1);
    }
}
if (~script16161 = trh211_promo) {
    if_setenabled(false, wheel_of_fortune:get_keys_button);
}
