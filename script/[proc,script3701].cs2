// 3701
[proc,script3701]
def_enum $enum0 = null;
def_string $string0 = "Do";
def_string $string1 = "anything!";
if (%crcs_agility_active = 1) {
    $enum0 = enum(int, enum, crcs_agility_lvl_request_enum, %crcs_move_requested);
    if ($enum0 ! null) {
        $string0 = enum(int, string, crcs_agility_move_request_capital, %crcs_move_requested);
        $string1 = enum(int, string, $enum0, %crcs_lvl_requested);
    }
} else if (%crcs_magic_active = 1) {
    $enum0 = enum(int, enum, crcs_magic_lvl_request_enum, %crcs_move_requested);
    if ($enum0 ! null) {
        $string0 = enum(int, string, crcs_magic_move_request_capital, %crcs_move_requested);
        $string1 = enum(int, string, $enum0, %crcs_lvl_requested);
    }
} else if (%crcs_ranged_active = 1) {
    $string0 = enum(int, string, crcs_ranged_move_request_capital, %crcs_move_requested);
    $string1 = enum(int, string, crcs_ranged_lvl_request, %crcs_lvl_requested);
} else if (%fmc_circus_poi_active = 1) {
    $string1 = enum(int, string, crcs_firemaking_lvl_poi_request, %crcs_lvl_requested);
} else if (%fmc_circus_firebreath_active = 1) {
    $string1 = enum(int, string, crcs_firemaking_lvl_breath_request, %crcs_lvl_requested);
} else if (%crcs_thieving_active = 1) {
    $string1 = enum(int, string, crcs_thieving_lvl_request, %crcs_lvl_requested);
}
def_string $string2 = append($string0, " ");
$string2 = append($string2, $string1);
if_settext($string2, crcs_info_overlay:audience_request);
