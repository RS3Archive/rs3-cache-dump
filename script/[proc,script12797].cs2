// 12797
[proc,script12797](int $int0)(boolean)
if (inv_size(death_inv) ! inv_freespace(death_inv)) {
    return(true);
}
if (inv_size(death_worn) ! inv_freespace(death_worn)) {
    return(true);
}
if ($int0 = 1 & inv_size(death_overflow) ! inv_freespace(death_overflow)) {
    return(true);
}
return(false);
