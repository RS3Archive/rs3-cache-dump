// 3415
[proc,script3415](boolean $boolean0, component $component1)
def_int $int2 = 0;
def_int $int3 = if_getnextsubid($component1);
while ($int2 < $int3) {
    if (cc_find($component1, $int2) = true) {
        if ($int2 = 16) {
            cc_setnoclickthrough(true);
        } else {
            cc_setnoclickthrough($boolean0);
        }
    }
    $int2 = calc($int2 + 1);
}
