// 16906
[proc,script16906](int $int0)(boolean)
switch_int ($int0) {
    case 1 :
        return(true);
    case 0 :
        return(true);
    case 2 :
        if (~script3112 = false) {
            return(false);
        }
        if (enum(int, int, gamemode_seasonal_fss_week_to_event_id, ~script16828(%lobby_runeday)) = -1) {
            return(false);
        }
        return(true);
    case 3 :
        if (~script3112 = false) {
            return(false);
        }
        if (enum(int, int, gamemode_seasonal_fss_week_to_event_id, calc(1 + ~script16828(%lobby_runeday))) = -1) {
            return(false);
        }
        return(true);
    case default :
        return(false);
}
