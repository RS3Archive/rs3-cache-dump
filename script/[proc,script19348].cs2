// 19348
[proc,script19348](int $int0)(int)
def_int $int1 = ~script19595(0, calc(200 - 1));
~error("Slot <tostring($int0)> is out of range (<tostring(200)>). First freeslot is <tostring(%group_ironman_storage_first_freeslot)>. Empty proc found at: <tostring($int1)>. If the free slot isn't null that will be used instead.");
if ($int1 ! -1) {
    return($int1);
}
return($int0);
