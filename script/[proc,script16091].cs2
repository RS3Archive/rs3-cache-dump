// 16091
[proc,script16091]()(struct)
if (%slayer_master = -1 | %smki_monster_struct_entry = -1 | %slayer_target = 0) {
    return(null);
}
def_enum $enum0 = enum(int, enum, slayer_master_enum, %slayer_master);
if ($enum0 = null) {
    return(null);
}
return(enum(int, struct, $enum0, %smki_monster_struct_entry));
