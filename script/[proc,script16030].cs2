// 16030
[proc,script16030](int $int0)(boolean)
switch_int ($int0) {
    case 0 :
        if (%rsraffle_entry_claimed_winning_cat_1 < %rsraffle_entry_total_cat_1_winning) {
            return(false);
        } else {
            return(true);
        }
    case 1 :
        if (%rsraffle_entry_claimed_winning_cat_2 < %rsraffle_entry_total_cat_2_winning) {
            return(false);
        } else {
            return(true);
        }
    case 2 :
        if (%rsraffle_entry_claimed_winning_cat_3 < %rsraffle_entry_total_cat_3_winning) {
            return(false);
        } else {
            return(true);
        }
    case 3 :
        if (%rsraffle_entry_claimed_winning_cat_4 < %rsraffle_entry_total_cat_4_winning) {
            return(false);
        } else {
            return(true);
        }
    case 4 :
        if (%rsraffle_entry_claimed_winning_cat_5 < %rsraffle_entry_total_cat_5_winning) {
            return(false);
        } else {
            return(true);
        }
    case 5 :
        if (%rsraffle_entry_claimed_winning_cat_6 < %rsraffle_entry_total_cat_6_winning) {
            return(false);
        } else {
            return(true);
        }
    case 6 :
        if (%rsraffle_entry_claimed_winning_cat_7 < %rsraffle_entry_total_cat_7_winning) {
            return(false);
        } else {
            return(true);
        }
    case 7 :
        if (%rsraffle_entry_claimed_winning_cat_8 < %rsraffle_entry_total_cat_8_winning) {
            return(false);
        } else {
            return(true);
        }
    case 8 :
        if (%rsraffle_entry_claimed_winning_cat_9 < %rsraffle_entry_total_cat_9_winning) {
            return(false);
        } else {
            return(true);
        }
    case 9 :
        if (%rsraffle_entry_claimed_winning_cat_10 < %rsraffle_entry_total_cat_10_winning) {
            return(false);
        } else {
            return(true);
        }
}
