// 20519
[clientscript,script20519]
def_int $int0 = ~script20518;
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = enum_getoutputcount(toplevel_v2_panel_id_listing);
def_struct $struct4 = null;
while ($int1 < $int3) {
    $int2 = enum(int, int, toplevel_v2_panel_id_listing, $int1);
    $struct4 = ~script10405($int2);
    if ($struct4 ! null) {
        if (struct_param($struct4, toplevel_v2_window_hide_borders) = true) {
            if (cc_find(toplevel_v2_edit_mode:advanced_click, $int2) = true) {
                if (cc_param(edit_mode_border_state) = 1 & $int0 = 1) {
                    ~script8393($int2, 1);
                } else {
                    ~script8393($int2, 0);
                }
            }
        } else if ($int2 = 1004 & %toplevel_v2_minimap_shape = 1) {
            ~script8393($int2, $int0);
        }
    }
    $int1 = calc($int1 + 1);
}
