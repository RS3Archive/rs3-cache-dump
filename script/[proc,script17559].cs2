// 17559
[proc,script17559](boolean $boolean0, int $rgb1, int $int2)
def_int $int3 = 0;
switch_int ($int2) {
    case 1 :
        if_sethide($boolean0, activity_progress_bar_extension:progress_bar_layer);
    case 2 :
        if_sethide($boolean0, activity_progress_bar_extension:progress_bar_2_layer);
}
if ($boolean0 = true) {
    if (if_gethide(activity_progress_bar_extension:progress_bar_layer) = false) {
        $int3 = 20;
    }
    if (if_gethide(activity_progress_bar_extension:progress_bar_2_layer) = false) {
        $int3 = 20;
    }
    if_setposition(0, $int3, ^setpos_abs_centre, ^setpos_abs_top, activity_progress_bar_extension:buff_bar_layer);
}
if ($rgb1 ! null) {
    switch_int ($int2) {
        case 1 :
            if_setcolour($rgb1, activity_progress_bar_extension:progress_bar_fill_rect);
        case 2 :
            if_setcolour($rgb1, activity_progress_bar_extension:progress_bar_2_fill_rect);
    }
}
