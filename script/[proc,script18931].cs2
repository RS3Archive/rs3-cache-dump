// 18931
[proc,script18931](dbrow $dbrow0)(int)
def_int $int1 = -1;
switch_dbrow ($dbrow0) {
    case uitutorial_helpbutton_adventures_calendar_4 :
        if (cc_find(calendar_main:entries_container, 0) = true) {
            $int1 = 0;
        }
    case default :
        $int1 = db_getfield($dbrow0, uitutorial_step:col7, 0);
}
return($int1);
