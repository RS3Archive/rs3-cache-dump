// 16948
[proc,script16948](int $int0)(dbrow)
def_int $int1 = 0;
def_dbrow $dbrow2 = null;
while (++$int1 <= 3) {
    if (~script13383($int1) = 1) {
        $dbrow2 = ~script16947($int1);
        if ($dbrow2 ! null & db_getfield($dbrow2, trh188_ingredient:col1, 0) = $int0) {
            return($dbrow2);
        }
    }
}
return(null);
