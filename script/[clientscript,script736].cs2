// 736
[clientscript,script736](int $key0, char $char1)
if ($key0 = ^key_escape) {
    ~script14987(true);
    if_closesubclient(toplevel_v2:map_loaded_tracker);
    if_setontimer("script1055(calc(clientclock + 250))", toplevel_v2:map_loaded_tracker);
    if (%loadingscreen_on = 1) {
        map_loadingscreen_settriggerpercent(0, 0);
        map_loadingscreen_settriggerpercent(20, 90);
    }
}
