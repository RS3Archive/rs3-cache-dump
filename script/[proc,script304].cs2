// 304
[proc,script304](coord $coord0)
if ($coord0 = null) {
    return;
}
sound_synth(interface_select1, 1, 0);
def_wma $wma1 = worldmap_getmap($coord0);
$wma1 = ~script2785($wma1);
if ($wma1 = null) {
    return;
}
~worldmap_overlay_clear(worldmap_v2_ui:worldmap_overlay);
%worldmap_findtext = "";
~script308(worldmap_v2_ui:findbox_text);
if_settext(worldmap_getmapname($wma1), worldmap_v2_ui:mapname);
~deltooltip_action(worldmap_v2_ui:tooltip);
worldmap_setmap_coord_override($wma1, $coord0);
if (has_html5 = true) {
    ~script8102($wma1);
    ~script7954($coord0);
} else {
    worldmap_jumptosourcecoord($coord0);
}
~worldmap_setzoom(worldmap_getconfigzoom($wma1), 1);
~script307;
