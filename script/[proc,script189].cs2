// 189
[proc,script189](int $int0, int $int1)
switch_int ($int0) {
    case 9 :
        switch_int ($int1) {
            case 3 :
                ~script8820(1);
                ~script2254;
            case 4 :
                ~music_search_close_both;
            case 1 :
                ~script8841(11, 0);
        }
    case 4 :
        if ($int1 = 5) {
            ~script8841(52, 0);
        }
    case 1 :
        switch_int ($int1) {
            case 2 :
                ~script13220;
                ~script3123;
            case 1 :
                ~script3123;
            case 3 :
                ~script3123;
                ~script15006;
            case 4 :
                ~script3123;
                ~script15006;
                if (%player_kit_mom_open_via_wardrobe = 1) {
                    ~script18531;
                }
            case 5 :
                ~script3123;
                ~script15006;
            case 6 :
                ~script3123;
                ~script15006;
        }
    case 0 :
        switch_int ($int1) {
            case 1 :
                ~script3123;
            case 3 :
                ~script3123;
            case 2 :
                %skillguide_skill_set = 0;
                %skillguide_filter_set = 0;
            case 4 :
                if (if_getscrolly(achievements_sub:build_layer) > 0) {
                    %cheevo_last_subcat_scroll_pos = if_getscrolly(achievements_sub:build_layer);
                }
                ~cheevo_reset_goto;
            case 5 :
                ~script19424;
        }
    case 5 :
        if ($int1 = 1) {
            if (%p_dialog_listener = stockmarket:search_input_listener) {
                if_setondialogabort(null, stockmarket:search_input_listen);
                ~script7206(stockmarket:search_input_listener, stockmarket:search_input_listen, stockmarket:search_input_caret, stockmarket:search_input_display, stockmarket:search_input_clickout);
            } else if (%p_dialog_listener = stockmarket:price_input_listener) {
                if_setondialogabort(null, stockmarket:price_input_listen);
                ~script7206(stockmarket:price_input_listener, stockmarket:price_input_listen, stockmarket:price_input_caret, stockmarket:price_input_display, stockmarket:price_input_clickout);
            } else if (%p_dialog_listener = stockmarket:quantity_input_listener) {
                if_setondialogabort(null, stockmarket:quantity_input_listen);
                ~script7206(stockmarket:quantity_input_listener, stockmarket:quantity_input_listen, stockmarket:quantity_input_caret, stockmarket:quantity_input_display, stockmarket:quantity_input_clickout);
            }
        } else if ($int1 = 4 & %p_dialog_listener = stock_favourites:search_input_listener) {
            if_setondialogabort(null, stock_favourites:search_input_listen);
            ~script7206(stock_favourites:search_input_listener, stock_favourites:search_input_listen, stock_favourites:search_input_caret, stock_favourites:search_input_display, stock_favourites:search_input_clickout);
        }
    case 7 :
        switch_int ($int1) {
            case 2 :
                ~script20650;
                ~script3123;
            case 3 :
                %marketplace_bonds_side_tab_id = 0;
                ~script3123;
            case 1, 4 :
                ~script3123;
        }
}
if (%uitutorial_current_parent = null) {
    return;
}
def_int $int2 = db_getfield(%uitutorial_current_parent, uitutorial:col10, 0);
def_int $int3 = db_getfield(%uitutorial_current_parent, uitutorial:col11, 0);
if ($int2 = $int0 & $int3 = $int1) {
    ~script16411;
}
