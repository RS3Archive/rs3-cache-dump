// 12471
[proc,script12471](int $int0)(dbrow)
db_find(mm16_task:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Tried to find a task for an invalid ID: <tostring_localised($int0, true)>");
}
return($dbrow1);
