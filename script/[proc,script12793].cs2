// 12793
[proc,script12793](struct $struct0, int $int1, boolean $boolean2)(string)
if (struct_param($struct0, rewardshop_is_claimable) = 1) {
    if ($boolean2 = true) {
        return("Claim X");
    }
    if ($int1 ! ^min_32bit_int) {
        if ($int1 < 2) {
            return("Claim");
        } else {
            return("Claim <tostring($int1)>");
        }
    }
    return("Claim All");
}
if (struct_param($struct0, rewardshop_is_upgrade) = 1) {
    if ($boolean2 = true) {
        return("Unlock X");
    }
    if ($int1 ! ^min_32bit_int) {
        if ($int1 < 2) {
            return("Unlock");
        } else {
            return("Unlock <tostring($int1)>");
        }
    }
    return("Unlock All");
}
if ($boolean2 = true) {
    return("Buy X");
}
if ($int1 ! ^min_32bit_int) {
    return("Buy <tostring($int1)>");
}
return("Buy All");
