// 3981
[proc,quest_max]()(int)
def_int $int0 = calc(enum_getoutputcount(ql4_quests) + 8);
if (enum(int, quest, ql4_quests, $int0) = null) {
    ~error("Latest quest ID calculated as <tostring($int0)>. That's not valid. Update [proc,quest_max]");
}
return($int0);
