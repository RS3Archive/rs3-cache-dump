// 1308
[clientscript,script1308](int $int0)
switch_int ($int0) {
    case 1 :
        %trh169_slay_clicked = 1;
        if_triggerop(wheel_of_fortune:fake_op, -1, 1);
    case 2 :
        if_triggerop(wheel_of_fortune:fake_op, -1, 2);
    case 3 :
        if_triggerop(wheel_of_fortune:fake_op, -1, 3);
    case 4 :
        if_triggerop(wheel_of_fortune:fake_op, -1, 4);
    case 5 :
        if_triggerop(wheel_of_fortune:fake_op, -1, 5);
    case 6 :
        if_triggerop(wheel_of_fortune:fake_op, -1, 6);
    case 7 :
        %trh169_slay_clicked = 1;
        if_triggerop(wheel_of_fortune:fake_op, -1, 7);
}
