// 11480
[proc,inv_freespace_range](inv $inv0, int $int1, int $int2)(int)
if ($inv0 = null | $int1 < 0 | $int2 < 0 | $int2 < $int1) {
    return(0);
}
$int2 = min($int2, inv_size($inv0));
def_int $int3 = 0;
while ($int1 < $int2) {
    if (inv_getobj($inv0, $int1) ! null) {
        $int3 = calc($int3 + 1);
    }
    $int1 = calc($int1 + 1);
}
return($int3);
