// 16025
[proc,script16025](int $int0)(int)
def_dbrow $dbrow1 = ~script16023($int0);
def_int $int2 = date_runeday;
if ($dbrow1 ! null) {
    $int2 = date_runeday_fromdate(db_getfield($dbrow1, weekend_events:col5, 0));
} else {
    ~error("Event information cannot be found from provided event ID: <tostring($int0)>.");
}
return($int2);
