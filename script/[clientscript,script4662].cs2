// 4662
[clientscript,script4662](component $component0, int $rgb1)
def_int $int2 = 0;
def_int $int3 = 0;
$int2, $int3 = ~script4661;
if ($int2 = 1 & $int3 >= 3) {
    switch_component ($component0) {
        case fremsagamulti4:multi4a :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4b :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4c :
            return;
        case fremsagamulti4:multi4d :
            return;
    }
} else if ($int2 = 2 & $int3 >= 3) {
    switch_component ($component0) {
        case fremsagamulti4:multi4c :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4d :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4a :
            return;
        case fremsagamulti4:multi4b :
            return;
    }
} else {
    switch_component ($component0) {
        case fremsagamulti4:multi4b :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4c :
            if_setcolour($rgb1, $component0);
        case fremsagamulti4:multi4a :
            return;
        case fremsagamulti4:multi4d :
            return;
    }
}
