// 1497
[proc,script1497](obj $obj0, int $int1)(int)
if ($int1 = 0) {
    return(0);
}
def_int $int2 = ~bank_find_bank($obj0, 0, 1);
def_int $int3 = 0;
if ($int2 ! -1) {
    if (cc_find(bank:bank_inv, $int2) = true) {
        if (calc(^max_32bit_int - cc_getinvcount - $int1) > 0) {
            $int3 = $int1;
            ~script13796($obj0, calc(cc_getinvcount + $int1));
        } else {
            $int3 = calc(^max_32bit_int - cc_getinvcount);
            ~script13796($obj0, ^max_32bit_int);
        }
    }
} else {
    $int2 = %bank_first_freeslot;
    if ($int2 = -1) {
        $int2 = ~script9309(%bank_used_slots);
    }
    if ($int2 < ~script3999($obj0) & cc_find(bank:bank_inv, $int2) = true) {
        $int3 = $int1;
        ~script13796($obj0, $int1);
        if ($int2 >= %bank_used_slots) {
            %bank_used_slots = calc(%bank_used_slots + 1);
            ~script9318(1);
        }
    }
}
return($int3);
