// 4217
[proc,script4217]()(int, int)
def_int $int0 = 0;
def_int $int1 = 0;
def_int $int2 = 1;
while ($int2 <= enum_getoutputcount(int_to_stat)) {
    if (enum(int, stat, int_to_stat, $int2) ! null) {
        $int0 = calc($int0 + stat_visible_xp(enum(int, stat, int_to_stat, $int2)));
        if ($int0 > 1000000000) {
            $int1, $int0 = calc($int1 + 1), calc($int0 - 1000000000);
        }
        $int2 = calc($int2 + 1);
    }
}
return($int0, $int1);
