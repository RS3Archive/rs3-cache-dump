// 2410
[proc,script2410](string $string0, cursor $cursor0, string $string1, cursor $cursor1, string $string2, cursor $cursor2, string $string3, cursor $cursor3, string $string4, cursor $cursor4, string $string5, cursor $cursor5, cursor $cursor6, string $string6, int $int7)
if ($int7 = 0) {
    if (string_length($string0) > 0) {
        cc_setop(1, $string0);
    }
    if (string_length($string1) > 0) {
        cc_setop(2, $string1);
    }
    if (string_length($string2) > 0) {
        cc_setop(3, $string2);
    }
    if (string_length($string3) > 0) {
        cc_setop(7, $string3);
    }
    if (string_length($string4) > 0) {
        cc_setop(8, $string4);
    }
    return;
}
if (string_length($string0) > 0) {
    cc_setop(1, $string0);
    if ($cursor0 ! null) {
        cc_setopcursor(1, $cursor0);
    } else {
        cc_setopcursor(1, cursor_default);
    }
}
if (string_length($string1) > 0) {
    cc_setop(2, $string1);
    if ($cursor1 ! null) {
        cc_setopcursor(2, $cursor1);
    } else {
        cc_setopcursor(2, cursor_default);
    }
}
if (string_length($string2) > 0) {
    cc_setop(3, $string2);
    if ($cursor2 ! null) {
        cc_setopcursor(3, $cursor2);
    } else {
        cc_setopcursor(3, cursor_default);
    }
}
cc_settargetverb($string5);
cc_settargetopcursor($cursor5);
cc_settargetcursors($cursor5, $cursor6);
cc_setop(10, "Examine");
if (string_length($string3) > 0) {
    cc_setop(7, $string3);
    if ($cursor3 ! null) {
        cc_setopcursor(7, $cursor3);
    } else {
        cc_setopcursor(7, cursor_default);
    }
}
if (string_length($string4) > 0) {
    cc_setop(8, $string4);
    if ($cursor4 ! null) {
        cc_setopcursor(8, $cursor4);
    } else {
        cc_setopcursor(8, cursor_default);
    }
}
