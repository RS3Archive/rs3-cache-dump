// 14
[proc,script14](int $int0)(int)
def_enum $enum1 = struct_param(%rewardshop_struct, rewardshop_item_list);
if ($enum1 = null) {
    return(0);
}
def_int $int2 = 1;
def_int $int3 = 0;
while ($int2 < $int0) {
    if (enum(int, struct, $enum1, $int3) = null) {
        $int2 = calc($int2 + 1);
    }
    $int3 = calc($int3 + 1);
}
def_int $int4 = 1;
while (enum(int, struct, $enum1, calc($int3 + $int4)) ! null) {
    $int4 = calc($int4 + 1);
}
return($int4);
