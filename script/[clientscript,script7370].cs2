// 7370
[clientscript,script7370](int $int0)
if ($int0 = 0) {
    if_settext("You have not reached this zone yet.", pop_manager:zone_text);
} else {
    if_settext("<col=ffcb05>Resource: <enum(int, string, pop_port_resource_strings, calc($int0 + 1))></col><br><enum(int, string, pop_map_zone_tooltips, $int0)>", pop_manager:zone_text);
}
