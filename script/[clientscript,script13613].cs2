// 13613
[clientscript,script13613](component $component0)
def_int $int1 = enum_getreverseindex(component, int, cheevo_category_btn_active_index, $component0, 0);
if ($int1 < 0 | $int1 > calc(enum_getoutputcount(cheevo_category_button_index) - 1)) {
    return;
}
def_component $component2 = enum(int, component, cheevo_category_btn_build, $int1);
~script14001($component2, rs3tli_3x3_button, 0, false, 0, 0);
%cheevo_last_cat_varp = 0;
cc_deleteall(achievements_sub:subcategory_container);
if_sethide(true, achievements_sub:subcategory_container);
if (cc_find(enum(int, component, cheevo_category_btn_text_index, $int1), 0) = true) {
    cc_setvflip(false);
}
def_int $int3 = 0;
def_int $int4 = 0;
while ($int3 < enum_getoutputcount(cheevo_category_button_index)) {
    $int4 = calc(2 + (45 + 2) * $int3);
    if_setposition(0, $int4, ^setpos_abs_centre, ^setpos_abs_top, enum(int, component, cheevo_category_button_index, $int3));
    $int3 = calc($int3 + 1);
}
~cheevo_populate_scrollbar(calc($int1 + 1));
def_struct $struct5 = enum(int, struct, cheevo_category_button_struct, $int1);
if ($struct5 ! null) {
    %cheevo_current_list = struct_param($struct5, cheevo_list);
} else {
    %cheevo_current_list = null;
}
if (%cheevo_current_list ! null) {
    ~script13334(%cheevo_current_list, 1);
} else {
    cc_deleteall(achievements_sub:build_layer);
}
if_setonop("script13331(event_com)", $component0);
