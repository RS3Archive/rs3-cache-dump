// 7167
[proc,script7167](int $int0, int $int1)(boolean)
def_int $int2 = 1;
def_stat $stat3 = enum(int, stat, int_to_stat, $int2);
while ($stat3 ! null) {
    if (stat_base($stat3) < $int0) {
        if ($stat3 ! invention) {
            return(false);
        }
        if ($int1 = 1) {
            return(false);
        }
    }
    $int2 = calc($int2 + 1);
    $stat3 = enum(int, stat, int_to_stat, $int2);
}
return(true);
