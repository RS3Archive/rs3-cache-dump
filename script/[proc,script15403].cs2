// 15403
[proc,script15403]()(dbrow)
def_int $int0 = db_listall(secret_santa);
if ($int0 = 0) {
    return(null);
}
def_int $int1 = 0;
def_int $int2 = 0;
def_dbrow $dbrow3 = db_findnext;
while ($dbrow3 ! null) {
    if (~script11928($dbrow3, date_runeday) = 2) {
        return($dbrow3);
    }
    $dbrow3 = db_findnext;
}
return(null);
